---
title: "MolDA: Molecular Understanding and Generation via Large Language Diffusion Model"
title_zh: MolDA：通过大语言扩散模型实现分子理解与生成
authors: "Seohyeon Shin, HanJun Choi, Jun-Hyung Park, Hongkook Kim, Mansu Kim"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04403v1"
tags: ["query:llmagt"]
score: 8.0
evidence: 用于分子理解和生成的 LLM 框架
tldr: 针对现有分子大模型采用自回归架构导致难以处理全局约束（如闭环）且易累积结构错误的问题，本文提出MolDA。该框架将自回归骨干替换为离散大语言扩散模型，结合混合图编码器提取拓扑特征，并通过Q-Former实现模态对齐。此外，还针对掩码扩散重新定义了分子结构偏好优化。MolDA在分子生成、描述和属性预测任务中显著提升了化学有效性和全局结构一致性。
selection_source: fresh_fetch
motivation: 传统的自回归分子语言模型由于严格的从左到右生成偏置，难以处理非局部全局约束并容易累积结构误差。
method: 提出MolDA框架，采用离散大语言扩散模型替代自回归骨干，并结合混合图编码器与Q-Former进行多模态对齐。
result: 实验证明MolDA在分子生成、描述生成和属性预测等任务中确保了更好的全局结构一致性和化学有效性。
conclusion: MolDA通过双向迭代去噪机制，为多模态分子理解与生成提供了一个更具鲁棒性的新范式。
---

## 摘要
大语言模型（LLMs）显著推动了分子发现的进展，但现有的多模态分子架构在根本上依赖于自回归（AR）主干网络。这种严格的从左到右归纳偏置对于生成化学有效的分子而言并非最优，因为它难以兼顾非局部全局约束（如闭环），且在顺序生成过程中往往会累积结构误差。为了解决这些局限性，我们提出了 MolDA（基于掩码扩散与掩码机制的分子语言模型），这是一种新型多模态框架，它采用离散大语言扩散模型取代了传统的自回归主干。MolDA 利用混合图编码器提取全面的结构表示，捕获局部和全局拓扑结构，并通过 Q-Former 将其对齐到语言标记空间。此外，我们专门针对掩码扩散重新构建了分子结构偏好优化的数学公式。通过双向迭代去噪，MolDA 在分子生成、分子描述和性质预测任务中确保了全局结构的一致性、化学有效性以及鲁棒的推理能力。

## Abstract
Large Language Models (LLMs) have significantly advanced molecular discovery, but existing multimodal molecular architectures fundamentally rely on autoregressive (AR) backbones. This strict left-to-right inductive bias is sub-optimal for generating chemically valid molecules, as it struggles to account for non-local global constraints (e.g., ring closures) and often accumulates structural errors during sequential generation. To address these limitations, we propose MolDA (Molecular language model with masked Diffusion with mAsking), a novel multimodal framework that replaces the conventional AR backbone with a discrete Large Language Diffusion Model. MolDA extracts comprehensive structural representations using a hybrid graph encoder, which captures both local and global topologies, and aligns them into the language token space via a Q-Former. Furthermore, we mathematically reformulate Molecular Structure Preference Optimization specifically for the masked diffusion. Through bidirectional iterative denoising, MolDA ensures global structural coherence, chemical validity, and robust reasoning across molecule generation, captioning, and property prediction.

---

## 论文详细总结（自动生成）

这篇论文提出了 **MolDA**，这是首个将离散大语言扩散模型（Discrete Large Language Diffusion Model）引入多模态分子建模的框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的分子大语言模型（LLMs）大多采用**自回归（AR）**架构。这种从左到右的生成方式在处理分子结构时存在固有缺陷：
    *   **缺乏全局视野**：分子化学有效性高度依赖非局部约束（如环状结构的闭合、化合价平衡），自回归模型在生成早期标记时无法预见未来的结构需求。
    *   **误差累积**：顺序生成过程中微小的局部错误会不断累积，导致最终生成的分子结构在全局上无效。
*   **研究背景**：扩散模型在 3D 构象生成中表现出色，但在结合自然语言理解与 2D 拓扑结构的综合分子任务中尚未得到充分探索。

### 2. 方法论
MolDA 的核心思想是利用**双向迭代去噪**代替顺序生成，以确保分子的全局结构一致性。
*   **混合图编码器（Hybrid Graph Encoder）**：结合了 **GINE**（捕获局部子结构）和 **TokenGT**（利用 Transformer 捕获长程拓扑依赖），生成统一的分子图表示。
*   **跨模态对齐（Q-Former）**：使用 32 个可学习的查询标记（Query Tokens），通过交叉注意力机制将变长的图特征压缩并对齐到 LLM 的标记空间。
*   **扩散模型主干（LLaDA）**：采用 LLaDA-8B-Instruct 作为主干。训练过程包括前向掩码（逐步将 Token 替换为 `[MASK]`）和反向去噪（预测被掩码的 Token）。
*   **分子结构偏好优化（MolPO）的重构**：针对扩散模型重新定义了偏好优化公式。通过对比原始图和扰动图（基于 MACCS 密钥修改功能团）的掩码对数似然（Log-Likelihood），强制模型更多地利用 2D 图结构信息而非仅仅依赖 1D 文本。
*   **任务自适应采样策略**：
    *   **文本任务**：采用分块扩散（Block Diffusion）和低置信度重掩码，提升文本流畅度。
    *   **分子生成任务**：采用全序列纯扩散，确保模型在生成过程中始终关注全局拓扑约束（如环闭合）。

### 3. 实验设计
*   **数据集**：整合了 SMolInstruct、Mol-Instructions、ChEBI-20 和 PubChem，共计约 **330 万个实例**，涵盖 8 类任务。
*   **任务场景**：分子性质预测（MoleculeNet）、反应预测、逆合成分析、分子描述生成（Captioning）和文本引导的分子生成。
*   **对比基准（Baselines）**：对比了 6 个主流的 7B-8B 级自回归模型，包括 Mol-LLM、ChemDFM、LlaSMol、Galactica、MolT5 和 3D-MoLM。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 40GB GPU**。
*   **精度**：采用 `bf16` 混合精度训练。
*   **训练细节**：分为三个阶段（GNN 预训练、Q-Former 对齐、全组件 MolPO 联合微调）。推理时默认使用 $T=64$ 步去噪。

### 5. 实验数量与充分性
*   **实验规模**：在 8 个下游任务上进行了广泛测试，涵盖了回归、分类、生成等多种评价指标。
*   **消融实验**：专门研究了去噪步数 $T$（32, 64, 128）对反应预测性能的影响，验证了推理开销与准确率之间的平衡。
*   **评价**：实验设计较为全面，对比了当前最先进的（SOTA）自回归分子模型，能够客观反映扩散模型在分子领域的优劣势。

### 6. 主要结论与发现
*   **结构理解占优**：MolDA 在性质预测（如 SIDER 数据集 AUROC 达 0.846）和反应预测任务中表现出色，显著优于大多数自回归基准。
*   **化学有效性更高**：通过双向上下文参考，MolDA 在处理复杂的化学约束（如逆合成）时比 AR 模型更具鲁棒性。
*   **文本生成稍逊**：在纯文本描述生成（Captioning）任务中，MolDA 的指标（如 ROUGE、METEOR）略低于顶尖的 AR 模型，说明扩散模型在语言流畅性上仍有提升空间。

### 7. 优点
*   **架构创新**：首次证明了离散语言扩散模型可以作为多模态分子建模的有效主干。
*   **解决模态失衡**：通过数学重构 MolPO，有效缓解了多模态模型中常见的“忽略图结构、仅依赖文本”的问题。
*   **全局一致性**：非自回归的生成方式天然适合具有环状和复杂约束的分子结构。

### 8. 不足与局限
*   **推理延迟**：由于需要多次迭代去噪（如 64 步），其推理速度通常慢于单次通过的自回归模型。
*   **文本流畅度**：在长文本生成任务中，扩散模型生成的语言逻辑和连贯性相较于成熟的 AR 模型仍有差距。
*   **算力需求**：扩散模型的训练和推理对计算资源的要求相对较高。

（完）
