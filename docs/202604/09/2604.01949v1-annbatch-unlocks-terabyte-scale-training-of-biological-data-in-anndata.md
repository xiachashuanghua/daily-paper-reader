---
title: annbatch unlocks terabyte-scale training of biological data in anndata
title_zh: annbatch 实现了 anndata 中 TB 级规模生物数据的训练
authors: "Ilan Gold, Felix Fischer, Lucas Arnoldt, F. Alexander Wolf, Fabian J. Theis"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01949v1"
tags: ["query:llmagt"]
score: 8.0
evidence: 针对转录组学和测序等大规模生物数据的高效训练
tldr: 随着生物数据集规模达到TB级，系统内存限制和数据访问效率成为机器学习训练的主要瓶颈。本文推出了 annbatch，这是一款专为 anndata 设计的原生小批量加载器，支持直接在磁盘存储的数据集上进行核外训练。它在保持与 scverse 生态系统兼容的同时，显著提升了单细胞转录组、显微镜成像和全基因组测序等任务的加载吞吐量，将训练时间从数天缩短至数小时，为大规模生物人工智能提供了实用的基础设施。
selection_source: fresh_fetch
motivation: 生物数据集规模的激增使得数据访问而非模型计算成为训练中的主要瓶颈，且现有格式难以高效支持大规模核外训练。
method: 开发了 annbatch，一种原生于 anndata 的小批量加载器，允许直接从磁盘加载异构、稀疏或稠密的生物数据进行训练。
result: 在多项生物学基准测试中，annbatch 将加载吞吐量提高了一个数量级，并将训练时间从几天缩短到几小时。
conclusion: annbatch 为可扩展的生物 AI 提供了高效的数据加载方案，使用户无需放弃标准数据格式即可处理超大规模数据集。
---

## 摘要
生物数据集的规模现在通常超过系统内存，使得数据访问而非模型计算成为训练机器学习模型的主要瓶颈。这一瓶颈在生物学领域尤为突出，因为广泛使用的社区数据格式必须支持异构元数据、稀疏和密集测定，以及在既定计算生态系统内的下游分析。在这里，我们介绍了 annbatch，这是一种原生于 anndata 的小批量加载器（mini-batch loader），可直接在基于磁盘的数据集上进行离存（out-of-core）训练。在单细胞转录组学、显微镜成像和全基因组测序基准测试中，annbatch 将加载吞吐量提高了一个数量级，并将训练时间从几天缩短到几小时，同时保持与 scverse 生态系统的完全兼容。Annbatch 为可扩展的生物人工智能建立了实用的数据加载基础设施，使得在不放弃标准生物数据格式的情况下，能够使用日益庞大且多样化的数据集。

## Abstract
The scale of biological datasets now routinely exceeds system memory, making data access rather than model computation the primary bottleneck in training machine-learning models. This bottleneck is particularly acute in biology, where widely used community data formats must support heterogeneous metadata, sparse and dense assays, and downstream analysis within established computational ecosystems. Here we present annbatch, a mini-batch loader native to anndata that enables out-of-core training directly on disk-backed datasets. Across single-cell transcriptomics, microscopy and whole-genome sequencing benchmarks, annbatch increases loading throughput by up to an order of magnitude and shortens training from days to hours, while remaining fully compatible with the scverse ecosystem. Annbatch establishes a practical data-loading infrastructure for scalable biological AI, allowing increasingly large and diverse datasets to be used without abandoning standard biological data formats. Github: https://github.com/scverse/annbatch

---

## 论文详细总结（自动生成）

这篇论文介绍了 **annbatch**，一个专为生物学标准数据格式 `anndata` 设计的高性能小批量数据加载框架。以下是对该论文的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：随着生物技术的发展，单细胞转录组、全基因组测序等数据集规模已达到 TB 级，远超系统内存（RAM）容量。
*   **核心痛点**：在训练生物领域深度学习模型时，瓶颈已从“模型计算”转移到了“数据加载（I/O）”。现有的高性能加载框架（如 NVIDIA Merlin）通常需要将数据转换为特定格式，这破坏了生物学界通用的 `scverse` 生态系统（基于 `anndata` 格式）的兼容性，导致模型训练与下游分析脱节。
*   **整体含义**：annbatch 旨在不改变社区标准格式的前提下，通过底层 I/O 优化，解锁大规模生物数据的核外（out-of-core）训练能力。

### 2. 论文提出的方法论
annbatch 的核心思想是**“预洗牌（Pre-shuffling）+ 伪随机块读取”**，主要包含两个组件：
*   **预洗牌器 (Pre-shuffler)**：
    *   针对磁盘上的多个 `anndata` 文件，采用延迟加载（lazy loading）技术。
    *   将数据划分为随机的连续块（contiguous chunks）读入内存，在内存中洗牌后再以 Zarr 格式写回磁盘。
    *   这种方式保证了训练时的随机性，同时允许加载器进行大块连续读取。
*   **高性能加载器 (Data Loader)**：
    *   **Zarr 后端**：利用 Rust 编写的 `zarrs-python` 桥接库，实现比传统 HDF5 更快的读取。
    *   **底层优化**：采用了 Linux 的**直接 I/O (Direct I/O)** 技术绕过内核缓存；使用**固定内存 (Pinned Memory)** 加速数据向 GPU 的传输。
    *   **异步处理**：支持异步 GPU 传输和加速洗牌，确保 GPU 计算不被 I/O 阻塞。

### 3. 实验设计
论文针对三种典型的生物数据场景进行了基准测试（Benchmark）：
*   **单细胞转录组 (scRNA-seq)**：使用 **Tahoe-100M** 数据集（约 1 亿个细胞），测试高样本量下的吞吐量。
*   **单细胞显微成像**：测试大负载（128x128x5 像素图像）下的加载速度。
*   **全基因组测序 (WGS)**：使用 **1000 Genomes** 数据集（扩展至 50 万个体），测试高维度特征（稀疏和稠密变异数据）下的表现。
*   **对比方法**：包括 `scDataset`、`MappedCollection`、`numpy memmap` 以及传统的 `h5ad` 格式读取。

### 4. 资源与算力
论文在多种硬件环境下进行了测试以确保结果的可重复性：
*   **云端实例**：AWS `ml.g4dn.4xlarge`（配备 NVIDIA T4 GPU，16 vCPUs，64GB RAM）。
*   **高性能计算 (HPC)**：de.NBI 节点（T4 GPU）和 Helmholtz Munich HPC 节点（Intel Xeon Gold 6142M）。
*   **顶级算力**：使用 **NVIDIA HGX-H100** 测试分布式训练下的扩展性。
*   **训练时长**：在 1 亿细胞数据集上，annbatch 将单轮（epoch）迭代时间从 21 小时缩短至 **17 分钟**。

### 5. 实验数量与充分性
*   **实验覆盖面**：涵盖了从单机到分布式、从稀疏矩阵到稠密图像、从云端到本地 HPC 的多种组合。
*   **充分性**：论文不仅对比了吞吐量，还分析了磁盘占用、预洗牌时间开销以及不同 Batch Size 对性能的影响。
*   **客观性**：作者明确指出在小规模数据集上 OS 缓存可能导致测试偏差，因此特意选用超大规模数据集进行“核外”测试，确保了实验的真实性和公平性。

### 6. 主要结论与发现
*   **性能飞跃**：annbatch 的吞吐量比现有方法（如 `MappedCollection`）高出约 **40 倍**。
*   **存储格式影响**：采用 Zarr 格式配合 Rust 后端，比传统的 `h5ad` (HDF5) 格式在预洗牌和加载速度上都有显著提升。
*   **成本效益**：虽然预洗牌需要数小时的初始开销，但由于训练速度的大幅提升，这种开销通常在第一个训练 Epoch 就能被抵消。
*   **分布式潜力**：annbatch 的高吞吐量足以饱和 H100 等顶级 GPU 的计算需求，解决了分布式训练中的 I/O 瓶颈。

### 7. 优点
*   **生态兼容性**：这是其最大的亮点，完全支持 `anndata` 和 `scverse` 生态，用户无需转换数据格式即可享受加速。
*   **端到端方案**：提供了从磁盘预处理到模型喂入的完整工具链。
*   **跨模态支持**：证明了该方法在组学、影像和遗传学数据上的通用性。

### 8. 不足与局限
*   **预洗牌开销**：对于仅需训练 1-2 个 Epoch 的极小模型，预洗牌的时间成本可能显得较高。
*   **采样策略受限**：由于采用连续块读取，目前难以实现复杂的加权采样（Weighted Sampling），需要未来版本通过特定的磁盘写入逻辑来解决。
*   **多模态原生支持**：目前尚未直接提供多模态（Multi-modal）数据的联合加载优化，仍需进一步开发。

（完）
