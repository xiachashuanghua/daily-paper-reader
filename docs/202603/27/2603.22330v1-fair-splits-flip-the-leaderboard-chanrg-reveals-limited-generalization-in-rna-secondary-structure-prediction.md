---
title: "Fair splits flip the leaderboard: CHANRG reveals limited generalization in RNA secondary-structure prediction"
title_zh: 公平划分颠覆排行榜：CHANRG 揭示了 RNA 二级结构预测中有限的泛化能力
authors: "Zhiyuan Chen, Zhenfeng Deng, Pan Deng, Yue Liao, Xiu Su, Peng Ye, Xihui Liu"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.22330v1"
tags: ["query:llmagt"]
score: 9.0
evidence: RNA二级结构预测与基础模型
tldr: 准确预测RNA二级结构对转录组研究至关重要，但现有深度学习模型可能因基准测试偏差而高估了泛化能力。本研究提出了CHANRG基准，包含从Rfam 15.0中筛选的17万余条结构非冗余RNA，采用结构感知去重和基因组感知划分。评估发现，基础模型在分布内表现优异，但在分布外泛化能力显著下降，而结构化解码器表现更稳健。该基准为开发具备真实泛化能力的RNA预测模型提供了更严格的评估框架。
selection_source: fresh_fetch
motivation: 现有的RNA二级结构预测基准可能因数据重叠高估了深度学习模型的泛化能力，导致排行榜结果失真。
method: 提出了CHANRG基准，通过结构感知去重和基因组感知划分，从千万级序列中构建了包含17万条非冗余RNA的大规模数据集。
result: 基础模型在分布外测试中优势大幅缩减，而结构化解码器和直接神经预测器在处理复杂结构和长序列时表现出更强的鲁棒性。
conclusion: CHANRG为RNA结构预测提供了一个更严格、抗批次干扰的评估框架，揭示了当前模型在分布外泛化方面的局限性。
---

## 摘要
准确预测 RNA 二级结构是转录组注释、非编码 RNA 机制分析和 RNA 药物设计的基础。深度学习和 RNA 基础模型近期取得的进展难以解释，因为目前的基准测试可能高估了跨 RNA 家族的泛化能力。我们提出了非编码 RNA 组综合分级注释（CHANRG），这是一个包含 170,083 个结构非冗余 RNA 的基准测试。这些数据是通过结构感知去重、基因组感知划分设计和多尺度结构评估，从 Rfam 15.0 的 1000 多万条序列中精选而来的。在 29 个预测器中，基础模型方法在留出集上达到了最高准确率，但在分布外测试中失去了大部分优势，而结构化解码器和直接神经预测器则表现出显著更强的鲁棒性。在控制序列长度后，这种差距依然存在，并反映在结构覆盖度的缺失和错误的高阶连接上。CHANRG 与无填充、对称感知的评估栈共同提供了一个更严格且批次不变的框架，用于开发具有可证明的分布外鲁棒性的 RNA 结构预测器。

## Abstract
Accurate prediction of RNA secondary structure underpins transcriptome annotation, mechanistic analysis of non-coding RNAs, and RNA therapeutic design. Recent gains from deep learning and RNA foundation models are difficult to interpret because current benchmarks may overestimate generalization across RNA families. We present the Comprehensive Hierarchical Annotation of Non-coding RNA Groups (CHANRG), a benchmark of 170{,}083 structurally non-redundant RNAs curated from more than 10 million sequences in Rfam~15.0 using structure-aware deduplication, genome-aware split design and multiscale structural evaluation. Across 29 predictors, foundation-model methods achieved the highest held-out accuracy but lost most of that advantage out of distribution, whereas structured decoders and direct neural predictors remained markedly more robust. This gap persisted after controlling for sequence length and reflected both loss of structural coverage and incorrect higher-order wiring. Together, CHANRG and a padding-free, symmetry-aware evaluation stack provide a stricter and batch-invariant framework for developing RNA structure predictors with demonstrable out-of-distribution robustness.

---

## 论文详细总结（自动生成）

这是一份关于论文《Fair splits flip the leaderboard: CHANRG reveals limited generalization in RNA secondary-structure prediction》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：RNA 二级结构预测对生物学研究和药物设计至关重要。虽然近年来基于深度学习（DL）和基础模型（FM）的方法在标准基准测试上取得了极高的分数，但研究者怀疑这些成绩可能源于基准测试的缺陷（如训练集与测试集之间存在结构冗余、序列相似性去重不彻底等），导致模型在面对真实世界的未知 RNA 家族时泛化能力不足。
*   **核心问题**：现有的排行榜是否真实反映了模型的泛化能力？当消除数据泄露并进行严格的分布外（OOD）测试时，不同类型的预测器表现如何？

### 2. 方法论
*   **CHANRG 基准构建**：
    *   **数据源**：从 Rfam 15.0 的 1000 万条原始序列出发。
    *   **结构感知去重**：不仅进行序列身份去重（<99%），还利用 `bpRNA-CosMoS` 进行结构相似性去重（相似度 <0.9），从 570 万条序列精简至 17 万条高度非冗余的结构。
    *   **分层划分设计**：
        *   **GenA (Architecture)**：测试对复杂、未分类架构的泛化。
        *   **GenC (Clans)**：测试对训练集中未出现的“家族簇（Clans）”的泛化。
        *   **GenF (Families)**：测试在基因组覆盖稀疏、家族内多样性有限的情况下的表现。
*   **技术创新（NestedTensor）**：
    *   针对 RNA 长度差异巨大的问题，开发了**无填充（Padding-free）且对称感知**的评估栈。
    *   通过 `NestedTensor` 排除计算图中的填充位置，确保预测结果仅取决于序列本身，消除了传统批处理中“批次上下文依赖（Batch-context dependence）”导致的预测不一致问题。

### 3. 实验设计
*   **数据集/场景**：使用了 CHANRG 的六个子集：Train, Validation, Test (分布内), 以及 GenA, GenC, GenF (分布外)。
*   **对比方法**：共评估了 **29 种预测器**，分为三类：
    1.  **结构化解码器 (SD)**：如 EternaFold, RNAfold, RNAstructure。
    2.  **直接神经预测器 (DL)**：如 bpFold, SPOT-RNA, UFold。
    3.  **基础模型 (FM)**：如 RiNALMo (micro/mega/giga), ERNIE-RNA, RNA-FM, AIDO.RNA。
*   **评估指标**：构建了多尺度指标梯队，包括 Base-pair F1（碱基对精度）、Stem F1（螺旋区精度）、Topology F1（拓扑结构精度）和 Topology GED（拓扑编辑距离）。

### 4. 资源与算力
*   **算力说明**：文中未详细列出所有 29 个模型训练的总算力（因为许多是已发表的基准模型），但在系统性能测试部分提到：
    *   使用其提出的 `NestedTensor` 方案，在合成批次测试中，相比传统的填充张量（Padded tensor），**推理延迟降低了 3.3 倍**，**GPU 显存占用降低了 6.7 倍**（从 4.61 GB 降至 0.69 GB）。
    *   该框架显著提升了在大规模基准测试上的评估效率。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 29 个模型在 6 个不同性质的数据集上的表现，总计处理了超过 17 万条序列。
*   **充分性与公平性**：
    *   **控制变量**：进行了序列长度匹配实验（限制在 50-200 nt），证明了 OOD 性能下降并非单纯由序列变长引起。
    *   **消融/扩展实验**：对基础模型进行了规模缩放（Scaling）分析，对比了不同参数量（micro 到 giga）的影响。
    *   **统计严谨性**：使用了 95% 自举置信区间（Bootstrap confidence intervals）来确保结论的可靠性。
    *   **客观性**：通过结构感知去重，极大地降低了数据泄露风险，使得评估比以往任何基准都更严格、更公平。

### 6. 主要结论与发现
*   **排行榜颠覆**：基础模型（FM）在分布内（Test）表现最强，但在分布外（OOD）性能大幅崩塌（性能保留率仅约 26.7%）。相比之下，结构化解码器（SD）虽然上限较低，但极其稳健（保留率 >90%）。
*   **失败模式分析**：FM 的 OOD 失败主要表现为“覆盖度缺失”（召回率极低）和“接线错误”（能识别局部螺旋但无法正确组装全局拓扑）。
*   **规模效应的局限**：增加基础模型的参数规模（Scaling）能显著提升分布内精度，但对提升 OOD 鲁棒性的贡献非常有限。
*   **批次干扰**：证明了传统深度学习模型在有填充的批处理中，预测结果会受同批次其他序列的影响（约 2% 的决策会发生翻转）。

### 7. 优点
*   **基准质量高**：首次在 RNA 领域引入了如此严格的结构感知去重和生物学驱动的 OOD 划分。
*   **评估维度深**：不满足于碱基对预测，深入到了拓扑结构和高阶连接的评估。
*   **工程实现优**：提出的 `NestedTensor` 方案不仅解决了科学上的批次一致性问题，还带来了显著的计算效率提升。

### 8. 不足与局限
*   **伪结（Pseudoknots）覆盖有限**：虽然包含伪结，但整体占比仍然较低，且当前模型对复杂伪结的预测依然普遍较差。
*   **静态结构局限**：主要关注单一二级结构，未涉及 RNA 的动态构象系综（Ensembles）或三维结构。
*   **OOD 定义的局限**：虽然划分了三个 OOD 维度，但仍无法完全涵盖自然界中所有可能的生物学新颖性。

（完）
