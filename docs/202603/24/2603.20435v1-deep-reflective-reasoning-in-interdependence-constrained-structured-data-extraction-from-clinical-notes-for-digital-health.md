---
title: Deep reflective reasoning in interdependence constrained structured data extraction from clinical notes for digital health
title_zh: 数字健康领域中受相互依赖约束的临床记录结构化数据提取的深度反思推理
authors: "Jingwei Huang, Kuroush Nezafati, Zhikai Chi, Ruichen Rong, Colin Treager, Tingyi Wanyan, Yueshuang Xu, Xiaowei Zhan, Patrick Leavey, Guanghua Xiao, Wenqi Shi, Yang Xie"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20435v1"
tags: ["query:llmagt"]
score: 8.0
evidence: 用于肿瘤数据提取的 LLM 智能体框架
tldr: 针对临床笔记中结构化数据提取面临的变量间逻辑依赖挑战，本文提出了一种“深度反思推理”LLM智能体框架。该框架通过迭代自省和修正，结合输入文本与领域知识，确保提取结果的逻辑一致性。在结直肠癌、尤文肉瘤及肺癌分期等肿瘤学应用中，该方法显著提升了提取的准确率和F1分数，为构建高质量临床数据集提供了可靠方案。
selection_source: fresh_fetch
motivation: 现有的LLM提取流程难以处理临床笔记中变量间的复杂逻辑约束，导致输出结果在临床上不一致。
method: 提出一种深度反思推理框架，通过迭代自省、修正以及与领域知识的对齐，直至输出结果收敛。
result: 在三项肿瘤学任务中，该方法显著提高了分类变量的F1值和数值变量的准确率，如肺癌分期准确率从0.680提升至0.833。
conclusion: 深度反思推理能系统性地提高受约束结构化数据提取的可靠性，促进数字健康领域的知识发现。
---

## 摘要
从临床记录中提取结构化信息需要处理相互依赖变量构成的密集网络，其中一个属性的值在逻辑上会约束其他属性。现有的基于大语言模型（LLM）的提取流程往往难以捕捉这些依赖关系，导致输出结果在临床上不一致。我们提出了深度反思推理（deep reflective reasoning），这是一种大语言模型智能体框架，通过检查变量、输入文本和检索到的领域知识之间的一致性，迭代地进行自我批判并修正结构化输出，直到输出收敛为止。我们在三种不同的肿瘤学应用中对所提方法进行了广泛评估：(1) 在基于大体描述的结直肠癌概要报告（n=217）中，反思推理将八个分类概要变量的平均 F1 分数从 0.828 提高到 0.911，并将四个数值变量的平均正确率从 0.806 提高到 0.895；(2) 在尤文肉瘤 CD99 免疫组化染色模式识别（n=200）中，准确率从 0.870 提高到 0.927；(3) 在肺癌肿瘤分期（n=100）中，肿瘤分期准确率从 0.680 提高到 0.833（pT：0.842 -> 0.884；pN：0.885 -> 0.948）。结果表明，深度反思推理可以系统地提高相互依赖约束下基于 LLM 的结构化数据提取的可靠性，从而实现更一致的机器可操作临床数据集，并促进利用机器学习和数据科学进行面向数字健康的知识发现。

## Abstract
Extracting structured information from clinical notes requires navigating a dense web of interdependent variables where the value of one attribute logically constrains others. Existing Large Language Model (LLM)-based extraction pipelines often struggle to capture these dependencies, leading to clinically inconsistent outputs. We propose deep reflective reasoning, a large language model agent framework that iteratively self-critiques and revises structured outputs by checking consistency among variables, the input text, and retrieved domain knowledge, stopping when outputs converge. We extensively evaluate the proposed method in three diverse oncology applications: (1) On colorectal cancer synoptic reporting from gross descriptions (n=217), reflective reasoning improved average F1 across eight categorical synoptic variables from 0.828 to 0.911 and increased mean correct rate across four numeric variables from 0.806 to 0.895; (2) On Ewing sarcoma CD99 immunostaining pattern identification (n=200), the accuracy improved from 0.870 to 0.927; (3) On lung cancer tumor staging (n=100), tumor stage accuracy improved from 0.680 to 0.833 (pT: 0.842 -> 0.884; pN: 0.885 -> 0.948). The results demonstrate that deep reflective reasoning can systematically improve the reliability of LLM-based structured data extraction under interdependence constraints, enabling more consistent machine-operable clinical datasets and facilitating knowledge discovery with machine learning and data science towards digital health.

---

## 论文详细总结（自动生成）

以下是对论文《Deep reflective reasoning in interdependence constrained structured data extraction from clinical notes for digital health》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：从非结构化的临床笔记（如病理报告大体描述）中提取结构化数据是数字健康和临床决策支持的基础。
*   **核心痛点**：临床变量之间存在密集的**逻辑依赖关系**（如：若“边缘受累”，则“肿瘤距边缘距离”必须为0）。现有的LLM提取流程通常将变量视为独立个体，导致输出结果在医学逻辑上不一致。
*   **研究动机**：开发一种能够模拟人类专家“自省”过程的机制，使LLM在提取过程中能够自我检查并修正逻辑矛盾，从而生成高质量、机器可操作的临床数据集。

### 2. 方法论：深度反思推理（DRR）
*   **核心思想**：构建一个LLM智能体框架，通过**迭代自省（Self-critique）**和**自我修正（Self-revision）**，确保输出变量、原始文本与领域知识三者之间的一致性。
*   **关键技术细节**：
    *   **RAG集成**：利用Llama Index将医学指南（如CAP癌症模板、AJCC分期手册）转化为Markdown格式的知识库，供模型检索。
    *   **多轮迭代流程**：
        1.  **初始化**：基于RAG进行初步的结构化提取。
        2.  **反思循环**：Agent审查前几轮的输出，识别“类型1（与事实不符）”和“类型2（变量间逻辑矛盾）”的错误。
        3.  **回溯机制（Back-view）**：在提示词中包含过去 $p$ 轮的推理历史，帮助模型跳出局部最优解或识别震荡。
        4.  **收敛判定**：当输出结果在连续轮次中保持稳定，或达到预设的最大轮次（如16轮）时停止。
*   **算法逻辑**：该方法不依赖外部环境反馈（如代码执行结果），而是纯粹的内部逻辑一致性检查。

### 3. 实验设计
*   **应用场景与数据集**：
    1.  **结直肠癌（CRC）概要报告**：217例大体描述文本，提取8个分类变量和4个数值变量。
    2.  **尤文肉瘤 CD99 染色模式**：200例扫描版病理报告，识别复杂的免疫组化模式。
    3.  **肺癌 TNM 分期**：100例临床笔记，根据AJCC7标准推断pT、pN和最终分期。
*   **Benchmark（基准对比）**：
    *   单次查询（One-by-one QA）无RAG。
    *   单次查询（One-by-one QA）结合RAG。
    *   深度反思推理（DRR）的不同迭代轮次。
*   **对比模型**：主要使用 Llama 3.3-70B，并在尤文肉瘤任务中使用了 GPT-5.1（文中指代某种先进推理模型）。

### 4. 资源与算力
*   **计算耗时**：论文明确对比了效率。在CRC任务中，单次查询（带RAG）耗时约 **7.16 小时**；而进行16轮反思推理则耗时 **18.51 小时**。
*   **Token消耗**：反思推理的输入/输出Token量显著高于单次查询（输入Token从5.8M增至6.8M，输出Token从0.48M增至1.08M）。
*   **硬件信息**：文中未明确提及具体的GPU型号和数量。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三种不同的肿瘤学应用，样本量在100到217例之间。
*   **充分性评价**：
    *   **多维度评估**：不仅评估了分类准确率（F1），还评估了数值变量的正确率、平均绝对误差（MAE）和均方根误差（RMSE）。
    *   **客观性**：所有实验均以病理学家或资深医师标注的“金标准”为基准，并提供了详细的混淆矩阵分析。
    *   **消融对比**：通过对比有无RAG、有无反思，证明了各组件的有效性。

### 6. 主要结论与发现
*   **性能显著提升**：在CRC任务中，平均F1分数从0.828提升至0.911；肺癌分期准确率从0.680大幅提升至0.833。
*   **逻辑一致性增强**：反思机制成功修正了诸如“边缘状态与距离矛盾”等逻辑错误。
*   **收敛特性**：大多数变量在3-10轮内趋于收敛。约11%的复杂案例会出现“吸引子”现象（在两个值之间震荡），反映了模型对模糊文本的高度不确定性。
*   **模型通用性**：该框架在开源模型（Llama）和闭源先进模型（GPT系列）上均表现出改进效果。

### 7. 优点
*   **解决逻辑约束**：通过内部反思解决了LLM在处理相互依赖变量时的“幻觉”和逻辑断裂问题。
*   **知识驱动**：将复杂的医学指南（如AJCC规则）无缝集成到推理链中，而非仅仅依赖模型的预训练记忆。
*   **透明度**：模型在反思过程中会输出“解释（Explanation）”，便于人工审核推理逻辑。

### 8. 不足与局限
*   **计算成本高昂**：多轮迭代导致推理时间和Token成本成倍增加，可能限制其在大规模实时系统中的应用。
*   **震荡与不收敛**：对于极度模糊或矛盾的原始文本，模型可能陷入无限循环，需要额外的启发式规则来强制终止。
*   **空间推理局限**：尽管有反思，LLM在处理复杂的解剖学空间关系（如多个切缘的相对位置）时仍存在理解瓶颈。
*   **样本覆盖**：虽然涵盖了三种癌症，但样本均来自特定医疗机构，其跨机构的泛化能力尚待验证。

（完）
