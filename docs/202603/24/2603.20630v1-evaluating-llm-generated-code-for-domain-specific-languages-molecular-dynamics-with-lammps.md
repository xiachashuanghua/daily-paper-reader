---
title: "Evaluating LLM-generated code for domain-specific languages: molecular dynamics with LAMMPS"
title_zh: 评估大语言模型生成的领域特定语言代码：以 LAMMPS 分子动力学为例
authors: "Ethan Holbrook, Juan C. Verduzco, Alejandro Strachan"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20630v1"
tags: ["query:llmagt"]
score: 8.0
evidence: 评估LLM生成的分子动力学模拟代码
tldr: 本研究评估了大语言模型（LLM）在生成分子动力学软件LAMMPS特定领域语言（DSL）代码方面的表现。研究提出了一套评估流程，包含规范化步骤和可扩展解析器，旨在帮助领域专家在无需深入掌握DSL语法的情况下验证代码有效性。结果揭示了当前LLM在生成科学DSL时的局限性，并为将其集成到科学计算生态系统提供了实践路径。
selection_source: fresh_fetch
motivation: 尽管LLM在通用编程语言上表现出色，但其在生成科学计算领域特定语言（如LAMMPS）方面的有效性仍缺乏深入研究。
method: 提出了一种包含代码规范化和语法解析的评估流程，使专家能快速识别错误并利用LLM加速验证测试。
result: 识别出了LLM在生成科学DSL时的常见错误模式，并量化评估了当前主流模型的性能表现。
conclusion: LLM在生成科学DSL方面存在局限，但通过合理的评估与集成流程，仍能显著提升领域专家的科研效率。
---

## 摘要
大语言模型（LLMs）正在改变研究人员在科学计算中与代码和数据交互的方式。虽然它们生成通用代码的能力已得到公认，但在为领域特定语言（DSLs）生成科学有效的代码或输入脚本方面的有效性在很大程度上仍有待探索。我们提出了一种评估程序，使领域专家（即使并非 DSL 专家）能够评估为广泛使用的分子动力学（MD）软件 LAMMPS 生成的 LLM 输入文件的有效性，并利用这些评估结果来评价最先进 LLM 的性能并识别常见问题。该评估程序的关键在于生成规范文件的归一化步骤以及用于语法分析的可扩展解析器。后续步骤可以在不耗费昂贵测试（时间和计算资源）的情况下隔离常见错误。一旦生成了可运行的输入文件，LLM 即可加速验证测试。我们的研究结果突显了 LLM 在生成科学 DSL 方面的局限性，并为领域专家将其集成到领域特定计算生态系统中提供了一条切实可行的路径。

## Abstract
Large language models (LLMs) are changing the way researchers interact with code and data in scientific computing. While their ability to generate general-purpose code is well established, their effectiveness in producing scientifically valid code/input scripting for domain-specific languages (DSLs) remains largely unexplored. We propose an evaluation procedure that enables domain experts (who may not be experts in the DSL) to assess the validity of LLM-generated input files for LAMMPS, a widely used molecular dynamics (MD) code, and to use those assessments to evaluate the performance of state-of-the-art LLMs and identify common issues. Key to the evaluation procedure are a normalization step to generate canonical files and an extensible parser for syntax analysis. The following steps isolate common errors without incurring costly tests (in time and computational resources). Once a working input file is generated, LLMs can accelerate verification tests. Our findings highlight limitations of LLMs in generating scientific DSLs and a practical path forward for their integration into domain-specific computational ecosystems by domain experts.

---

## 论文详细总结（自动生成）

这是一份关于论文《Evaluating LLM-generated code for domain-specific languages: molecular dynamics with LAMMPS》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：虽然大语言模型（LLM）在 Python 等通用编程语言上表现出色，但在科学计算领域的**领域特定语言（DSL）**（如 LAMMPS 分子动力学输入脚本）上的表现尚不明确。
*   **核心挑战**：科学 DSL 具有语法严苛、指令依赖性强、物理逻辑复杂等特点。微小的语法偏差或物理参数错误（如单位不匹配、势函数定义误用）都会导致模拟失败或产生错误的科学结论。
*   **研究目标**：开发一套评估流程，帮助领域专家在无需精通 DSL 语法的情况下，验证 LLM 生成脚本的有效性，并量化评估当前顶尖 LLM 在科学 DSL 任务中的可靠性。

### 2. 论文提出的方法论
研究者提出了一套**多阶段评估流水线（Pipeline）**，核心步骤如下：
*   **规范化（Normalization）**：通过 `lammps-ast` 工具去除注释、合并多行指令、展开循环，并进行变量解析与替换，将脚本转化为规范的“标准型”，以便后续对比。
*   **静态语法解析（Static Parsing）**：利用基于 Lark 库开发的自定义解析器，将脚本转化为**抽象语法树（AST）**。这一步可以在不运行模拟的情况下，通过结构化检查发现语法错误和参数类型不匹配。
*   **执行测试（Execution Test）**：
    *   **缩短运行步数**：将所有 `run` 指令改为仅运行 10 步，以极低的计算成本测试脚本的可执行性。
    *   **势函数隔离（PSZ 替换）**：引入“Pair Style Zero (PSZ)”步骤，将复杂的势函数指令替换为零势能模型。这有助于区分是“语法/逻辑错误”还是“特定势函数参数配置错误”。
*   **物理准确性检查（Accuracy Check）**：基于预定义的 Checklist（如晶格常数、系综选择、温度/压力设定值、单位换算等）对通过执行测试的脚本进行人工/自动化审查。

### 3. 实验设计
*   **测试场景（Prompts）**：设计了三个难度梯度的任务：
    1.  **P1（简单）**：铝（Al）单晶在 300K 下的平衡模拟（NPT 系综）。
    2.  **P2（中等）**：镍（Ni）单晶从 300K 连续加热至 2500K 的熔化模拟。
    3.  **P3（复杂）**：铌（Nb）单晶的高速冲击（Spall）模拟，涉及复杂的几何区域定义、非周期性边界和单位换算。
*   **对比方法（模型）**：评估了 5 个顶尖模型：**GPT-4o, GPT-4.1, GPT-o3, GPT-5, Claude 4 Opus**（注：论文标注日期为 2026 年，包含当时的最前沿模型）。
*   **实验设置**：每个模型针对每个任务生成 10 次（10-shot sampling），以捕捉模型输出的随机性。

### 4. 资源与算力
*   **算力消耗**：论文未明确提及具体的 GPU 训练算力，因为所有模型均通过**公共 API** 访问。
*   **模拟开销**：由于采用了“10 步缩减运行”策略，执行测试阶段的计算资源消耗极低，可在普通工作站上快速完成。

### 5. 实验数量与充分性
*   **实验规模**：共生成并评估了 **150 个 LAMMPS 脚本**（5 模型 × 3 任务 × 10 次重复）。
*   **充分性评价**：
    *   **客观性**：使用了统一的 System Prompt 和评估 Checklist，标准一致。
    *   **局限性**：虽然 150 个样本足以揭示模型间的性能差异，但对于 LAMMPS 极其庞大的指令集（数百个命令）而言，覆盖范围仍较窄，主要集中在金属系统的简单力场上。

### 6. 论文的主要结论与发现
*   **性能随复杂度剧降**：在简单任务（P1）中，约 66% 的脚本完全正确；但在复杂任务（P3）中，仅有 2%（150 个中的 1 个）完全正确。
*   **模型表现差异**：
    *   **Claude 4 Opus**：在语法解析通过率（97%）和执行成功率上表现最稳健。
    *   **GPT-5**：虽然整体稳健性略逊，但是**唯一一个**在最复杂任务（P3）中生成完全正确脚本的模型，展现了极强的上限。
*   **典型错误模式**：
    *   **势函数误用**：如混淆 `eam` 和 `eam/alloy`。
    *   **几何推理失败**：在定义区域（Region）和组（Group）的空间关系时经常出错。
    *   **单位换算幻觉**：在 `units metal` 模式下错误地应用了 SI 单位制的数值。

### 7. 优点
*   **工具链实用性强**：开发的 `lammps-ast` 解析器填补了科学 DSL 缺乏静态检查工具的空白。
*   **评估维度全面**：不仅关注脚本能否运行（Executability），更关注物理逻辑是否正确（Scientific Validity）。
*   **解耦错误来源**：通过 PSZ 替换巧妙地隔离了“语法逻辑”与“势函数配置”这两类不同性质的错误。

### 8. 不足与局限
*   **任务多样性有限**：实验仅限于单组分金属系统，未涉及多组分合金、有机分子、反应力场（ReaxFF）等更复杂的科研场景。
*   **闭源模型依赖**：评估完全基于闭源 API，无法分析模型内部的训练数据偏差。
*   **自动化程度尚需提高**：最后的物理准确性检查仍部分依赖人工 Checklist，尚未实现完全闭环的自动修复。

（完）
