---
title: "Cross-Granularity Representations for Biological Sequences: Insights from ESM and BiGCARP"
title_zh: 生物序列的跨粒度表示：来自 ESM 和 BiGCARP 的启示
authors: "Hanlin Xiao, Rainer Breitling, Eriko Takano, Mauricio A. Álvarez"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20825v1"
tags: ["query:llmagt"]
score: 10.0
evidence: 大型生物序列模型与跨粒度知识整合
tldr: 本研究探讨了生物序列模型中跨粒度知识的整合，以氨基酸水平的ESM模型和结构域水平的BiGCARP模型为案例。通过表示分析和探测任务，研究揭示了深层嵌入能更准确地捕捉模型学习到的上下文知识。研究证明不同粒度的表示具有互补性，结合这些表示可显著提升中间层级预测任务的性能，为提高生物基础模型的性能和可解释性提供了新策略。
selection_source: fresh_fetch
motivation: 旨在探索如何有效整合生物序列中不同层级（如氨基酸和蛋白质结构域）的层次化粒度信息以提升模型效能。
method: 通过对比分析ESM和BiGCARP模型的表示，研究了不同层级嵌入的初始化策略及跨粒度知识的互补性。
result: 实验表明深层嵌入能更忠实地反映模型知识，且结合不同粒度的表示在中间层级预测任务中表现出明显的性能增益。
conclusion: 跨粒度表示整合是提升生物基础模型性能和可解释性的有效且具有前景的策略。
---

## 摘要
通用基础模型的最新进展推动了大型生物序列模型的发展。虽然自然语言表现出符号粒度（字符、单词、句子），但生物序列呈现出分层粒度，其各个层级（核苷酸、氨基酸、蛋白质结构域、基因）进一步编码了生物功能信息。在本文中，我们通过对 BiGCARP（一种针对生物合成基因簇的 Pfam 结构域级模型）和 ESM（一种氨基酸级蛋白质语言模型）的案例研究，探讨了模型间跨粒度知识的整合。利用表示分析工具和一系列探测任务，我们首先解释了为什么简单的跨模型嵌入初始化未能提高 BiGCARP 的下游性能，并表明深层嵌入捕获了模型所学知识中更具上下文相关性且更真实的表示。此外，我们证明了不同粒度的表示编码了互补的生物学知识，并且将它们结合起来可以在中间层级预测任务中产生显著的性能提升。我们的研究结果强调，跨粒度整合是提高生物基础模型性能和可解释性的一种极具前景的策略。

## Abstract
Recent advances in general-purpose foundation models have stimulated the development of large biological sequence models. While natural language shows symbolic granularity (characters, words, sentences), biological sequences exhibit hierarchical granularity whose levels (nucleotides, amino acids, protein domains, genes) further encode biologically functional information. In this paper, we investigate the integration of cross-granularity knowledge from models through a case study of BiGCARP, a Pfam domain-level model for biosynthetic gene clusters, and ESM, an amino acid-level protein language model. Using representation analysis tools and a set of probe tasks, we first explain why a straightforward cross-model embedding initialization fails to improve downstream performance in BiGCARP, and show that deeper-layer embeddings capture a more contextual and faithful representation of the model's learned knowledge. Furthermore, we demonstrate that representations at different granularities encode complementary biological knowledge, and that combining them yields measurable performance gains in intermediate-level prediction tasks. Our findings highlight cross-granularity integration as a promising strategy for improving both the performance and interpretability of biological foundation models.

---

## 论文详细总结（自动生成）

这篇论文深入探讨了生物序列模型中不同层级（粒度）信息的整合问题，以下是对该研究的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：生物序列（DNA、氨基酸、蛋白质结构域、基因簇）具有天然的分层结构，类似于自然语言中的字符、单词和句子。然而，现有的生物基础模型通常只专注于单一粒度（如 ESM 关注氨基酸，BiGCARP 关注结构域）。
*   **研究动机**：尽管 BiGCARP 尝试使用 ESM 的表示来初始化其结构域嵌入，但这种“跨粒度”初始化并未显著提升性能。论文旨在探究其失效的原因，并寻找有效整合不同粒度知识的方法，以提升模型在复杂生物任务中的表现和可解释性。

### 2. 论文提出的方法论
*   **核心思想**：通过表示分析工具（UMAP 和 CKA）量化模型在训练过程中如何处理跨粒度信息，并评估不同层级嵌入对下游任务的贡献。
*   **关键技术细节**：
    *   **模型选择**：对比了 **ESM**（基于 Transformer 的氨基酸级模型）和 **BiGCARP**（基于扩张卷积/CARP 架构的 Pfam 结构域级模型）。
    *   **初始化策略**：对比了“随机初始化”与“ESM 初始化”（利用 ESM 对结构域对应的氨基酸序列进行编码并均值池化，作为 BiGCARP 的初始嵌入）。
    *   **表示提取**：对比了“嵌入层（Embedder Layer）”和“最终层（Last Layer）”的表示。
    *   **分析工具**：使用 **CKA（中心核对齐）** 来测量模型不同层级之间表示的相似性，追踪预训练知识在网络中的保留情况；使用 **UMAP** 进行降维可视化，观察功能聚类。
    *   **融合方式**：采用简单的特征拼接（Concatenation）来整合 ESM 的局部信息和 BiGCARP 的全局上下文信息。

### 3. 实验设计
*   **数据集**：
    *   **antiSMASH-DB**：用于 BiGCARP 的预训练（约 12.7 万个基因簇）。
    *   **MIBiG 3.0**：用于下游探测任务的验证（约 2500 个经过实验验证的基因簇）。
    *   **Pfam**：提供结构域定义和功能注释。
*   **探测任务（Benchmark）**：
    1.  **全局任务**：BGC 产物类别预测（多标签分类）。
    2.  **局部任务**：A-结构域底物属性预测（评估对微观序列特征的捕捉）。
    3.  **中间任务**：卤化产物预测（评估是否产生含卤素分子，需结合局部酶特征与全局背景）。
*   **对比方法**：随机初始化模型、Pfam2Vec、单模型表示（仅 ESM 或仅 BiGCARP）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。考虑到 BiGCARP 和 ESM 的规模，通常需要高性能计算集群（如 A100 或 H100 级别），但文中主要侧重于表示分析而非从头训练大型模型。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 BGC 产物预测采用了 **5 折分层交叉验证**，并重复 **10 个随机种子**以确保统计显著性。
    *   针对样本量极小的卤化预测任务（仅 46 个样本），采用了 **留一法交叉验证（LOOCV）** 和 **10,000 次 Bootstrap 重采样**。
*   **充分性与客观性**：实验设计较为严谨，通过 CKA 分析解释了初始化失效的内在机制（即深层网络会重写初始化的知识），这使得结论不仅停留在性能数字上，更具解释性。对比实验覆盖了局部、全局和中间三个维度，评估较为全面。

### 6. 主要结论与发现
*   **初始化失效原因**：CKA 分析显示，BiGCARP 的早期层（尤其是第一层扩张卷积）会迅速转换输入空间，导致 ESM 初始化的结构信息在深层被“覆盖”，因此对最终性能贡献有限。
*   **深层表示更可靠**：相比于静态的嵌入层，模型的最终层（Last-layer）捕获了更丰富的上下文和功能信息，在下游任务中表现更好。
*   **跨粒度互补性**：ESM 擅长捕捉局部结构特征（如底物特异性），而 BiGCARP 擅长捕捉全局功能背景。
*   **性能提升**：在中间层级任务（如卤化预测）中，结合 ESM 和 BiGCARP 的表示比单一模型表现显著更好（AUROC 提升明显）。

### 7. 优点
*   **视角独特**：将生物序列的层次性类比为 NLP 的粒度，提出了“跨粒度整合”这一具有前景的方向。
*   **深度分析**：不仅展示了结果，还利用 CKA 工具深入剖析了深度学习模型内部的表示动力学，解释了“为什么简单初始化没用”。
*   **生物学解释性**：通过 UMAP 聚类分析，证明了模型学习到的表示与生物学功能（如调节、转运、修饰）高度相关。

### 8. 不足与局限
*   **融合策略简单**：目前仅采用了简单的特征拼接，未探索更先进的融合机制（如跨注意力机制 Cross-Attention 或联合预训练）。
*   **数据瓶颈**：部分探测任务（如卤化预测）样本量极小，虽然使用了统计学手段弥补，但结论的普适性仍需在更大规模的标注数据集上验证。
*   **应用范围限制**：研究主要集中在生物合成基因簇（BGC），尚未扩展到非编码区、全基因组或其他生物序列领域。

（完）
