---
title: "MolEvolve: LLM-Guided Evolutionary Search for Interpretable Molecular Optimization"
title_zh: MolEvolve：用于可解释分子优化的 LLM 引导进化搜索
authors: "Xiangsen Chen, Ruilong Wu, Yanyan Lan, Ting Ma, Yang Liu"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24382v1"
tags: ["query:llmagt"]
score: 9.0
evidence: LLM引导的自主分子优化
tldr: 针对深度学习在化学领域缺乏可解释性且难以处理“活性峭壁”的问题，本文提出 MolEvolve 框架。该框架将分子发现重新定义为自主的前瞻性规划问题，利用大语言模型（LLM）探索和演化可执行的化学符号操作库。通过结合蒙特卡洛树搜索（MCTS）和外部工具，MolEvolve 能够自主发现最优演化路径，生成透明且人类可读的推理链。实验证明，该方法在属性预测和分子优化任务上均优于现有基准。
selection_source: fresh_fetch
motivation: 传统深度学习方法难以解释分子微小结构变化导致的活性剧变（活性峭壁），限制了其在化学领域的应用。
method: 提出 MolEvolve 框架，利用 LLM 引导符号操作的演化，并结合 MCTS 引擎进行自主的分子优化路径规划。
result: 实验结果显示，MolEvolve 不仅能生成可解释的化学见解，在分子属性预测和优化任务的性能上也超过了基准模型。
conclusion: MolEvolve 通过符号化推理与 LLM 的结合，为实现可解释且高效的自主分子发现提供了新路径。
---

## 摘要
尽管深度学习在化学领域取得了成功，但其影响力受限于缺乏可解释性以及无法解决“活性悬崖”（activity cliffs）问题，即微小的结构差异会引发剧烈的性质变化。当前的表示学习受限于相似性原理，往往无法捕捉这些结构-活性不连续性。为此，我们引入了 MolEvolve，这是一个将分子发现重新表述为自主前瞻性规划问题的进化框架。与依赖人工特征工程或僵化先验知识的传统方法不同，MolEvolve 利用大语言模型（LLM）主动探索并演化出一套可执行的化学符号操作库。通过利用 LLM 进行冷启动，并结合蒙特卡洛树搜索（MCTS）引擎在测试阶段利用外部工具（如 RDKit）进行规划，该系统能够自主发现最优轨迹。这一过程演化出了透明的推理链，将复杂的结构转换转化为可操作且人类可读的化学见解。实验结果表明，MolEvolve 的自主搜索不仅能演化出透明、人类可读的化学见解，而且在性质预测和分子优化任务中均优于基准模型。

## Abstract
Despite deep learning's success in chemistry, its impact is hindered by a lack of interpretability and an inability to resolve activity cliffs, where minor structural nuances trigger drastic property shifts. Current representation learning, bound by the similarity principle, often fails to capture these structural-activity discontinuities. To address this, we introduce MolEvolve, an evolutionary framework that reformulates molecular discovery as an autonomous, look-ahead planning problem. Unlike traditional methods that depend on human-engineered features or rigid prior knowledge, MolEvolve leverages a Large Language Model (LLM) to actively explore and evolve a library of executable chemical symbolic operations. By utilizing the LLM to cold start and an Monte Carlo Tree Search (MCTS) engine for test-time planning with external tools (e.g. RDKit), the system self-discovers optimal trajectories autonomously. This process evolves transparent reasoning chains that translate complex structural transformations into actionable, human-readable chemical insights. Experimental results demonstrate that MolEvolve's autonomous search not only evolves transparent, human-readable chemical insights, but also outperforms baselines in both property prediction and molecule optimization tasks.

---

## 论文详细总结（自动生成）

以下是对论文《MolEvolve: LLM-Guided Evolutionary Search for Interpretable Molecular Optimization》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：深度学习（如 GNN）在化学领域虽表现优异，但存在“黑盒”属性，且难以处理**“活性峭壁”（Activity Cliffs）**现象——即分子结构的微小变化导致性质剧烈波动的非连续性问题。
*   **研究动机**：
    *   **表示-精度悖论**：GNN 倾向于平滑处理结构差异，导致无法捕捉活性峭壁；而 LLM 虽有语义直觉，但在处理定量物理性质时容易产生“数值幻觉”。
    *   **缺乏可解释性**：科学家不仅需要预测结果，更需要知道导致特定性质的明确因果规则。
    *   **目标**：通过将分子优化重构为可执行符号空间的动态搜索问题，结合 LLM 的推理能力与物理工具的严谨性。

### 2. 方法论
MolEvolve 框架将分子工程定义为在可执行符号化学空间上的定向搜索，主要包含两个阶段：
*   **阶段一：符号知识蒸馏（冷启动）**：
    *   **知识合成**：利用思维链（CoT）提示 LLM 识别与目标性质相关的关键子结构或理化性质。
    *   **符号落地**：LLM 作为代码生成器，将文本规则转化为基于 RDKit 等化学库的可执行 Python 函数。
    *   **迭代自纠错**：通过执行反馈捕捉错误，让 LLM 自动修复代码漏洞，确保规则的有效性。
*   **阶段二：LLM 引导的进化搜索（MCTS）**：
    *   **状态定义**：在预测任务中为特征集合，在优化任务中为分子 SMILES 字符串。
    *   **节点扩张**：LLM 作为策略网络，基于冷启动阶段的先验知识提出高潜力的化学修改动作。
    *   **闭环验证与奖励**：每个操作都由外部工具（RDKit）验证。奖励函数结合了任务性能（如 RMSE）、符号先验对齐度以及结构约束（相似度惩罚）。
    *   **回溯与规划**：利用蒙特卡洛树搜索（MCTS）进行前瞻性规划，通过反向传播更新路径价值，避免陷入局部最优。

### 3. 实验设计
*   **数据集**：
    *   **性质预测**：MoleculeNet 中的 5 个基准（ESOL, Lipophilicity, BACE, BBBP, HIV）。
    *   **性质优化**：ChemCoTBench（针对 LogP 和 QED 的优化）。
*   **对比方法（Baselines）**：
    *   **GNN 类**：GIN, Graphformer。
    *   **LLM 代理/RAG 类**：Automolco, MolRAG, Attrilens-mol。
    *   **强推理模型**：GPT-4o, GPT-5 (预估/最新版), DeepSeek-R1, DeepSeek-V3, Qwen2.5 系列。
    *   **传统模型**：XGBoost, 决策树, 逻辑回归。

### 4. 资源与算力
*   **模型使用**：实验使用了 Qwen2.5-7B/32B-Instruct、GPT-4o 及 GPT-4o-mini 作为骨干 LLM。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量及总训练/搜索时长。考虑到 MCTS 涉及多次 LLM 调用和外部工具执行，其推理成本（Test-time compute）会高于传统的单次推理模型。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了回归与分类两大类预测任务，以及多目标的分子结构优化任务。
*   **消融实验**：详细对比了“仅 LLM”、“LLM + 冷启动”、“LLM + MCTS”以及完整框架的效果，验证了各模块的必要性。
*   **敏感性分析**：对奖励函数中的启发式权重（$\lambda$）和相似度惩罚（$\gamma$）进行了热力图分析。
*   **充分性评价**：实验设计较为全面，通过跨模型（不同参数量 LLM）和跨任务的对比，客观地证明了符号搜索在解决活性峭壁问题上的优越性。

### 6. 主要结论与发现
*   **性能超越**：MolEvolve 在性质预测上显著优于专门的 GNN 模型，在优化任务上，其 7B 版本的表现甚至超过了 DeepSeek-R1 等强推理大模型。
*   **解决活性峭壁**：通过离散的符号逻辑而非连续的嵌入向量，模型能精准捕捉微小结构变化带来的性质跃迁。
*   **可解释性**：系统生成的“推理链”是可执行的代码和逻辑步骤，而非事后解释（Post-hoc），为化学家提供了透明的修改建议。
*   **模型无关性**：演化出的符号特征可以迁移到随机森林、XGBoost 等不同下游评估器中，且均能提升性能。

### 7. 优点与亮点
*   **解耦语义与物理**：巧妙地利用 LLM 提供战略方向（语义），利用 MCTS 和 RDKit 保证物理严谨性（精度）。
*   **闭环自进化**：通过代码执行反馈实现自我修正，降低了 LLM 产生化学幻觉的风险。
*   **实时可追溯**：优化过程中的每一步都有明确的化学规则对应，实现了真正的“白盒”发现。

### 8. 不足与局限
*   **计算开销**：MCTS 搜索过程涉及大量的 LLM API 调用或本地推理，时间复杂度较高，可能不适合超大规模筛选。
*   **工具依赖**：系统的上限受限于外部化学工具（如 RDKit）的功能范围，对于某些复杂的量子化学性质，可能需要更昂贵的模拟工具。
*   **搜索空间限制**：虽然 LLM 引导了搜索，但在极度广阔的化学组合空间中，搜索深度和广度的平衡仍是一个挑战。

（完）
