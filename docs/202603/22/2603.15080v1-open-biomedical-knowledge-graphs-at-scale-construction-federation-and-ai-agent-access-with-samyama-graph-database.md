---
title: "Open Biomedical Knowledge Graphs at Scale: Construction, Federation, and AI Agent Access with Samyama Graph Database"
title_zh: 大规模开放生物医学知识图谱：基于 Samyama 图数据库的构建、联邦与 AI 智能体访问
authors: "Madhulatha Mandarapu, Sandeep Kunkunuru"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15080v1"
tags: ["query:llmagt"]
score: 9.0
evidence: AI智能体访问生物医学知识图谱
tldr: 本研究针对生物医学数据碎片化问题，利用高性能Rust图数据库Samyama构建了Pathways和Clinical Trials两个大规模开源知识图谱。通过可重复的ETL流程实现跨源去重与联邦查询，并引入模型上下文协议（MCP）为AI智能体提供自然语言访问接口。该系统支持千万级规模数据的快速加载与复杂跨域查询，显著提升了生物医学研究的数据整合效率。
selection_source: fresh_fetch
motivation: 解决生物医学数据库孤岛化导致的跨源数据整合困难、效率低下且难以复现的问题。
method: 基于Samyama图数据库开发了可重复的ETL模式，实现跨图谱联邦查询，并通过MCP协议自动生成LLM工具接口。
result: 成功构建了包含近800万节点和2800万关系的联邦图谱，在普通硬件上实现了秒级的复杂跨域查询响应。
conclusion: 该研究为大规模生物医学知识图谱的构建、集成及AI驱动的自动化访问提供了一套高效、开源且可扩展的解决方案。
---

## 摘要
生物医学知识分散在孤立的数据库中——Reactome 用于通路，STRING 用于蛋白质相互作用，Gene Ontology 用于功能注释，ClinicalTrials.gov 用于研究注册，以及其他数十个数据库。研究人员通常从每个来源下载扁平文件并编写定制脚本进行交叉引用，这一过程缓慢、易错且不可重复。我们展示了两个开源生物医学知识图谱——Pathways KG（包含来自 5 个数据源的 118,686 个节点和 834,785 条边）和 Clinical Trials KG（包含来自 5 个数据源的 7,774,446 个节点和 26,973,997 条边）——它们构建于用 Rust 编写的高性能图数据库 Samyama 之上。我们的贡献有三个方面：首先，我们描述了一种可重复的 ETL 模式，用于从异构公共数据源构建大规模知识图谱，支持跨源去重、批量 Cypher 加载和便携式快照导出。其次，我们展示了跨知识图谱的联邦：将两个快照加载到单个图租户中，可以实现跨数据集的基于属性的连接，从而回答诸如“哪些生物通路被目前处于乳腺癌 3 期临床试验的药物所干扰？”之类的问题——这是任何一个知识图谱都无法单独回答的查询。第三，我们引入了模式驱动的 MCP 服务器生成：每个知识图谱通过模型上下文协议（Model Context Protocol）自动为大语言模型（LLM）智能体提供类型化工具，从而实现对图查询的自然语言访问，而无需手动编写工具。所有数据源均为开源许可（CC BY 4.0, CC0, OBO）。快照、ETL 代码和 MCP 配置均已公开。合并后的联邦图（789 万个节点，2780 万条边）在商用硬件（Mac Mini M4, 16GB RAM）上加载仅需 76 秒，而标志性的跨知识图谱查询——“哪些通路被乳腺癌 3 期试验中的药物所干扰？”——在 2.1 秒内即可返回经过验证的结果。

## Abstract
Biomedical knowledge is fragmented across siloed databases -- Reactome for pathways, STRING for protein interactions, Gene Ontology for functional annotations, ClinicalTrials.gov for study registries, and dozens more. Researchers routinely download flat files from each source and write bespoke scripts to cross-reference them, a process that is slow, error-prone, and not reproducible. We present two open-source biomedical knowledge graphs -- Pathways KG (118,686 nodes, 834,785 edges from 5 sources) and Clinical Trials KG (7,774,446 nodes, 26,973,997 edges from 5 sources) -- built on Samyama, a high-performance graph database written in Rust.   Our contributions are threefold. First, we describe a reproducible ETL pattern for constructing large-scale KGs from heterogeneous public data sources, with cross-source deduplication, batch Cypher loading, and portable snapshot export. Second, we demonstrate cross-KG federation: loading both snapshots into a single graph tenant enables property-based joins across datasets, answering questions like ``Which biological pathways are disrupted by drugs currently in Phase~3 trials for breast cancer?'' -- a query that neither KG can answer alone. Third, we introduce schema-driven MCP server generation: each KG automatically exposes typed tools for LLM agents via the Model Context Protocol, enabling natural-language access to graph queries without manual tool authoring.   All data sources are open-license (CC~BY~4.0, CC0, OBO). Snapshots, ETL code, and MCP configurations are publicly available. The combined federated graph (7.89M nodes, 27.8M edges) loads in 76 seconds on commodity hardware (Mac Mini M4, 16GB RAM), and the signature cross-KG query -- ``which pathways are disrupted by drugs in Phase~3 breast cancer trials?'' -- returns validated results in 2.1 seconds.

---

## 论文详细总结（自动生成）

### 论文总结：大规模开放生物医学知识图谱的构建、联邦与 AI 智能体访问

#### 1. 核心问题与研究动机
*   **核心问题**：生物医学知识高度碎片化，分散在如 Reactome（通路）、STRING（蛋白质相互作用）、ClinicalTrials.gov（临床试验）等多个孤立数据库中。
*   **研究动机**：
    *   **整合困难**：研究人员通常需要手动下载扁平文件并编写定制脚本进行交叉引用，过程缓慢、易错且难以复现。
    *   **现有方案局限**：现有的知识图谱（如 Hetionet）多为静态数据集，缺乏更新管线；或依赖复杂的 SPARQL 基础设施（如 Bio2RDF），在处理大规模遍历查询时性能受限。
    *   **AI 接入门槛**：大语言模型（LLM）难以直接高效地访问结构化的图数据，缺乏标准化的工具调用接口。

#### 2. 方法论
*   **核心思想**：利用高性能 Rust 原生图数据库 **Samyama**，构建可重复的 ETL 流程，通过“属性连接”实现跨图谱联邦，并利用 **MCP（模型上下文协议）** 实现 AI 智能体的自然语言访问。
*   **关键技术细节**：
    *   **Samyama 数据库**：采用 Rust 编写，支持 OpenCypher 查询语言，集成了图存储、向量搜索和元启发式求解器。
    *   **五阶段 ETL 模式**：包括下载（支持断点续传）、解析与过滤（针对人类物种）、去重（跨阶段实体追踪）、批量加载（Cypher 语句）和快照导出（.sgsnap 格式）。
    *   **跨图谱联邦（Federation）**：不采用加载时物理合并，而是将多个快照加载到同一租户中，通过共享标识符（如 UniProt ID、DrugBank ID）进行**基于属性的连接（Property-based Joins）**。
    *   **模式驱动的 MCP 生成**：自动根据图谱 Schema 生成 LLM 调用的工具接口，使 AI 智能体能通过自然语言执行复杂的图查询。

#### 3. 实验设计
*   **数据集**：
    *   **Pathways KG**：整合了 Reactome、STRING、GO、WikiPathways 和 UniProt（约 11.9 万节点，83.5 万边）。
    *   **Clinical Trials KG**：整合了 ClinicalTrials.gov、MeSH、RxNorm、OpenFDA 和 PubMed（约 777 万节点，2697 万边）。
*   **Benchmark 与对比**：
    *   对比了 Bio2RDF、Hetionet、CKG 和 PrimeKG 等先前工作，强调了 Samyama 在摄取吞吐量、快照便携性和 AI 集成方面的优势。
    *   **核心测试查询**：“哪些生物通路被目前处于乳腺癌 3 期临床试验的药物所干扰？”（涉及 6 跳跨图谱遍历）。

#### 4. 资源与算力
*   **硬件环境**：实验在商用硬件 **Mac Mini M4**（10 核 CPU，16GB RAM）上完成。
*   **性能表现**：
    *   **加载速度**：合并后的联邦图（789 万节点，2780 万边）加载仅需 **76 秒**。
    *   **查询延迟**：标志性的 6 跳跨图谱查询在 **2.1 秒** 内完成。
*   **GPU 使用**：文中未提到使用 GPU，主要依赖 Rust 编写的 CPU 优化引擎。

#### 5. 实验数量与充分性
*   **实验覆盖**：
    *   测试了构建性能（加载时间、快照大小）。
    *   测试了多种查询模式的延迟（标签分布、Hub 节点搜索、临床试验计数等）。
    *   验证了联邦查询的生物学正确性（如 HER2 信号通路与曲妥珠单抗的关联）。
*   **充分性评价**：实验设计较为充分，涵盖了从数据摄取到复杂业务查询的全流程。通过具体的生物医学案例验证了系统的实用性，实验结果客观且具有可复现性。

#### 6. 主要结论与发现
*   **高性能集成可行**：在普通商用硬件上即可实现千万级规模生物医学知识图谱的秒级加载与复杂查询。
*   **联邦模式优势**：基于属性的连接允许独立维护和组合不同的知识图谱快照，极大地提高了数据整合的灵活性。
*   **AI 接入便捷化**：通过 MCP 协议自动生成工具，显著降低了 LLM 智能体利用专业知识图谱的门槛，实现了自然语言到复杂 Cypher 查询的无缝转换。

#### 7. 优点
*   **卓越的性能**：Rust 原生引擎提供了极高的摄取和查询效率。
*   **高度可复现**：提供开源的 ETL 代码、快照文件和 MCP 配置，方便社区使用。
*   **现代 AI 集成**：率先引入 MCP 协议，解决了 LLM 访问图数据库的“最后一步”问题。
*   **架构灵活**：快照机制使得知识图谱像文件一样易于分发和组合。

#### 8. 不足与局限
*   **数据时效性**：快照是静态的，而临床试验等数据更新频繁，需要定期重新导出。
*   **存储冗余**：属性连接模式会导致节点重复（如两个图谱中各有一个相同的蛋白质节点），增加了存储开销。
*   **内存依赖**：完整联邦图谱需要约 8GB 内存，虽然在现代机器上不算高，但在极低端设备上可能受限。
*   **跨租户限制**：目前联邦查询仍需将数据加载到同一租户，未来需支持原生的跨租户/跨实例查询。

（完）
