---
title: "Open Biomedical Knowledge Graphs at Scale: Construction, Federation, and AI Agent Access with Samyama Graph Database"
title_zh: 大规模开放生物医学知识图谱：基于 Samyama 图数据库的构建、联邦与 AI 智能体访问
authors: "Madhulatha Mandarapu, Sandeep Kunkunuru"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15080v3"
tags: ["query:llmagt"]
score: 9.0
evidence: AI智能体访问生物医学知识图谱
tldr: 针对生物医学知识碎片化问题，本文基于高性能 Rust 图数据库 Samyama 构建了三个大规模开源知识图谱（Pathways、Clinical Trials、Drug Interactions）。研究提出了一套可复用的 ETL 流程，实现了跨图谱联邦查询，并引入了模式驱动的 MCP 服务，使 AI 智能体能高效访问图数据。在 BiomedQA 基准测试中，该方法显著提升了 LLM 的回答准确率，展示了在大规模生物医学数据集成与智能问答方面的卓越性能。
selection_source: fresh_fetch
motivation: 解决生物医学知识分散在不同孤立数据库中、难以统一集成和高效利用的问题。
method: 利用 Rust 编写的 Samyama 数据库构建大规模知识图谱，通过 ETL 模式进行去重集成，并生成模式驱动的 MCP 服务供 LLM 智能体调用。
result: "成功构建了包含 790 万节点和 2800 万条边的联邦图谱，在药理学问答测试中，MCP 工具辅助的智能体达到了 98% 的准确率。"
conclusion: 本研究证明了高性能图数据库结合模式驱动的智能体接口，是实现大规模生物医学知识集成与精准检索的有效方案。
---

## 摘要
生物医学知识分散在孤立的数据库中——Reactome 用于通路，STRING 用于蛋白质相互作用，ClinicalTrials.gov 用于研究注册，DrugBank 用于药物词汇，DGIdb 用于药物-基因相互作用，SIDER 用于副作用。我们介绍了三个开源生物医学知识图谱——Pathways KG（来自 5 个数据源的 118,686 个节点和 834,785 条边）、Clinical Trials KG（来自 5 个数据源的 7,774,446 个节点和 26,973,997 条边）以及 Drug Interactions KG（来自 3 个数据源的 32,726 个节点和 191,970 条边）——这些图谱均构建于用 Rust 编写的高性能图数据库 Samyama 之上。我们的贡献有三个方面。首先，我们描述了一种可重复的 ETL 模式，用于从异构公共数据源构建大规模知识图谱，包括跨源去重、批量加载（Python Cypher 和 Rust 原生加载器）以及便携式快照导出。其次，我们展示了跨知识图谱的联邦：将所有三个快照加载到单个图租户中，实现了跨数据集的基于属性的连接。第三，我们引入了用于大语言模型（LLM）智能体访问的模式驱动 MCP 服务器生成，并在新的 BiomedQA 基准测试（40 个药理学问题）上进行了评估：领域特定的 MCP 工具实现了 98% 的准确率，而模式感知的 text-to-Cypher 为 85%，独立 GPT-4o 为 75%，且模式错误率为零。所有数据源均采用开放许可。合并后的联邦图（790 万个节点，2800 万条边）在商用云硬件上加载约需 3 分钟，单知识图谱查询在 80-100 毫秒内完成，跨知识图谱联邦连接在 1-4 秒内完成。

## Abstract
Biomedical knowledge is fragmented across siloed databases -- Reactome for pathways, STRING for protein interactions, ClinicalTrials.gov for study registries, DrugBank for drug vocabularies, DGIdb for drug-gene interactions, SIDER for side effects. We present three open-source biomedical knowledge graphs -- Pathways KG (118,686 nodes, 834,785 edges from 5 sources), Clinical Trials KG (7,774,446 nodes, 26,973,997 edges from 5 sources), and Drug Interactions KG (32,726 nodes, 191,970 edges from 3 sources) -- built on Samyama, a high-performance graph database written in Rust.   Our contributions are threefold. First, we describe a reproducible ETL pattern for constructing large-scale KGs from heterogeneous public data sources, with cross-source deduplication, batch loading (Python Cypher and Rust native loaders), and portable snapshot export. Second, we demonstrate cross-KG federation: loading all three snapshots into a single graph tenant enables property-based joins across datasets. Third, we introduce schema-driven MCP server generation for LLM agent access, evaluated on a new BiomedQA benchmark (40 pharmacology questions): domain-specific MCP tools achieve 98% accuracy vs. 85% for schema-aware text-to-Cypher and 75% for standalone GPT-4o, with zero schema errors.   All data sources are open-license. The combined federated graph (7.9M nodes, 28M edges) loads in approximately 3 minutes on commodity cloud hardware, with single-KG queries completing in 80-100ms and cross-KG federation joins in 1-4s

---

## 论文详细总结（自动生成）

这是一份关于论文《Open Biomedical Knowledge Graphs at Scale: Construction, Federation, and AI Agent Access with Samyama Graph Database》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
生物医学领域的研究高度依赖于跨学科数据的整合，但目前知识碎片化严重，分散在如 Reactome（通路）、STRING（蛋白质相互作用）、ClinicalTrials.gov（临床试验）等相互孤立的数据库中。研究人员通常需要手动编写脚本处理异构格式（TSV, XML, JSON 等），过程缓慢且不可重复。
本文旨在解决**大规模生物医学知识图谱（KG）的构建、集成与智能化访问**问题。通过高性能图数据库 Samyama，实现跨领域的“联邦查询”，并利用 Model Context Protocol (MCP) 解决大语言模型（LLM）在处理复杂图查询时容易出现的模式幻觉（Schema Hallucination）问题。

### 2. 论文提出的方法论
论文提出了一套完整的技术框架，涵盖从原始数据到 AI 智能体访问的全流程：
*   **可复用的 ETL 模式**：分为五个阶段：下载（支持断点续传）、解析与过滤（针对人类物种过滤）、去重（通过共享注册表跟踪实体）、批量加载（提供 Python Cypher 和 Rust 原生加载器）、快照导出（生成便携式 `.sgsnap` 文件）。
*   **跨图谱联邦（Cross-KG Federation）**：不采用预先物理合并的方式，而是将多个 KG 快照加载到同一个数据库租户中，利用共享标识符（如 UniProt ID、DrugBank ID、基因符号）进行**基于属性的连接（Property-based Joins）**。
*   **模式驱动的 MCP 服务器生成**：
    *   自动从 Samyama 数据库提取模式（Schema）。
    *   为每个节点标签和边缘类型自动生成查询工具（Search, Get, Count）。
    *   通过 YAML 配置定义领域特定的 Cypher 模板工具，使 LLM 智能体能通过类型化接口（Typed Tools）访问图数据，而非直接生成 Cypher 语句。

### 3. 实验设计
*   **数据集**：整合了 13 个公开许可的数据源，构建了三个 KG：
    *   **Pathways KG**：11.8万节点，83.4万边。
    *   **Clinical Trials KG**：777万节点，2697万边。
    *   **Drug Interactions KG**：3.2万节点，19.1万边。
*   **Benchmark**：引入了 **BiomedQA**，包含 40 个涵盖 7 个类别的药理学问题。
*   **对比方法**：
    1.  **独立 GPT-4o**：无外部数据库访问。
    2.  **模式感知 Text-to-Cypher**：GPT-4o 配合全模式提示词和 Few-shot 示例。
    3.  **MCP 工具辅助**：LLM 仅负责工具选择和参数提取。

### 4. 资源与算力
*   **硬件环境**：AWS `g4dn.4xlarge` 实例（16 vCPU AMD EPYC, 62 GB RAM, NVIDIA A10G GPU）。
*   **性能表现**：
    *   **加载速度**：合并后的联邦图（7.9M 节点，28M 边）从快照加载仅需约 3 分钟。
    *   **构建速度**：Rust 原生加载器在 1 秒内即可完成 Drug Interactions KG 的构建。
    *   **查询延迟**：单图查询 80-100ms，跨图联邦连接查询 1-4s。
    *   **内存占用**：全量联邦图占用约 33 GB RAM。

### 5. 实验数量与充分性
*   **实验规模**：针对 40 个复杂的药理学问题进行了测试，涵盖了从简单的药物查找至跨三个 KG 的复杂链式推理。
*   **充分性评价**：实验设计较为客观，对比了当前主流的 Text-to-Cypher 方案。通过对失败案例的分析（如模式幻觉、变量匹配错误等），证明了 MCP 方案在消除模式错误方面的有效性。虽然 40 个问题的样本量不算巨大，但覆盖了生物医学研究中典型的跨领域查询场景。

### 6. 论文的主要结论与发现
*   **准确率提升**：MCP 工具方案达到了 **98%** 的准确率，显著优于 Text-to-Cypher（85%）和独立 GPT-4o（75%）。
*   **零模式错误**：通过预定义的工具接口，LLM 不再需要猜测图结构，彻底消除了模式幻觉。
*   **联邦查询可行性**：证明了基于属性的连接在处理千万级规模的生物医学图谱时，在商用硬件上具有秒级的响应速度，具备实用价值。
*   **性能优势**：Rust 编写的 Samyama 数据库在摄取速度和查询效率上表现优异。

### 7. 优点
*   **高性能与可移植性**：利用 Rust 的性能优势，并提供快照机制，使大规模 KG 的分发和部署变得极其简单（分钟级）。
*   **AI 友好性**：通过 MCP 协议将图数据库能力转化为 LLM 易于调用的工具，降低了 AI 智能体使用图数据的门槛。
*   **完全开源**：所有 ETL 代码、数据快照、Benchmark 和 MCP 配置均已开源，具有极高的复用价值。

### 8. 不足与局限
*   **数据时效性**：快照是静态的，而临床试验等数据更新频繁，需要定期重新导出。
*   **标识符对齐风险**：联邦查询依赖于标识符（如 UniProt ID）的覆盖率，若源数据缺失这些 ID，连接效果会打折扣。
*   **内存门槛**：全量联邦图需要至少 32GB 以上的内存，对于个人开发者或小型服务器仍有一定硬件要求。
*   **跨租户限制**：目前尚不支持跨不同数据库租户的实时查询，必须将数据加载至同一租户内。

（完）
