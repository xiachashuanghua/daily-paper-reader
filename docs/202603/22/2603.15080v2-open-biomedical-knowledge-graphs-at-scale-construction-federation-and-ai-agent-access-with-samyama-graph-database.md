---
title: "Open Biomedical Knowledge Graphs at Scale: Construction, Federation, and AI Agent Access with Samyama Graph Database"
title_zh: 大规模开放生物医学知识图谱：基于 Samyama 图数据库的构建、联邦化与 AI 智能体访问
authors: "Madhulatha Mandarapu, Sandeep Kunkunuru"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15080v2"
tags: ["query:llmagt"]
score: 8.0
evidence: 生物医学知识图谱与AI智能体访问
tldr: "本研究针对生物医学知识碎片化问题，基于高性能Rust图数据库Samyama构建了三个大规模开源知识图谱（Pathways、Clinical Trials、Drug Interactions）。研究提出了一套可复现的ETL流程，实现了跨图谱联邦查询，并引入了模式驱动的MCP服务器，使AI智能体能高效访问图数据。在BiomedQA基准测试中，该方法将LLM回答准确率提升至98%，显著优于传统方法，为生物医学研究提供了高效的数据集成与智能问答方案。"
selection_source: fresh_fetch
motivation: 旨在解决生物医学知识分散在多个孤立数据库中、难以统一集成和高效查询的问题。
method: 基于Rust开发的Samyama图数据库，通过可复现的ETL流程构建大规模知识图谱，并利用模式驱动的MCP服务器为AI智能体提供工具化访问。
result: "成功构建了包含790万节点和2800万条边的联邦图谱，在药理学问答测试中达到98%的准确率，且查询响应时间在毫秒级。"
conclusion: 该研究证明了高性能图数据库结合模式驱动的智能体接口，是实现大规模生物医学知识集成与智能化利用的有效途径。
---

## 摘要
生物医学知识分散在孤立的数据库中——Reactome 用于通路，STRING 用于蛋白质相互作用，ClinicalTrials.gov 用于研究注册，DrugBank 用于药物词汇，DGIdb 用于药物-基因相互作用，SIDER 用于副作用。我们展示了三个开源生物医学知识图谱——Pathways KG（来自 5 个数据源的 118,686 个节点和 834,785 条边）、Clinical Trials KG（来自 5 个数据源的 7,774,446 个节点和 26,973,997 条边）以及 Drug Interactions KG（来自 3 个数据源的 32,726 个节点和 191,970 条边）——这些图谱均构建于用 Rust 编写的高性能图数据库 Samyama 之上。我们的贡献有三个方面：首先，我们描述了一种可重复的 ETL 模式，用于从异构公共数据源构建大规模知识图谱，支持跨源去重、批量加载（Python Cypher 和 Rust 原生加载器）以及便携式快照导出；其次，我们展示了跨知识图谱的联邦化，通过将所有三个快照加载到单个图租户中，实现了跨数据集的基于属性的连接；第三，我们引入了用于大语言模型（LLM）智能体访问的模式驱动型 MCP 服务器生成，并在新的 BiomedQA 基准测试（40 个药理学问题）上进行了评估，结果显示领域特定的 MCP 工具实现了 98% 的准确率，而 text-to-Cypher 为 0%，独立 GPT-4o 为 75%。所有数据源均采用开放许可。合并后的联邦图（790 万个节点，2800 万条边）在商用云硬件上加载约需 3 分钟，跨知识图谱查询在 80 毫秒至 4 秒内完成。

## Abstract
Biomedical knowledge is fragmented across siloed databases -- Reactome for pathways, STRING for protein interactions, ClinicalTrials.gov for study registries, DrugBank for drug vocabularies, DGIdb for drug-gene interactions, SIDER for side effects. We present three open-source biomedical knowledge graphs -- Pathways KG (118,686 nodes, 834,785 edges from 5 sources), Clinical Trials KG (7,774,446 nodes, 26,973,997 edges from 5 sources), and Drug Interactions KG (32,726 nodes, 191,970 edges from 3 sources) -- built on Samyama, a high-performance graph database written in Rust.   Our contributions are threefold. First, we describe a reproducible ETL pattern for constructing large-scale KGs from heterogeneous public data sources, with cross-source deduplication, batch loading (Python Cypher and Rust native loaders), and portable snapshot export. Second, we demonstrate cross-KG federation: loading all three snapshots into a single graph tenant enables property-based joins across datasets. Third, we introduce schema-driven MCP server generation for LLM agent access, evaluated on a new BiomedQA benchmark (40 pharmacology questions): domain-specific MCP tools achieve 98% accuracy vs. 0% for text-to-Cypher and 75% for standalone GPT-4o.   All data sources are open-license. The combined federated graph (7.9M nodes, 28M edges) loads in approximately 3 minutes on commodity cloud hardware, and cross-KG queries complete in 80ms-4s.

---

## 论文详细总结（自动生成）

这篇论文介绍了一项关于大规模生物医学知识图谱（KG）构建、联邦化及 AI 智能体访问的研究。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：生物医学知识高度碎片化，分散在如 Reactome（通路）、STRING（蛋白质相互作用）、ClinicalTrials.gov（临床试验）等多个孤立数据库中。
*   **核心问题**：研究人员通常需要编写繁琐的脚本来交叉引用这些异构数据，过程缓慢、易错且不可复现。现有的知识图谱（如 Hetionet）往往是静态的，缺乏更新机制，或难以与现代大语言模型（LLM）智能体高效集成。
*   **整体含义**：本文旨在通过高性能图数据库 Samyama 构建可复现、可联邦化且支持 AI 智能体自然语言访问的大规模生物医学知识体系。

### 2. 论文提出的方法论
*   **核心思想**：利用 Rust 编写的高性能图数据库 Samyama，通过标准化的 ETL 流程构建多个独立 KG，并利用“属性连接”实现跨图谱联邦，最后通过 MCP 协议为 AI 提供工具化接口。
*   **关键技术细节**：
    *   **五阶段 ETL 模式**：下载（支持断点续传）、解析与过滤（针对人类数据）、去重（跨源实体追踪）、批量加载（Python Cypher 或 Rust 原生加载器）、快照导出（.sgsnap 便携格式）。
    *   **跨 KG 联邦（Federation）**：将多个 KG 快照加载到同一租户中，不进行物理合并，而是通过共享标识符（如 UniProt ID、DrugBank ID）在查询时进行属性连接（Property-based joins）。
    *   **模式驱动的 MCP 服务器**：自动从图数据库模式（Schema）生成类型化工具。LLM 智能体通过 Model Context Protocol (MCP) 调用这些工具（如 `pathway_members`），而非直接生成复杂的 Cypher 查询语句。

### 3. 实验设计
*   **数据集**：整合了 13 个开放许可的数据源，构建了三个 KG：
    1.  **Pathways KG**：11.9 万节点，83.5 万条边。
    2.  **Clinical Trials KG**：777 万节点，2700 万条边。
    3.  **Drug Interactions KG**：3.3 万节点，19.2 万条边。
*   **Benchmark**：引入了 **BiomedQA**，包含 40 个涵盖药理学、转化医学等 7 个类别的专业问题。
*   **对比方法**：
    1.  **Standalone GPT-4o**：仅依靠模型内置知识回答。
    2.  **Text-to-Cypher (GPT-4o)**：模型根据模式提示生成 Cypher 查询语句。
    3.  **MCP Tools（本文方法）**：模型选择并调用预定义的领域特定工具。

### 4. 资源与算力
*   **硬件环境**：AWS g4dn.4xlarge 实例（16 vCPU AMD EPYC, 62 GB RAM, NVIDIA A10G GPU）。
*   **性能表现**：
    *   **加载速度**：合并后的联邦图（7.9M 节点，28M 边）从快照加载仅需约 3 分钟。
    *   **查询延迟**：简单查询约 80-100ms，复杂的跨 KG 联邦查询在 3.6s - 4s 之间。
    *   **内存占用**：完整联邦图占用约 33 GB RAM。

### 5. 实验数量与充分性
*   **实验规模**：针对 40 个复杂的药理学问题进行了测试，并对查询延迟和构建性能进行了详细量化。
*   **充分性评价**：实验设计较为充分，涵盖了从底层数据库性能（Rust 加载器对比 Python 加载器）到上层 AI 应用准确率的全方位评估。通过对比 Text-to-Cypher 的失败案例，客观地展示了结构化工具访问的必要性。

### 6. 论文的主要结论与发现
*   **AI 访问效率**：MCP 工具在 BiomedQA 上达到了 **98% 的准确率**，而 Text-to-Cypher 准确率为 **0%**（主要由于幻觉导致的模式不匹配），独立 GPT-4o 为 75%。
*   **联邦化可行性**：证明了无需在 ETL 阶段进行复杂的实体对齐，仅通过属性连接即可在毫秒到秒级完成跨领域的复杂生物医学查询。
*   **性能优势**：Rust 原生加载器比 Python 加载器快几个数量级（Drug Interactions KG 构建仅需 928ms）。

### 7. 优点
*   **高性能与便携性**：基于 Rust 的 Samyama 数据库提供了极高的吞吐量，且 `.sgsnap` 快照机制极大地方便了知识图谱的分发与复现。
*   **创新的 AI 接口**：引入 MCP 协议，将 LLM 的角色从“查询生成者”转变为“工具选择者”，有效解决了 Text-to-Cypher 容易出错的问题。
*   **完全开源**：所有代码、ETL 流程、快照和 Benchmark 均公开，具有很高的社区价值。

### 8. 不足与局限
*   **快照时效性**：快照是静态的，而 ClinicalTrials.gov 等源数据更新频繁，需要定期重新导出。
*   **标识符覆盖率**：联邦查询的质量高度依赖于 UniProt ID 等标识符的归一化质量，部分实体若缺失 ID 则无法连接。
*   **内存门槛**：虽然在商用云服务器上表现良好，但 33GB 的内存占用对于普通个人电脑仍有一定门槛（尽管 Pathways KG 单独运行仅需 <500MB）。
*   **联邦机制**：目前的联邦是在单一租户内完成的，尚未实现真正的跨远程租户分布式查询。

（完）
