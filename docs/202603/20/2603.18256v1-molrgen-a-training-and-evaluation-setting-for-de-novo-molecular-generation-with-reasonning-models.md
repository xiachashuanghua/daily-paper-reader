---
title: "MolRGen: A Training and Evaluation Setting for De Novo Molecular Generation with Reasonning Models"
title_zh: MolRGen：一种基于推理模型的从头分子生成训练与评估设置
authors: "Philippe Formont, Maxime Darrin, Ismail Ben Ayed, Pablo Piantanida"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18256v1"
tags: ["query:llmagt"]
score: 9.0
evidence: 用于药物发现的推理大模型
tldr: 本研究针对推理型大语言模型在从头分子生成中缺乏有效训练与评估框架的问题，提出了 MolRGen 基准。该基准包含大规模数据集、全新的多样性感知 top-k 评分指标，以及一套完整的训练评估流程。通过强化学习训练 24B 参数模型，验证了推理模型在优化分子属性及生成多样性方面的潜力，为药物发现提供了新的工具和评估标准。
selection_source: fresh_fetch
motivation: 现有的分子设计方法多依赖有监督学习，缺乏针对无先验知识的从头分子生成的推理模型训练与评估体系。
method: 提出了 MolRGen 基准，引入了多样性感知 top-k 评分机制，并采用强化学习对 24B 参数的推理模型进行训练。
result: 成功构建了大规模基准数据集，并证明了强化学习能有效提升推理模型在分子生成任务中的质量与多样性。
conclusion: MolRGen 为利用推理模型进行从头分子生成提供了标准化的训练与评估框架，填补了该领域的空白。
---

## 摘要
基于推理的大语言模型（LLMs）的最新进展在复杂问题解决任务中展现了显著的提升。受这些进展的启发，多项研究探索了推理型 LLM 在药物发现和分子设计中的应用。然而，大多数现有方法要么侧重于评估，要么依赖于需要真实标签（ground-truth labels）的训练设置，例如具有已知属性修改的分子对。这种监督在从头（de novo）分子生成中是不可用的，因为其目标是在没有高分候选分子先验知识的情况下，生成能够优化期望评分的新颖分子。为了填补这一空白，我们推出了 MolRGen，这是一个用于在从头分子生成任务上训练和评估推理型 LLM 的大规模基准和数据集。我们的贡献主要有三个方面：首先，我们提出了一种用于评估和训练从头分子生成及属性预测模型的设置；其次，我们引入了一种新颖的、具有多样性感知能力的 top-k 评分，该评分能够同时捕捉生成分子的质量和多样性；第三，我们展示了该设置可用于训练分子生成的 LLM，通过强化学习训练了一个 24B 参数的 LLM，并对其性能和局限性进行了详细分析。

## Abstract
Recent advances in reasoning-based large language models (LLMs) have demonstrated substantial improvements in complex problem-solving tasks. Motivated by these advances, several works have explored the application of reasoning LLMs to drug discovery and molecular design. However, most existing approaches either focus on evaluation or rely on training setups that require ground-truth labels, such as molecule pairs with known property modifications. Such supervision is unavailable in \textit{de novo} molecular generation, where the objective is to generate novel molecules that optimize a desirability score without prior knowledge of high-scoring candidates. To bridge this gap, we introduce MolRGen, a large-scale benchmark and dataset for training and evaluating reasoning-based LLMs on \textit{de novo} molecular generation. Our contributions are threefold. First, we propose a setting to evaluate and train models for \textit{de novo} molecular generation and property prediction. Second, we introduce a novel diversity-aware top-$k$ score that captures both the quality and diversity of generated molecules. Third, we show our setting can be used to train LLMs for molecular generation, training a 24B LLM with reinforcement learning, and we provide a detailed analysis of its performance and limitations.

---

## 论文详细总结（自动生成）

### MolRGen: 基于推理模型的从头分子生成训练与评估基准总结

#### 1. 核心问题与整体含义
论文针对推理型大语言模型（Reasoning LLMs）在药物发现领域，特别是**从头分子生成（De Novo Molecular Generation）**任务中缺乏标准化训练与评估框架的问题展开研究。
*   **研究动机**：虽然推理模型在数学和编程领域表现优异，但在分子设计中，现有的 LLM 方法大多依赖于有监督的“真实标签”（如已知属性的分子对），而真正的从头生成任务在初始阶段并无高分候选分子参考。
*   **核心目标**：构建一个大规模基准（MolRGen），利用可验证的奖励（如对接评分、分子属性）来训练和评估推理模型在无先验知识情况下的分子设计能力。

#### 2. 方法论
论文提出了一个完整的闭环框架，包含任务定义、奖励设计和评估指标：
*   **任务建模**：将任务定义为根据提示词 $q$ 生成分子。提示词包含目标（最大化/最小化）、属性（如对接评分、QED、SA 等）和阈值。
*   **奖励函数设计**：
    *   **单属性奖励**：将对接评分（Docking Score）和 12 种经典分子属性归一化至 $[0, 1]$。
    *   **多目标聚合**：采用**几何平均值（Geometric Mean）**聚合多个属性奖励，确保模型必须同时优化所有目标（若任一属性为 0，总奖励即为 0）。
*   **多样性感知 Top-k 评分（Diversity-aware Top-k Score）**：
    *   这是论文的核心创新指标。传统 Top-k 只看最高分，忽略了化学多样性。
    *   该指标通过贪婪算法选择 $k$ 个分子，要求任意两个分子间的 Tanimoto 相似度低于阈值 $s_{max}$，从而同时衡量生成质量与空间探索能力。
*   **训练技术**：使用 **GRPO（Group Relative Policy Optimization）** 算法对预训练模型进行强化学习微调，通过群体比较来优化推理路径。

#### 3. 实验设计
*   **数据集**：
    *   **从头生成**：基于 SAIR 和 SIU 数据集提取了 4,500 个蛋白质结构（受体），构建了 4.9 万个训练提示词和各 1,000 个测试/评估提示词。
    *   **属性预测**：从 Polaris 平台整合了 27 个基准，包含 5.2 万个关于 ADMET 属性的提示词。
*   **对比方法（Benchmark）**：
    *   **通用推理模型**：Qwen3-30B/80B-Thinking、DeepSeek-R1 (Distill-Llama-70B/Qwen-32B)、Gemma-3-27B、MiniMax-M2 等。
    *   **化学专用模型**：ChemDFM-R、Ether0、ChemDFM-v2.0。
    *   **自研模型**：RL-Mistral（基于 Mistral-Small-24B 使用 GRPO 训练）。

#### 4. 资源与算力
*   **算力支持**：实验得到了 CalculQuebec、加拿大数字研究联盟（Digital Research Alliance of Canada）以及 MistralAI 的计算资源支持。
*   **训练细节**：RL-Mistral 模型训练了 200 个步长（Steps），每步包含 1,024 个样本（Batch Size），Group Size 为 8。
*   **硬件型号**：文中**未明确说明**具体的 GPU 型号和数量，但提到由于计算成本高昂，仅进行了有限的配置探索。

#### 5. 实验数量与充分性
*   **实验规模**：评估了超过 10 种主流 LLM，涵盖了从头生成、属性预测（回归与分类）等多个维度。
*   **充分性**：实验设计较为全面，不仅对比了 Top-k 性能，还深入分析了生成分子的有效性、唯一性、多样性以及模型在不同相似度阈值下的表现。
*   **客观性**：通过引入多样性感知指标，客观揭示了强化学习训练后模型容易出现的“多样性坍塌”问题，而非仅仅展示高分结果。

#### 6. 主要结论与发现
*   **推理能力的迁移**：通用推理模型（如 MiniMax-M2, Qwen3）在分子生成任务上表现出色，甚至能与化学专用模型持平。
*   **强化学习的效果**：通过 GRPO 训练的 RL-Mistral 在 Top-1 奖励上显著超过所有基准模型，证明了 MolRGen 提供的奖励信号能有效引导模型优化。
*   **多样性挑战**：强化学习虽然提升了采样效率（更容易找到高分分子），但会导致严重的**分布坍塌**，即模型倾向于重复生成相同的分子，导致多样性感知评分下降。
*   **属性预测难度**：所有模型在回归任务（预测具体数值）上表现普遍较差，但在分类任务上，通用推理模型表现出较强的泛化能力。

#### 7. 优点
*   **填补空白**：首次为推理模型在“无先验知识”的从头分子生成任务上提供了大规模训练环境。
*   **指标创新**：提出的“多样性感知 Top-k 评分”解决了传统指标无法衡量化学空间探索能力的问题。
*   **真实性强**：结合了实际的蛋白质-配体对接（Docking）模拟，比单纯的化学规则更接近真实的药物发现场景。

#### 8. 不足与局限
*   **代理奖励的局限性**：实验依赖于对接评分等计算模拟值，这些值只是实验结合亲和力的近似替代，存在一定偏差。
*   **多样性坍塌**：目前的训练策略（原生 GRPO）未能很好地平衡“高奖励”与“高多样性”，模型容易陷入局部最优。
*   **对齐风险**：研究发现某些模型（如 Ether0）由于过度的安全对齐，会将合法的分子设计请求误判为“制造有害物质”而拒绝回答。
*   **计算成本**：大规模对接模拟和强化学习训练对算力需求极高，限制了对更多超参数和初始化策略的探索。

（完）
