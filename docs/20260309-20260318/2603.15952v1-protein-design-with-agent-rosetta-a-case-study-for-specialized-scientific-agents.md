---
title: "Protein Design with Agent Rosetta: A Case Study for Specialized Scientific Agents"
title_zh: 使用 Agent Rosetta 进行蛋白质设计：专用科学智能体案例研究
authors: "Jacopo Teneggi, S. M. Bargeen A. Turzo, Tanya Marwah, Alberto Bietti, P. Douglas Renfrew, Vikram Khipple Mulligan, Siavash Golkar"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15952v1"
tags: ["query:llmagt"]
score: 10.0
evidence: 用于自主蛋白质设计和科学任务的LLM智能体
tldr: Agent Rosetta 是一款结合了大语言模型推理能力与 Rosetta 物理建模软件的自主科学智能体。它通过构建结构化环境，解决了现有机器学习方法在非天然氨基酸设计上的局限性。该智能体能够迭代优化设计方案，在天然与非天然残基设计任务中均达到了专家级水平，证明了良好的环境设计对于提升 LLM 在专业科学软件中应用的重要性。
selection_source: fresh_fetch
motivation: 现有的蛋白质设计机器学习方法大多局限于天然氨基酸和单一目标，难以处理复杂的非天然聚合物设计需求。
method: 开发了 Agent Rosetta，将 LLM 与 Rosetta 软件的结构化操作环境相结合，通过迭代推理和工具调用实现自动化设计。
result: 在天然氨基酸设计上匹配了专用模型，在非天然残基设计上填补了机器学习的空白，且表现优于单纯的提示词工程。
conclusion: 结构化环境设计是使 LLM 智能体能够有效驱动专业科学软件并达到专家级性能的关键。
---

## 摘要
大语言模型（LLMs）能够模拟推理并使用工具，为执行复杂科学任务的自主智能体创造了机会。蛋白质设计提供了一个天然的试验场：尽管机器学习（ML）方法取得了显著成果，但这些方法很大程度上局限于标准氨基酸和狭窄的目标，对于适用于广泛设计流程的通用工具仍存在未满足的需求。我们介绍了 Agent Rosetta，这是一个与结构化环境配对的 LLM 智能体，用于操作 Rosetta。Rosetta 是领先的基于物理的杂聚物设计软件，能够对非标准构建模块和几何结构进行建模。Agent Rosetta 通过迭代优化设计以实现用户定义的目标，将 LLM 的推理能力与 Rosetta 的通用性相结合。我们在标准氨基酸设计上评估了 Agent Rosetta，其表现与专用模型和专家基准相当；在机器学习方法失效的非标准残基设计上，它也取得了相当的性能。关键在于，仅靠提示工程往往无法生成有效的 Rosetta 操作，这表明环境设计对于将 LLM 智能体与专用软件集成至关重要。我们的结果表明，设计合理的环境使 LLM 智能体能够让科学软件变得易于使用，同时在性能上匹配专用工具和人类专家。

## Abstract
Large language models (LLMs) are capable of emulating reasoning and using tools, creating opportunities for autonomous agents that execute complex scientific tasks. Protein design provides a natural testbed: although machine learning (ML) methods achieve strong results, these are largely restricted to canonical amino acids and narrow objectives, leaving unfilled need for a generalist tool for broad design pipelines. We introduce Agent Rosetta, an LLM agent paired with a structured environment for operating Rosetta, the leading physics-based heteropolymer design software, capable of modeling non-canonical building blocks and geometries. Agent Rosetta iteratively refines designs to achieve user-defined objectives, combining LLM reasoning with Rosetta's generality. We evaluate Agent Rosetta on design with canonical amino acids, matching specialized models and expert baselines, and with non-canonical residues -- where ML approaches fail -- achieving comparable performance. Critically, prompt engineering alone often fails to generate Rosetta actions, demonstrating that environment design is essential for integrating LLM agents with specialized software. Our results show that properly designed environments enable LLM agents to make scientific software accessible while matching specialized tools and human experts.

---

## 论文详细总结（自动生成）

### 论文总结：Agent Rosetta —— 专用科学智能体案例研究

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：虽然机器学习（ML）模型（如 AlphaFold, ProteinMPNN）在蛋白质设计领域取得了巨大成功，但它们通常局限于 20 种天然氨基酸，且依赖大量训练数据。对于涉及非天然氨基酸（NCAA）或复杂几何约束的任务，这些模型往往失效。
*   **核心问题**：传统的物理建模软件（如 Rosetta）虽然功能强大且支持非天然残基，但其操作门槛极高，需要深厚的生物物理背景和复杂的脚本编写能力（如 RosettaScripts）。
*   **整体含义**：本论文旨在开发一种基于大语言模型（LLM）的自主智能体 **Agent Rosetta**，通过构建结构化的交互环境，将 LLM 的推理能力与 Rosetta 的物理模拟能力结合，实现复杂科学任务的自动化处理。

#### 2. 方法论：核心思想与关键技术
*   **核心思想**：建立一个“智能体-环境”闭环系统。智能体不直接编写原始 XML 脚本，而是通过一个类似 OpenAI Gym 的结构化环境进行交互，将复杂的语法细节抽象为语义化的操作。
*   **关键技术细节**：
    *   **环境设计（Environment Design）**：这是本文的核心。研究者发现单纯靠提示词工程（Prompt Engineering）无法让 LLM 生成完全正确的 Rosetta 语法。因此，他们设计了一套简化语法，由环境负责将其转换为标准的 RosettaScripts。
    *   **多轮迭代流程**：
        1.  **状态感知**：环境返回当前设计的物理指标（能量、空腔体积、RMSD 等）。
        2.  **结构化推理与动作选择**：智能体分析数据并选择动作（如侧链优化、骨架调整）。
        3.  **文档检索与参数生成**：环境提供所选动作的详细文档，智能体据此生成具体参数。
        4.  **执行与反馈**：环境执行 Rosetta 模拟，若出错则返回错误信息供智能体自我修正。
    *   **动作空间**：包括 `rotamer_change`（侧链旋转异构体改变）、`backbone_change`（骨架扰动）和 `go_back_to`（回溯到之前的状态）。

#### 3. 实验设计
*   **场景一：天然氨基酸骨架稳定化**
    *   **数据集**：8 个 PDB 结构（长度 74-125 残基）。
    *   **对比方法（Benchmark）**：专用 ML 模型 ProteinMPNN、人类专家编写的单步协议（One-shot）和分阶段协议（Staged）、以及简单的能量最小化基准（Na¨ıve）。
*   **场景二：非天然氨基酸（NCAA）设计**
    *   **任务**：在蛋白质核心引入 N1-甲酰基色氨酸（TRF）。
    *   **数据集**：4 个从头设计的蛋白质结构。
    *   **对比方法**：由于 ML 模型不支持 NCAA，主要对比人类专家协议，并使用 AlphaFold 3 进行事后验证。

#### 4. 资源与算力
*   **LLM 模型**：使用了 Qwen3-Instruct、GPT-4/5、Gemini 2.5 Flash 和 Claude 4.5 等前沿模型。
*   **算力说明**：
    *   论文未明确给出训练智能体的算力（因为是基于预训练模型的推理），但提到了 Rosetta 模拟的开销。
    *   每个设计步骤会生成包含 **128 个候选设计** 的系综，这需要在高性能计算集群（如 Flatiron Institute 的集群）上进行并行计算。
    *   LLM 的推理成本在图中有所体现（单次任务约 0.03 到 1.1 美元不等）。

#### 5. 实验数量与充分性
*   **实验规模**：针对 12 个不同的蛋白质结构进行了测试，每个方法运行了 16 次独立重复实验，并使用了 1000 次自助法（Bootstrap）抽样进行统计评估。
*   **充分性**：实验涵盖了从天然到非天然、从简单到复杂的多种场景。通过对比多种 LLM 底座和人类专家水平，验证了系统的鲁棒性。
*   **客观性**：引入了第三方验证工具（如 ESMFold 和 AlphaFold 3）作为客观评价指标，而非仅依赖 Rosetta 自身的评分函数。

#### 6. 主要结论与发现
*   **环境设计至关重要**：单纯的提示词工程在处理专业科学软件时极易出错，结构化的环境抽象是实现自主科学智能体的关键。
*   **性能匹配专家**：在天然氨基酸设计任务中，Agent Rosetta 的表现与专用 ML 模型 ProteinMPNN 相当；在 NCAA 设计任务中，其表现优于人类专家编写的固定协议。
*   **自我修正能力**：智能体能够根据环境反馈的错误信息（如语法错误或物理冲突）进行有效的自我调试。

#### 7. 优点（亮点）
*   **通用性**：突破了数据驱动模型的限制，能够处理非天然聚合物等缺乏训练数据的领域。
*   **易用性**：降低了使用 Rosetta 等复杂科学软件的门槛，非专家用户也能通过自然语言指令进行设计。
*   **迭代优化**：相比人类专家的静态协议，智能体能根据中间结果动态调整策略，探索更广阔的设计空间。

#### 8. 不足与局限
*   **成本与速度**：相比于毫秒级响应的 ProteinMPNN，Agent Rosetta 依赖 LLM API 调用和耗时的物理模拟，效率较低且存在 API 费用。
*   **动作空间限制**：目前仅集成了 Rosetta 的部分核心功能，对于更复杂的对接（Docking）或多链相互作用尚需扩展。
*   **验证依赖**：虽然能生成设计，但最终仍需依赖如 AlphaFold 3 或实验验证来确认结构的真实稳定性，物理模拟的准确性上限决定了智能体的上限。

（完）
