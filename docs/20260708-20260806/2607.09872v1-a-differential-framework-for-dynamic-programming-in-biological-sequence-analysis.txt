Title: A Differential Framework for Dynamic Programming in Biological Sequence Analysis

URL Source: https://arxiv.org/pdf/2607.09872v1

Published Time: Tue, 14 Jul 2026 00:08:11 GMT

Number of Pages: 74

Markdown Content:
# A Differential Framework for Dynamic Programming in Biological Sequence Analysis 

## Kiyoshi Asai 1,2∗

> 1

AI-Empowered Life Science Initiative (ALIS), Research Organization of Information and Systems, 4-1-1 Toranomon, Tokyo 105-6929, Japan 

> 2

School of Life Science and Technology, Institute of Science Tokyo, Tokyo, Japan 

Background: Dynamic programming in biological sequence analysis computes probabili-ties or partition functions by summing over exponentially many latent paths, alignments, derivation trees, or RNA secondary structures. Their backward and outside quantities are used model-specifically, but the relation between differential sensitivities and exact finite sequence changes is rarely stated in a common framework. 

Methods: We represent hidden Markov models, affine-gap alignment ensembles, stochastic context-free grammars, and RNA secondary-structure ensembles as sum–product dynamic programs, defining backward and outside quantities as adjoints of forward or inside variables and sequence changes as finite replacements of sequence-dependent local factors. 

Results: Posterior item marginals are normalized inside–outside products, local-event posteriors additionally include the local factor and child inside terms, and expected feature counts are logarithmic derivatives of the partition function. For HMMs, ordinary SCFGs, and single-position substitutions in affine-gap alignment, the partition function is multi-affine in position-specific factor groups, so a one-site change is recovered exactly from first-derivative coefficients and multisite changes from mixed derivatives. In nearest-neighbor RNA models a substitution alters overlapping loop, stacking, and multiloop factors and boundary contexts, so exact mutation effects instead require context-dependent inside– outside recombination, as in the Rchange algorithm. Numerical experiments reproduce brute-force recomputation to machine precision. 

Conclusions: The framework identifies when derivatives give exact finite sequence effects and when broader recombination is required, providing a unified basis for posterior marginals, expected counts, parameter sensitivity, mutation analysis, and sequence design. 

Keywords: dynamic programming; adjoint variables; hidden Markov model; sequence alignment; stochastic context-free grammar; RNA secondary structure; partition function; finite sequence perturbation  

> ∗Correspondence: asai@cbrc.jp

1arXiv:2607.09872v1 [q-bio.QM] 10 Jul 2026 1 Introduction 

Dynamic programming underlies many biological sequence-analysis methods that sum over exponentially many latent objects, including hidden state paths, alignment paths, derivation trees, and RNA secondary structures. Although these models differ biologically and geometrically, each evaluates a global probability or partition function by recursively combining local factors. Their forward or inside variables collect partial structures, whereas backward or outside variables collect compatible contexts. These quantities are usually introduced separately in HMMs, alignment, SCFGs, and RNA folding (Durbin et al., 1998), but they share a common interpretation: a backward or outside quantity is an adjoint, namely the derivative of the global partition function with respect to an intermediate forward or inside variable. Posterior marginals are then normalized inside–outside products, and expected feature counts are logarithmic derivatives with respect to local factors. This interpretation is connected to semiring dynamic programming (Goodman, 1999), sum–product message passing (Kschischang et al., 2001), network-polynomial differentia-tion (Darwiche, 2003), reverse-mode automatic differentiation (Griewank and Walther, 2008; Baydin et al., 2018), and its application to structured prediction (Eisner, 2016). Differentiable dynamic programming has also been developed as a trainable structured layer (Mensch and Blondel, 2018). We take this adjoint interpretation as established background; the contribution here concerns not the interpretation itself but its exact relation to finite sequence changes. That relation—between infinitesimal derivatives and discrete sequence changes—has been treated less systematically than the adjoint identities themselves. A parameter derivative describes infinitesimal sensitivity, whereas a nucleotide or amino-acid substitution finitely replaces the local factors selected by the observed symbols. These coincide only under specific algebraic conditions. After introducing position-specific factor groups, the partition functions of HMMs and of ordinary SCFGs—those in which each derivation emits exactly one terminal at each sequence position, with no context-dependent emission factorization—are multi-affine in those groups; the same holds for a substituted position 2in the affine-gap alignment ensemble considered here. A one-site replacement is then recovered exactly from first-derivative coefficients, whereas multiple replacements require mixed terms. This is an exact finite polynomial identity, not a first-order approximation. Nearest-neighbor RNA thermodynamic models expose the boundary of this simple position-wise factorization. One nucleotide can alter several overlapping hairpin, interior-loop, stacking, dangling-end, and multiloop factors, as well as the boundary contexts of dynamic-programming cells. Exact mutation effects therefore require context-dependent recombination of inside quantities, mutant local factors, and outside contexts. Predicting how mutations perturb the RNA structural ensemble has been addressed by a range of tools (Waldispühl et al., 2008; Halvorsen et al., 2010; Sabarinathan et al., 2013; Churkin and Barash, 2006); among these, the Rchange algorithm computes exact partition-function changes by this context-dependent recombination (Kiryu and Asai, 2012). This paper makes four contributions. First, we place HMMs, affine-gap alignment ensembles (in the Boltzmann formulation of Miyazawa (1995), which relates alignment to both pair-HMMs and RNA thermodynamics), SCFGs, and RNA partition-function programs in a single sum–product and adjoint calculus, and within it we keep separate three objects that are easily conflated: posterior item marginals, posterior local-event terms, and expected feature counts obtained as logarithmic derivatives of the partition function. Second, and centrally, we give a model-independent sufficient criterion—multi-affinity of the partition function in position-specific factor groups—under which the adjoint (first-derivative) coefficients reconstruct an exact finite sequence substitution (Section 2.3). HMMs, ordinary SCFGs, and single-site substitutions in affine-gap alignment satisfy it, so a one-site change is an exact polynomial identity in the outside quantities, whereas multisite changes require the corresponding mixed derivatives. Third, we identify nearest-neighbor RNA thermodynamics as the regime in which this criterion fails, and show that the Rchange algorithm realizes precisely the finite inside–local–outside recombination that the framework prescribes once a single derivative no longer suffices (Section 4). Fourth, we verify the finite-replacement identities numerically against brute-force recomputation for HMM, alignment, SCFG, and RNA-like models, obtaining machine-precision agreement 3(Section 5). These contributions are stated as exact, checkable identities with an explicit validity criterion, rather than as a survey of the adjoint viewpoint. The resulting framework separates posterior inference, infinitesimal sensitivity, and exact finite perturbation, and makes explicit when a first-derivative contraction coincides with an exact finite replacement—namely, under the multi-affinity condition of Section 2.3. 

# 2 A General Differential Framework Illustrated by HMMs and SCFGs 

Hidden Markov models (HMMs) and stochastic context-free grammars (SCFGs) provide canonical path- and tree-structured examples of the common construction. We there-fore introduce each general identity together with its HMM and SCFG instantiations. Complete recursions, boundary conditions, and explicit two-site expansions are given in Supplementary Section S1. 

## 2.1 Partition functions and adjoints 

Let D denote the observed sequence data and let Ω be the latent objects enumerated by a sum–product dynamic program. For nonnegative local factors ϕr, define 

Z = ∑

> ω∈Ω

w(ω), w(ω) = ∏

> r∈ω

ϕr. (1) Assuming Z > 0, P (ω | D ) = w(ω)/Z . Posterior expectations are taken over the derivations actually enumerated by the dynamic program. Thus, for an ambiguous grammar, ω is a derivation tree and distinct derivations are counted separately; identifying the same distribution with one over biological structures additionally requires an unambiguous representation. If admissibility depends on the observed sequence, we use a common combinatorial universe and assign zero weight to inadmissible choices, so that original and mutated sequences are compared on the same latent-object space. Let Au be the forward or inside value of item u. A broad class of recursions can be 4written as 

Au = ∑

> r∈R (u)

ϕr

> ∏
> v∈ch( r)

Av, (2) where R(u) contains the local decompositions creating u and ch (r) is the tuple of child items of decomposition r; the root item uroot satisfies Auroot = Z. The adjoint 

Bu = ∂Z ∂A u

(3) satisfies reverse accumulation through every parent term, 

Bu = 1{u = uroot } + ∑   

> p:Apdepends on Au

Bp

∂A p

∂A u

. (4) This is the abstract backward or outside recursion. For an HMM with states Q, transition probabilities aij , emission probabilities ej (c)

(Rabiner, 1989; Durbin et al., 1998), and an observed sequence x = x1 · · · xL,

fj (t) = ej (xt) ∑

> i∈Q

fi(t − 1) aij . (5) Writing Z = P (x), the usual backward quantity is precisely 

bi(t) = ∂P (x)

∂f i(t) . (6) For a Chomsky-normal-form SCFG with nonterminal set N , start symbol S ∈ N ,binary production probabilities tv→yz (v, y, z ∈ N ), and terminal emission probabilities 

ev(c), let α(i, j, v ) be the inside probability that nonterminal v ∈ N derives xi · · · xj . For 

i < j ,

α(i, j, v ) = 

> j−1∑
> k=i
> ∑
> y,z ∈N

tv→yz α(i, k, y )α(k + 1 , j, z ), (7) with α(i, i, v ) = ev(xi) and Z = P (x) = α(1 , L, S ). The standard outside quantity of the inside–outside algorithm (Baker, 1979; Lari and Young, 1990) is the adjoint 

β(i, j, v ) = ∂P (x)

∂α (i, j, v ). (8) 5Thus HMM backward variables and SCFG outside probabilities are not separate proba-bilistic constructions; both are reverse-mode derivatives of the corresponding forward or inside program. 

## 2.2 Posterior marginals and expected counts as logarithmic derivatives 

Posterior item and local-event quantities follow algebraically from differentiation of the sum–product computation and can be read as expected occurrence counts. When the differentiated quantity is a shared model factor, the same relation becomes the stan-dard exponential-family identity between log-partition derivatives and expected sufficient statistics (Wainwright and Jordan, 2008). Let Nu(ω) be the number of uses of item u. Then 

AuBu

Z = E[Nu | D ], (9) which, when Au > 0, equals the logarithmic derivative ∂ log Z/∂ log Au (using Bu =

∂Z/∂A u); the algebraic identity itself holds even when Au = 0 . It is an occurrence probability when the item is used at most once; in an HMM this is P (πt = j | x) = 

fj (t)bj (t)/P (x), and in an SCFG P ([ i, j ], v | x) = α(i, j, v )β(i, j, v )/P (x).A local decomposition r ∈ R(u) is more specific. With the local term 

Tr = ϕr 

> ∏
> v∈ch( r)

Av,

BuTr

Z = E[Nr | D ], (10) for example fi(t − 1) aij ej (xt)bj (t)/P (x) for the HMM transition–emission event i → j

at t, and β(i, j, v )tv→yz α(i, k, y )α(k + 1 , j, z )/P (x) for an SCFG production v → yz on 

[i, j ] split at k. Equations (9) and (10) must not be conflated: the first sums all ways of creating an item, the second selects one local term. Applied instead to a shared factor type ϕk > 0, for which w(ω) = ∏ 

> k

ϕNk (ω) 

> k

, the same 6identity gives expected feature counts, 

∂ log Z∂ log ϕk

= ϕk

Z∂Z ∂ϕ k

= E[Nk | D ]; (11) in particular ∂ log P (x)/∂ log aij = E[N (T ) 

> ij

| x] for HMM transitions (with an analogous emission identity) and ∂ log P (x)/∂ log tv→yz = E[N (T ) 

> v→yz

| x] for SCFG productions. Under probabilistic normalization, derivatives in an unconstrained parameterization follow by the chain rule. These expected counts are the sufficient statistics re-estimated in EM training (Baum–Welch and inside–outside) and serve as feature vectors in marginalized kernels for biological sequences (Tsuda et al., 2002), including count kernels for RNA (Kin et al., 2002). 

## 2.3 Exact finite replacements and multi-affinity 

A sequence substitution is a finite replacement of position-specific local factors, not an infinitesimal parameter change. For each affected position p, collect the replaced factors into ρp = ( ρp,a )a∈A p . We call Z multi-affine in these groups if it is affine in each ρp with the others fixed, equivalently if no monomial contains more than one factor from the same group. If every latent object uses exactly one factor from a group, the dependence is additionally homogeneous of degree one in that group. HMM emission groups and ordinary SCFG terminal groups have this stronger property. Let P be the set of substituted positions. For U ⊆ P , write AU = ∏ 

> p∈U

Ap and 

aU = ( ap)p∈U .

Proposition 1 (Exact finite replacement under multi-affinity) . Suppose that Z is multi-affine in the factor groups {ρp : p ∈ P} . Then 

Z(

{ρp + ∆ ρp : p ∈ P} )

− Z(

{ρp : p ∈ P} )

= ∑  

> ∅̸ =U⊆P
> ∑
> aU∈A U

∂|U |Z 

> ∏
> p∈U

∂ρ p,a p

> ∏
> p∈U

∆ρp,a p , (12) 

with derivatives evaluated at the original factors. 

7Proof. Expand successively in the affine coordinates of the groups in P. A second derivative with respect to two coordinates from the same group vanishes, so each surviving term contains at most one increment from each position group. For one replaced position, 

∆pZ = ∑

> a∈A p

∂Z ∂ρ p,a 

∆ρp,a . (13) This is exact because Z is affine in the entire replaced group. The corresponding log-partition change remains nonlinear, 

∆p log Z = log 

> (

1 + ∆pZZ

> )

. (14) For an HMM, set ρp,s = es(xp). Every hidden path emits position p exactly once, and 

∂P (x)

∂ρ p,s 

=

> (∑
> r

fr(p − 1) ar,s 

> )

bs(p). (15) Here we use the standard start convention fs(1) = πses(x1), so that at p = 1 the coefficient reads πsbs(1) ; the initial probabilities πs are treated as separate factors (Supplementary Section S1). Hence, for xp → c∗,

P (xp→c∗

) − P (x) = ∑

> s

∂P (x)

∂ρ p,s 

{es(c∗) − es(xp)}. (16) For an ordinary SCFG, set ρp,v = ev(xp). Every derivation tree contains exactly one terminal emission at p, so 

∂P (x)

∂ρ p,v 

= β(p, p, v ), (17) and 

P (xp→c∗

) − P (x) = ∑

> v

β(p, p, v ){ev(c∗) − ev(xp)}. (18) Thus the same outside quantities used for posterior inference are exact coefficients for finite one-site replacements. 8For two positions p < q , the exact expansion is 

∆p,q Z = ∑

> a

∂Z ∂ρ p,a 

∆ρp,a + ∑

> b

∂Z ∂ρ q,b 

∆ρq,b 

+ ∑

> a,b

∂2Z∂ρ p,a ∂ρ q,b 

∆ρp,a ∆ρq,b . (19) The final term is the mixed finite interaction. Repeated substitutions at a single position compose into one replacement of that position’s factor group, so it suffices to treat distinct positions p < q . Higher-order replacements require the corresponding nonempty subsets in Eq. (12) ; the number of terms therefore grows exponentially with the number of replaced positions. Explicit HMM and SCFG one- and two-site expansions are given in Supplementary Section S1. 

# 3 Extensions to Alignment and RNA Structure En-sembles 

Alignment and RNA folding instantiate the same adjoint, posterior, and logarithmic-derivative identities as Section 2, but they also expose two important extensions. In affine-gap alignment, a residue may be consumed without using any match factor, so the relevant position group is affine but not generally homogeneous. In nearest-neighbor RNA folding, one nucleotide can alter several overlapping factors and boundary contexts, so a mutation is not localized to one position-specific factor group. 

## 3.1 Affine-gap alignment ensembles 

For sequences x = x1 · · · xL and y = y1 · · · yK , an alignment is a monotone lattice path. We use the Boltzmann factors 

q(a, b ) = exp {s(a, b )/T }, pd = exp {− d/T }, pe = exp {− e/T }, (20) 9where d and e are gap-opening and extension penalties. Let FM , F X , F Y be the forward values ending in a match, an x-residue gap state, or a y-residue gap state. Under the standard affine-gap convention (Gotoh, 1982), 

FM (i, j ) = q(xi, y j ) ∑  

> U∈{ M,X,Y }

FU (i − 1, j − 1) , (21) 

FX (i, j ) = pdFM (i − 1, j ) + peFX (i − 1, j ), (22) 

FY (i, j ) = pdFM (i, j − 1) + peFY (i, j − 1) . (23) The terminal sum is the partition function Z(x, y ). With normalized transitions and state-specific emissions, the same recursion topology gives a three-state pair-HMM (Durbin et al., 1998). We retain the score-based Boltzmann form of Miyazawa (1995), closely related to the probabilistic alignment interpretation of Bucher and Hofmann (1996), because it places alignment scores and RNA free-energy parameters in the same exponential-family calculus: 

∂ log Z∂s (a, b ) = 1

T E[NM (a, b ) | x, y ], (24) 

∂ log Z∂d = − 1

T E[Nopen | x, y ], (25) 

∂ log Z∂e = − 1

T E[Next | x, y ]. (26) The lattice adjoints 

BU (i, j ) = ∂Z (x, y )

∂F U (i, j ), U ∈ { M, X, Y }, (27) produce the match-item marginal 

P (xi ∼ yj | x, y ) = FM (i, j )BM (i, j )

Z(x, y ) (28) 10 and, for example, the local posterior of opening an X-gap, 

FM (i − 1, j )pdBX (i, j )

Z(x, y ) . (29) For a substitution xi → x∗ 

> i

, define the match-factor group ρMi,j = q(xi, y j ). A path may match xi to one yj , or align xi to a gap and use no member of this group. Therefore Z is affine, but not generally homogeneous, in {ρMi,j : 1 ≤ j ≤ K}. Section 2 then gives 

∂Z (x, y )

∂ρ Mi,j 

= FM (i, j )BM (i, j )

q(xi, y j ) , (30) and the exact finite change 

Z(xi→x∗ 

> i

, y ) − Z(x, y )=

> K∑
> j=1

FM (i, j )BM (i, j )

q(xi, y j ) {q(x∗ 

> i

, y j ) − q(xi, y j )}. (31) This treats a substitution in one sequence. If both sequences are mutated, the factor 

q(xi, y j ) at an alignable pair (i, j ) belongs to the position groups of both xi and yj , so it must be replaced jointly rather than as two independent single-site effects. Complete boundary conditions, reverse recursions, gap-event formulas, and finite-replacement derivations are given in Supplementary Section S2. 

## 3.2 RNA secondary-structure ensembles 

For an RNA sequence x, let S be a common set of pseudoknot-free secondary-structure candidates. For a local configuration ℓ, set ϕx(ℓ) = exp {− Ex(ℓ)/RT } when compatible with x, and ϕx(ℓ) = 0 otherwise, where Ex(ℓ) is a (molar) free energy, R is the gas constant, and T is the absolute temperature; unlike the formal temperature scaling dimensionless scores in Section 3.1, the physical Boltzmann factor here carries RT . Then 

Z(x) = ∑

> σ∈S
> ∏
> ℓ∈σ

ϕx(ℓ). (32) 11 McCaskill-type recursions evaluate this sum through interval quantities such as Zb(i, j ),the inside partition function conditional on base pair (i, j ) (McCaskill, 1990). Its outside adjoint and base-pair posterior are 

W b(i, j ) = ∂Z ∂Z b(i, j ), (33) 

P (i, j | x) = Zb(i, j )W b(i, j )

Z(x) . (34) If E(σ) = ∑ 

> k

θkfk(σ) and ηk = −θk/RT , then 

∂ log Z∂η k

= E[fk | x]. (35) Thus the same adjoint and logarithmic-derivative identities apply. Derivatives of base-pairing probabilities with respect to thermodynamic parameters, obtained from the same differentiated partition-function machinery, have recently been used to estimate RNA thermodynamic parameters (Yamamura et al., 2025b). Sequence changes, however, behave differently: because one nucleotide can participate in several overlapping loop, stacking, and multiloop factors and can alter nearest-neighbor boundary contexts, a mutation is not one replacement of a single position-specific factor group, and exact mutation effects require the context-dependent recombination developed in Section 4. A simplified McCaskill-type decomposition with its outside adjoints, together with the Rchange finite-difference formulas, is given in Supplementary Section S4. 

## 3.3 Unified correspondence across the four models 

Table 1 summarizes the model-specific items, adjoints, posterior marginals, expected counts, and finite-replacement structure. Figure 1 distinguishes posterior item marginals from posterior local-event terms. 12 Table 1: Correspondence among four biological sequence-analysis models. Backward or outside quantities are adjoints of forward or inside items. An item marginal is AuBu/Z ;a particular local event additionally contains its local factor and child inside values. Expected feature counts are logarithmic derivatives with respect to shared local factors. For alignment, U ∈ { M, X, Y }.

HMM Affine-gap alignment SCFG RNA (McCaskill) 

Latent object state path π alignment path π derivation tree π secondary structure 

σ

Partition function 

P (x) Z(x, y ) P (x) = α(1 , L, S ) Z(x)

Forward / inside item 

fj (t) FU (i, j ) α(i, j, v ) Zb(i, j )

Backward / outside adjoint 

bj (t) = ∂P (x)

∂f j (t) BU (i, j ) = 

∂Z ∂F U (i, j )

β(i, j, v ) = 

∂P (x)

∂α (i, j, v )

W b(i, j ) = ∂Z ∂Z b(i, j )

Shared local factors 

aij , e j (c) q(a, b ), p d, p e tv→yz , e v (c) ϕ(ℓ) = e−E(ℓ)/RT 

Posterior item marginal 

fj (t)bj (t)

P (x)

FM (i, j )BM (i, j )

Z(x, y )

α(i, j, v )β(i, j, v )

P (x)

Zb(i, j )W b(i, j )

Z(x)

Representative expected count 

∂ log P (x)

∂ log aij 

∂ log Z∂ log q(a, b )

∂ log P (x)

∂ log tv→yz 

∂ log Z∂η k

One-site sequence replacement exact under position-specific emission multi-affinity exact for position-specific substitution factors exact under terminal-factor multi-affinity context-dependent inside–outside recombination For RNA, ηk = −θk/RT (with R the gas constant and T the absolute temperature) is the natural parameter associated with structural feature fk. With a suitable normalized parameterization of state transitions and state-specific emissions, the same three-state recursion topology yields a pair-HMM. 

13 Bu = ∂Z ∂A u

AuBu

Z = E[Nu] Buϕr 

> ∏
> v∈ch( r)

Av

Z = E[Nr]

∂ log Z∂ log ϕk

= E[Nk]   

> fs(t)bs(t)
> forward / inside backward / outside
> position t

(a) HMM: P (πt = s | x) = fs(t)bs(t)/P (x)  

> FM(i, j )
> BM(i, j )
> y
> x

(b) Alignment: 

P (xi ∼ yj | x, y ) = FM BM /Z (x, y )  

> β(i, j, v )
> α(i, j, v )
> S
> 1Lij

(c) SCFG: 

P ([ i, j ], v | x) = α(i, j, v )β(i, j, v )/P (x)  

> Wb(i, j )
> Zb(i, j )
> ij

(d) RNA: 

P (i, j | x) = Zb(i, j )W b(i, j )/Z (x)

Figure 1: Adjoint anatomy shared by the four models. The panels show item marginals: an inside or forward item Au is paired with its outside or backward adjoint Bu, and 

AuBu/Z gives the posterior expected use of the selected item (green). The boxed relations additionally show the posterior of a particular local decomposition r, which contains its local factor ϕr and child inside values. Blue denotes inside/forward contributions and orange denotes outside/backward contexts. 14 4 Context-Dependent RNA Recombination 

## 4.1 Why a nucleotide mutation is not a single factor-group replacement 

Section 3.2 introduced the RNA secondary-structure partition function [Eq. (32) ], the McCaskill-type interval quantity Zb(i, j ) with its outside adjoint W b(i, j ), the base-pair posterior, and the logarithmic-derivative identity for expected structural features. To compare a sequence with its mutants we add only one convention: a common latent-object universe. Let ΩL denote a sequence-independent set of pseudoknot-free secondary-structure candidates on L labeled positions, and assign zero weight to decompositions incompatible with the sequence—for an admissible local decomposition let ϕℓ(x) = exp {− Eℓ(x)/RT },and for an inadmissible one set ϕℓ(x) = 0 . Then 

Z(x) = ∑

> σ∈ΩL
> ∏
> ℓ∈σ

ϕℓ(x), (36) which separates the fixed combinatorial universe from the sequence-dependent thermo-dynamic factors and reduces to Eq. (32) for each fixed sequence. The McCaskill-type quantities and posteriors of Section 3.2 are evaluated on this common universe. The RNA dynamic program is differentiable, and its adjoint identities deliver posterior inference and parameter sensitivity exactly as before. The real question is whether a sequence change can be written as one affine factor-group replacement of the type used in Section 2.3. It cannot, because nearest-neighbor thermodynamics has a different sequence-to-factor map from HMMs, ordinary SCFGs, or the basic affine-gap alignment ensemble, in each of which a position is consumed once and affects at most one factor group. A single nucleotide can instead contribute to alternative hairpin, interior-loop, bulge, stacking, dangling-end, terminal-mismatch, or multiloop factors, and nearest-neighbor factors can depend on bases at the boundary of a dynamic-programming interval. A mutation can therefore change 1. several local factors within the same structural decomposition; 15 2. factors belonging to many alternative decompositions; and 3. the boundary context under which neighboring inside or outside cells must be evaluated. Recall the one-site result of Section 2.3. When the factors selected by position p form a single affine group {ρp,a }a∈A p , Proposition 1 gives the exact difference ∆pZ = ∑

> a

(∂Z/∂ρ p,a ) ∆ ρp,a ,a sum over that one group whose coefficients are fixed inside–outside products. Nearest-neighbor RNA breaks this one-group hypothesis, but not the broader finite-recombination principle: the factors touched by p no longer form one group, yet the partition function is still a polynomial that is linear in each decomposition-specific factor occurrence. The remedy keeps the principle but broadens the one-group contraction into a context-dependent recombination over inside–local–outside cases, with joint replacement of all mutation-dependent primitive factors in each case. We first make the relevant inside and outside quantities concrete in McCaskill terms (Section 4.2), then assemble the mutant partition function by context-dependent finite recombination of these cases (Section 4.3); the RNA-like SCFG of Section 4.4 is the specialization in which the cases reduce to four explicit roles. 

## 4.2 McCaskill decompositions and outside adjoints 

We work with concrete McCaskill-type inside quantities: Zbi,j , the partition function of structures in which i and j pair; and the multiloop partition functions Zmi,j (a multiloop segment on [i, j ] containing at least one branch) and Zm1 

> i,j

(the contribution of a multiloop segment containing one base-paired component starting at i, followed by unpaired bases). Write W bi,j = ∂Z/∂Z bi,j for the corresponding outside adjoint [Eq. (33) ]. A representative inside recursion for the paired state is 

Zbi,j = ϕ1(i, j ) + ∑

> i<k<ℓ<j

Zbk,ℓ ϕ2(i, j, k, ℓ ) + ∑

> i+1 <k<j

Zmi+1 ,k −1 Zm1 

> k,j −1

ϕ3(i, j ), (37) where ϕ1(i, j ), ϕ2(i, j, k, ℓ ), and ϕ3(i, j ) are the Boltzmann factors of, respectively, a hairpin closed by (i, j ), an interior loop or stack enclosing (k, ℓ ) inside (i, j ), and a multiloop 16 closed by (i, j ). Equation (37) is a representative decomposition drawn from the simplified McCaskill-type system used in Supplementary Section S4; it is not a complete Turner nearest-neighbor implementation, and the full recursions, boundary conventions, and the remaining states are given there. Differentiating Z through Eq. (37) exposes, for each term, an unnormalized inside– local–outside contribution to Z(x):hairpin closed by (i, j ) : W bi,j ϕ1(i, j ),

interior loop / stack (i, j ) → (k, ℓ ) : W bi,j Zbk,ℓ ϕ2(i, j, k, ℓ ),

multiloop closure at (i, j ) with split k : W bi,j Zmi+1 ,k −1 Zm1 

> k,j −1

ϕ3(i, j ). (38) Each line is an unnormalized contribution to the partition function, of the outside–local– inside form. Dividing any one of them by Z(x) gives the posterior expected occurrence of the corresponding local event; the base-pair posterior [Eq. (34) ] is the analogous normalization for the paired item Zbi,j . These are individual local-decomposition contributions; they are not, by themselves, a complete mutation formula. Replacing a single exposed factor in one contribution is exact for that factor alone. For the interior/stacking term, replacing ϕ2 by its mutant value ϕ∗ 

> 2

changes Z by 

W bi,j Zbk,ℓ 

> [

ϕ∗

> 2

(i, j, k, ℓ ) − ϕ2(i, j, k, ℓ )]

.

This expression is exact when only that exposed factor is replaced, but a nucleotide substitution generally changes several primitive factors and boundary contexts at once. These must be replaced jointly, within the nonoverlapping recombination cases developed next, as in the Rchange construction, rather than by independent single-factor differences. 

## 4.3 Context-dependent finite recombination in Rchange 

For realistic nearest-neighbor energies, the ordinary inside and outside quantities are not always reusable after a mutation, because the energy of a loop or a dangling end may depend on nucleotides immediately outside an interval. Replacing xp can therefore change 17 the value of a dynamic-programming cell even when p is a boundary neighbor rather than a position contained in that cell. The Rchange algorithm (Kiryu and Asai, 2012) resolves this dependence by augmenting the inside and outside items with boundary nucleotide contexts. Collecting the McCaskill states into one family, write 

Zτu

> (

κin )

, W τu

> (

κout )

, τ ∈ { ext , 1, b, m, m 1}, (39) where u = ( i, j ) is an interval, τ is the state type (the external assembly ext , a single leading component 1, the paired state b, and the multiloop states m and m1), and the two context slots have different meanings: κin records the bases immediately external to an inside interval, while κout records the boundary bases carried by an outside item. The paired quantities Zbi,j and W bi,j of Section 4.2 are the principal, context-free example ( τ = b, empty context). The paired state alone does not suffice: a mutation can alter mutation-dependent unpaired-segment factors and boundary contexts that enter the multiloop assembly through the states m and m1, so those states also carry recombination cases—even though the index-free multiloop branch factor is not itself changed by the mutation. Let ΓR 

> p

denote the finite set of recombination cases constructed by the Rchange algorithm for a mutation at p, and let κγ (c) be the context induced in case γ when symbol 

c is placed at p. Let Φγ (c; κ) be the joint product of all mutation-dependent primitive nearest-neighbor factors exposed in case γ (stacking, dangling-end, terminal-mismatch, loop, and mutation-dependent multiloop terms); these factors are excluded from the inside quantities Zτv 

> v

and the outside quantity W τγ 

> uγ

before insertion, and are replaced jointly. The Rchange construction then assembles 

Rctx  

> p

(c) = ∑

> γ∈ΓR
> p

W τγ

> uγ
> (

κout  

> γ

(c))

Φγ (c; κγ (c)) ∏

> v∈ch( γ)

Zτv

> v
> (

κin 

> γ,v

(c))

, (40) In this Rchange construction, c = c∗ and c = xp return Z(xp→c∗

) and Z(x). Here κγ (c)

is the complete local context induced when c is placed at p; κout  

> γ

(c) is the outside-state context of the parent item and κin 

> γ,v

(c) the context of child v, each an appropriate projection of κγ (c) onto its slot, with the in/out slot semantics of Eq. (39) retained; τv is the state 18 type of child v; and W τγ 

> uγ

(κout  

> γ

(c)) is the corresponding context-indexed outside adjoint. Equation (40) is schematic: it records the form of each recombination case, while the enumeration of the cases ΓR 

> p

, the state types τγ , and the context slots is fixed by the Rchange algorithm. Its exactness for the neighbor-dependent energies is the exactness of that algorithmic construction (Kiryu and Asai, 2012); we do not claim here an independent exactly-once decomposition of all admissible structures for the full Turner model. Because only finitely many boundary neighborhoods occur, the context-indexed copies are a finite enlargement of the state space: unaffected subproblems are reused while every affected local factor is evaluated in the correct mutant neighborhood. The context conventions and the corresponding finite-recombination formulas are given in Supplementary Section S4. 

## 4.4 RNA-like SCFG specialization and relation to Rchange 

The recombination becomes fully explicit in a simplified specialization: an RNA-like SCFG of the type used to model RNA secondary structure (Sakakibara et al., 1994; Eddy and Durbin, 1994), which makes the Rchange-style recombination explicit. Fix a position p.In every derivation tree the base at p is emitted exactly once, in one of four local roles: the left or the right base of a pair, or a left or right unpaired base. This role is a function of the tree, so the roles—refined by the partner or cut position—partition the derivation trees; let Γp index the resulting blocks. Each block γ ∈ Γp is one inside–local–outside decomposition: an outside context (adjoint) Buγ , a local emission factor ϕγ (c) carrying the symbol c placed at p, and inside child values Av.Summing the corresponding local terms over the blocks defines 

Rp(c) = ∑

> γ∈Γp

Buγ ϕγ (c) ∏

> v∈ch( γ)

Av, (41) which is exactly the one-site sum of Section 2.3 with the single factor group replaced by the partition Γp: each summand is a local term BuTr as in Eq. (10) , and the mutated symbol c enters only through ϕγ . Because the blocks partition the trees, every global tree is counted once, so inserting the mutant and the original symbol yields the mutant and 19 original partition functions, 

Z(xp→c∗

) = Rp(c∗), Z(x) = Rp(xp), (42) and the exact finite difference 

∆pZ = Rp(c∗) − R p(xp). (43) Two observations make the exactness transparent. First, at c = xp each normalized block is a local-event posterior [Eq. (10) ]; since the base at p has exactly one role per tree, these posteriors sum to one, 

> ∑
> γ∈Γp

Buγ ϕγ (xp) ∏ 

> v

Av

Z = ∑

> γ∈Γp

E[Nγ | x] = 1 , (44) so Rp(xp) = Z, as required. Second, when the sequence-dependent blocks of Γp are indexed by the coordinates of a single position-specific factor group, Eq. (43) reduces to the multi-affine one-site formula, Eq. (13) , recovering the homogeneous HMM and ordinary-SCFG cases. In affine-gap alignment the match blocks correspond to the coordinates ρMi,j , while paths that align the residue to a gap form a sequence-independent block that supplies the constant term. Nearest-neighbor RNA differs because a mutation changes several overlapping, context-dependent factors within the recombination blocks rather than the coordinates of one affine group. The explicit four-term RNA-like specialization, summed over partner positions, is given in the Supplementary Material. This simplified RNA-like SCFG makes the Rchange-style recombination explicit and is the model used in the numerical verification of Section 5 and in the RNA rows of Table 2; it is not a full Turner nearest-neighbor implementation. Accordingly, the numerical experiments verify only this RNA-like SCFG specialization of the one- and two-site finite-replacement identities [Eqs. (43) and (46) ], not the general context-dependent construction of Eq. (40). 20 4.5 Multiple mutations and computational cost 

For two mutations, the mixed finite difference remains the natural measure of interaction: 

∆p∆qZ = Z(xp→c∗,q →d∗

) − Z(xp→c∗

)

− Z(xq→d∗

) + Z(x). (45) Unlike the simple multi-affine examples, this interaction can arise not only because two position groups are used in the same latent object, but also because one mutation changes the local thermodynamic context in which the other is evaluated. The mixed term is best read as the change in the one-site effect of p when that effect is measured on the q-mutant background rather than on the original sequence: 

∆p∆qZ = {

Rctx  

> p,x q→d∗

(c∗) − R ctx  

> p,x q→d∗

(xp)}

− {

Rctx 

> p,x

(c∗) − R ctx 

> p,x

(xp)}

.

The first brace is the effect of mutating p on the q-mutant background xq→d∗

; the second is the effect of mutating p on the original background x; their difference is the finite interaction. In particular, if mutating p has the same effect on both backgrounds the two braces coincide and ∆p∆qZ = 0 , so the change is additive; the additive residual reported in Section 5 measures precisely this departure from additivity. The sign of the mixed term compares the signed one-site effects: it is positive when the effect of mutating p is more positive, or less negative, on the q-mutant background than on the original sequence, and negative in the opposite case. Its magnitude measures how much the q-substitution reshapes the context seen by p.Each endpoint of the first brace is obtained by the one-site context recombination of Section 4.3 applied to the single-mutant background x′ = xq→d∗

, so that the double mutant is 

Z(xp→c∗,q →d∗

) = Rctx  

> p,x ′

(c∗), (46) computed by recombining position p against inside and outside quantities appropriate to 

x′. The p-then-q and q-then-p routes are two computational decompositions of the same 

21 endpoint partition function Z(xp→c∗,q →d∗

); any discrepancy between them is therefore numerical round-off, not a theoretical asymmetry. Efficient implementations reuse dynamic-programming cells unaffected by the first mutation instead of recomputing the complete ensemble. The total change still decomposes as 

∆p,q Z = ∆ pZ + ∆ qZ + ∆ p∆qZ, (47) but the mixed component is computed by exact context-aware recombination, not by assuming two independent single-site sensitivities. As in the one-site case, the numerical verification of Section 5 exercises only the RNA-like SCFG specialization of Eq. (46) , not a full Turner-model implementation. For the full Rchange energy-model implementation, if L is the sequence length and W

is the maximum base-pair span, exhaustive single-mutant analysis requires O(LW 2) time and unrestricted exhaustive double-mutant analysis O(L2W 2); restricting the maximum separation of the two mutated positions to D reduces the latter to O(LW 2D) (Kiryu and Asai, 2012). This cost reflects evaluating many single-mutant-background recombinations— one context-dependent recombination per background—rather than forming independent products of one-site sensitivities; the gain comes from reusing context-dependent inside and outside quantities, while exhaustive interaction scans remain intrinsically more costly than one-site scans. These finite recombinations do not displace the differential viewpoint but complete it. Outside quantities remain the adjoints that expose local-term coefficients, and logarithmic derivatives with respect to energy factors give expected structural features and first-order rankings of candidate mutations; the exact effect of a substitution additionally inserts the mutant factors, includes their joint products, and evaluates all affected boundary contexts. The Rchange algorithm can thus be read as the finite-difference side of the same adjoint framework: inside and outside quantities identify reusable structural contexts, while finite replacement of the sequence-dependent thermodynamic factors produces the exact mutant partition function. 22 5 Numerical Verification 

To confirm that the finite-replacement identities are exact rather than first-order approxima-tions, we implemented small instances of four models and compared the partition-function change predicted by the adjoint formulas with the change obtained by rebuilding the mutant sequence and rerunning the dynamic program. All model parameters are fixed, so the results are deterministic and reproducible; the complete script is provided as Supplementary Code. We used a three-state HMM on a length-eight sequence over a four-letter alphabet, an affine-gap alignment ensemble on two short sequences with gaps (lengths four and three), an ordinary Chomsky-normal-form SCFG with three nonterminals, and the five-rule RNA-like SCFG of Kiryu and Asai (2012) on a length-six sequence. The alignment case exercises the affine but non-homogeneous group of Section 3.1, in which a gapped path uses no match factor and contributes a constant term. For every position and every substitution we evaluated the one-site formulas [Eqs. (16) , (31) , (18) , (43) ]. For the HMM and the RNA-like SCFG we additionally evaluated, for every pair of positions p < q 

and every pair of replacement symbols, the HMM two-site expansion [Eq. (19) ] and the RNA-like specialization of Eq. (46) . For the RNA-like grammar we also checked the self-consistency identity Rp(xp) = Z(x), which held with zero residual, and evaluated both application orders of the background construction [Eq. (46) ], which agreed up to round-off, verifying its order independence. This experiment verifies the role-partition recombination of Section 4.4 [Eqs. (41) –(43) ]; it is not intended as a revalidation of a complete nearest-neighbor Rchange implementation with dangling-end, terminal-mismatch, and multiloop boundary-context energies, whose context-indexed recombination [Eq. (40) ]is described in Section 4.3 but not exercised here. Table 2 reports the maximum relative residual max | formula − brute force |/Z (x) over all tested mutations. In every case the residual is at the level of double-precision round-off (on the order of 10 −15 ). The identities themselves are proved analytically in Sections 2.3 and 4; these experiments are not proofs but confirm the implementation and the explicit formulas and illustrate the gap from the differential approximations below. Repeating 23 each check over 50 random parameter sets per model (fixed seeds; see the script) left every residual below 2 × 10 −14 .Table 2: Numerical verification of the exact finite-replacement identities. For each model and identity we report the maximum relative residual max | adjoint formula −

brute-force recomputation |/Z (x) over all positions and substitutions. The tabulated resid-uals are from a single fixed small instance per model (reproducible script, Supplementary Code); across 50 additional random parameter sets per model, every residual remained below 2 × 10 −14 . The additive and log-linearization approximation errors ( radd , rlog ) are defined and reported in the text.                           

> Model Verified identity Max. relative residual
> HMM, one-site Eq. (16) 3×10 −16
> HMM, two-site (mixed) Eq. (19) 2×10 −15
> Affine-gap alignment, one-site Eq. (31) 4×10 −16
> SCFG (CNF), one-site Eq. (18) 2×10 −16
> RNA-like SCFG, one-site Eq. (43) 8×10 −16
> RNA-like SCFG, two-site Eq. (46) 2×10 −15

The same experiment quantifies what a differential shortcut discards. Approximating a two-site change by the sum of two independent single-site effects discards the mixed term, giving the normalized residual 

radd =

> ∣∣∣

∆p,q Z − (∆ pZ + ∆ qZ)∣∣∣

Z(x) =

> ∣∣∣

∆p∆qZ∣∣∣

Z(x) , (48) the normalized magnitude of the omitted mixed term, which reached 0.96 over the tested position pairs. This is a residual relative to Z(x), not a relative error against the true change |∆p,q Z|. Likewise, the exact one-site change in log Z departs from its first-order value ∆pZ/Z (x) by 

rlog = 

> ∣∣∣∣∣

∆p log Z − ∆pZZ(x) 

> ∣∣∣∣∣

, (49) which reached 0.58 (in nats), consistent with the nonlinearity in Eq. (14) . Both quantities use the original partition function Z(x) as the reference. The finite identities are therefore numerically exact where the theory predicts, whereas the naive additive and first-order approximations are not. 24 Table 3: Computational implications after one forward/inside and one backward/outside computation. The per-candidate costs refer to evaluating a single substitution candidate from stored dynamic-programming quantities; they do not include the initial dynamic-programming pass. Here Q = |Q| , N = |N | , and R2 is the set of binary SCFG productions.            

> Model Base forward/inside– outside cost One substitution candidate after preprocessing All one-site candidates
> HMM O(LQ 2)O(Q)O(L|Σ|Q)
> Affine-gap alignment, substitutions in sequence xO(LK )O(K)O(LK |Σ|)
> Chomsky-normal-form SCFG
> O(L3|R2|)O(N)O(L|Σ|N)
> Nearest-neighbor RNA model- and implementation-dependent context-dependent recombination no universal local formula

# 6 Computational Implications 

After one forward/inside and backward/outside pass, a candidate substitution is evaluated by contracting stored coefficients with changed local factors, rather than rerunning the global dynamic program. Table 3 summarizes representative costs. An HMM candidate costs O(Q) after an O(LQ 2) pass; an alignment substitution in x costs O(K) after O(LK )

preprocessing because all partners in y must be considered; and an ordinary SCFG candidate costs O(N ) after the cubic inside–outside computation. Thus an all-substitution scan replaces repeated global runs by one global pass followed by local contractions. This does not reduce the base DP complexity or make arbitrary multisite changes inexpensive: the number of mixed terms can grow exponentially, and RNA requires context-dependent recombination. 

# 7 Discussion 

## 7.1 Relation to established views 

The adjoint interpretation of backward and outside quantities is established in semiring dynamic programming, factor-graph message passing, network polynomials, and reverse-mode automatic differentiation (Goodman, 1999; Kschischang et al., 2001; Darwiche, 25 2003; Griewank and Walther, 2008; Baydin et al., 2018; Eisner, 2016). Differentiable dynamic programming has also been studied as a trainable structured layer (Mensch and Blondel, 2018), and discriminative posterior models such as CONTRAfold exploit the same inside–outside expectations (Do et al., 2006). The present contribution is to organize these ideas for biological sequence models and to delimit when local derivatives reconstruct exact discrete sequence changes. Two distinctions are central. First, AuBu/Z is the posterior use of an item, whereas a specific local rule has posterior Buϕr 

> ∏
> v

Av/Z . Second, an infinitesimal derivative is not generally a finite mutation effect. Proposition 1 shows that a one-site replacement is exact under multi-affinity, while multisite replacements require mixed terms. Nearest-neighbor RNA models mark the boundary of this simple position-wise factorization because one nucleotide changes overlapping factors and boundary contexts; the Rchange algorithm therefore uses context-dependent finite recombination (Kiryu and Asai, 2012), in contrast to tools that quantify mutation-induced structural change by other means (Waldispühl et al., 2008; Halvorsen et al., 2010; Sabarinathan et al., 2013; Churkin and Barash, 2006). The reverse-mode interpretation developed here also points to a related optimization-based extension. In max-plus or min-plus dynamic programs, such as Viterbi alignment and minimum-free-energy RNA folding, the reverse computation propagates through optimizing branches and is closely related to traceback. When the optimum is unique, the selected optimal derivation can be viewed as a gradient of the optimal value with respect to local scores, yielding optimal feature counts; when optima are tied, this interpretation requires generalized derivatives or a set-valued subgradient. A full treatment of this connection, including its relation to semiring dynamic programming and structured prediction, is beyond the scope of the present work. 

## 7.2 Implications for inference and design 

The same dynamic program supports distinct queries: posterior marginals, expected feature counts, infinitesimal parameter sensitivities, and exact finite sequence changes. Keeping these objects separate avoids a common ambiguity in gradient-based sequence 26 analysis. Gradients can rank candidate changes or support continuous optimization, but they equal a discrete edit only in the special multi-affine setting. For several sites, mixed terms measure finite-site interaction; for nearest-neighbor RNA, exact evaluation also requires mutant boundary contexts. In RNA analysis, posterior base-pairing probabilities and their parameter derivatives already underlie applications such as the interpretation of structural-probing data (Yamamura et al., 2025a) and the estimation of thermodynamic parameters (Yamamura et al., 2025b). Normalized HMM and SCFG probabilities and unnormalized alignment and RNA Boltzmann weights share the same factor-level calculus. Probability-simplex constraints or energy parameterizations enter only through an additional chain rule. Automatic differentiation can generate the adjoints, whereas explicit reverse recursions reveal their biological meaning and enable model-specific reuse and recombination. 

## 7.3 Relation to nonlinear sequence predictors 

For a continuously differentiable predictor F on a chosen continuous extension of sequence space, 

F (x′) − F (x) =  

> ∫10

∇F (x + t(x′ − x)) ⊤ (x′ − x) dt. (50) The gradient at x is only the tangent at t = 0 and does not in general determine the finite difference F (x′) − F (x). In the multi-affine one-site cases with F = Z the integrand is constant along the replacement segment, so the path integral reduces to Eq. (13) ;multi-affinity guarantees this for every replacement within the factor group. The collapse concerns Z itself—the induced change in log Z stays nonlinear [Eq. (14) ]. General nonlinear sequence predictors do not in general satisfy this condition, so their endpoint gradients may rank or guide candidate edits but need not equal a finite substitution effect. The algebraic parallel between the forward–backward recursion and reverse-mode backpropagation is recorded in Supplementary Section S3. 27 7.4 Scope and limitations 

The analysis assumes a finite acyclic sum–product computation with local factorization. Adjoint identities extend algebraically beyond nonnegative weights, but posterior inter-pretations require nonnegative object weights and Z > 0. Ratio and logarithmic forms require care at zero factors. The exactness of the finite-replacement identities is also confined to Z and its linear functionals: derived quantities such as log Z, posterior ratios like base-pairing probabilities, and arg max structures remain nonlinear in a substitution even in the multi-affine case. Multi-affinity depends on the chosen factorization. It can fail when one position affects several factors used simultaneously, when scores contain nonlocal couplings, or when factors are tied nonlinearly. Occurrence-specific factors may restore polynomial structure, yet a biological mutation can still coordinate many changes, as in RNA thermodynamics. Efficient multisite algorithms must exploit model-specific transfer matrices, sparsity, checkpointing, or recombination. Pseudoknots, tertiary interactions, cyclic grammars, insertions, deletions, and black-box potentials may require enlarged state spaces or different formulations. 

## 7.5 Conclusion 

Backward and outside quantities in major biological sequence dynamic programs are adjoints of forward or inside quantities. This common structure explains item marginals, local-event posteriors, and expected feature counts. The finite-replacement analysis further specifies when derivative coefficients give exact mutation effects, when mixed terms are required, and why nearest-neighbor RNA models demand context-dependent recombination. The framework thereby separates posterior inference, parameter sensitivity, and finite sequence change while deriving all three from the same dynamic-programming quantities. 28 Acknowledgements 

The author thanks Goro Terai and Takumi Otagaki for constructive discussions and the participants of the RNA Informatics Dojo 2025 in Nagasaki, Japan, for fruitful discussions. 

# Funding 

This work was supported by Japan Society for the Promotion of Science (JSPS) KAKENHI grant numbers JP24H00737 and JP22H04925 (PAGS), and Japan Science and Technology Agency (JST) CREST grant number JPMJCR23N1. 

# Declaration of conflicting interests 

The author declares no potential conflicts of interest with respect to the research, authorship, and/or publication of this article. 

# Data and Software Availability 

No new biological datasets were generated or analyzed in this theoretical study. The only software is a short, self-contained verification script ( verify_numerics.py ), provided with the supplementary material, which reproduces the machine-precision residuals reported in the numerical verification section. 

# Generative AI disclosure 

OpenAI ChatGPT and Anthropic Claude/Claude Code were used to assist with scientific editing, structural reorganization, drafting and revision of English and LaTeX exposition, consistency checking, mathematical review, file-level integration, and compilation. They did not determine the research question or the final scientific conclusions. The author independently examined all derivations, numerical results, citations, and wording and takes full responsibility for the work. 29 References 

James K. Baker. Trainable grammars for speech recognition. In Dennis H. Klatt and Jared J. Wolf, editors, Speech Communication Papers for the 97th Meeting of the Acoustical Society of America , pages 547–550, 1979. Atilim Gunes Baydin, Barak A. Pearlmutter, Alexey Andreyevich Radul, and Jeffrey Mark Siskind. Automatic differentiation in machine learning: A survey. Journal of Machine Learning Research , 18(153):1–43, 2018. Philipp Bucher and Kay Hofmann. A sequence similarity search algorithm based on a probabilistic interpretation of an alignment scoring system. In Proceedings of the Fourth International Conference on Intelligent Systems for Molecular Biology (ISMB) , pages 44–51, 1996. Alexander Churkin and Danny Barash. RNAmute: RNA secondary structure mutation analysis tool. BMC Bioinformatics , 7:221, 2006. doi: 10.1186/1471-2105-7-221. Adnan Darwiche. A differential approach to inference in Bayesian networks. Journal of the ACM , 50(3):280–305, 2003. doi: 10.1145/765568.765570. Chuong B. Do, Daniel A. Woods, and Serafim Batzoglou. CONTRAfold: RNA secondary structure prediction without physics-based models. Bioinformatics , 22(14):e90–e98, 2006. doi: 10.1093/bioinformatics/btl246. Richard Durbin, Sean R. Eddy, Anders Krogh, and Graeme Mitchison. Biological Sequence Analysis: Probabilistic Models of Proteins and Nucleic Acids . Cambridge University Press, Cambridge, 1998. Sean R. Eddy and Richard Durbin. RNA sequence analysis using covariance models. 

Nucleic Acids Research , 22(11):2079–2088, 1994. doi: 10.1093/nar/22.11.2079. Jason Eisner. Inside-Outside and Forward-Backward algorithms are just Backprop. Pro-ceedings of the Workshop on Structured Prediction for NLP , W16(5901):1–17, 2016. 30 Joshua Goodman. Semiring parsing. Computational Linguistics , 25(4):573 – 605, 1999. Osamu Gotoh. An improved algorithm for matching biological sequences. Journal of Molecular Biology , 162(3):705 – 708, 1982. Andreas Griewank and Andrea Walther. Evaluating Derivatives: Principles and Tech-niques of Algorithmic Differentiation . Society for Industrial and Applied Mathematics, Philadelphia, 2008. Matthew Halvorsen, Joshua S. Martin, Sam Broadaway, and Alain Laederach. Disease-associated mutations that alter the RNA structural ensemble. PLoS Genetics , 6(8): e1001074, 2010. doi: 10.1371/journal.pgen.1001074. Taishin Kin, Koji Tsuda, and Kiyoshi Asai. Marginalized kernels for RNA sequence data analysis. Genome Informatics , 13:112–122, 2002. doi: 10.11234/gi1990.13.112. Hisanori Kiryu and Kiyoshi Asai. Rchange: algorithms for computing energy changes of RNA secondary structures in response to base mutations. Bioinformatics , 28(8):1093 – 1101, 2012. Frank R. Kschischang, Brendan J. Frey, and Hans-Andrea Loeliger. Factor graphs and the sum-product algorithm. IEEE Transactions on Information Theory , 47(2):498–519, 2001. doi: 10.1109/18.910572. K. Lari and S. J. Young. The estimation of stochastic context-free grammars using the Inside-Outside algorithm. Computer Speech & Language , 4(1):35–56, 1990. doi: 10.1016/0885-2308(90)90022-X. John S McCaskill. The equilibrium partition function and base pair binding probabilities for RNA secondary structure. Biopolymers , 29(6-7):1105–1119, 1990. Arthur Mensch and Mathieu Blondel. Differentiable dynamic programming for structured prediction and attention. In Proceedings of the 35th International Conference on Machine Learning , volume 80 of Proceedings of Machine Learning Research , pages 3462–3471. PMLR, 2018. 31 Sanzo Miyazawa. A reliable sequence alignment method based on probabilities of residue correspondences. Protein Engineering , 8(10):999 – 1009, 1995. Lawrence R. Rabiner. A tutorial on Hidden Markov Models and selected applications in speech recognition. Proceedings of the IEEE , 77(2):257–286, 1989. doi: 10.1109/5.18626. Radhakrishnan Sabarinathan, Hakim Tafer, Stefan E. Seemann, Ivo L. Hofacker, Peter F. Stadler, and Jan Gorodkin. RNAsnp: Efficient detection of local RNA secondary structure changes induced by SNPs. Human Mutation , 34(4):546–556, 2013. doi: 10.1002/humu.22273. Yasubumi Sakakibara, Michael Brown, Richard Hughey, I. Saira Mian, Kimmen Sjölander, Rebecca C. Underwood, and David Haussler. Stochastic context-free grammars for tRNA modeling. Nucleic Acids Research , 22(23):5112–5120, 1994. doi: 10.1093/nar/22.23.5112. Koji Tsuda, Taishin Kin, and Kiyoshi Asai. Marginalized kernels for biological sequences. 

Bioinformatics , 18(Suppl 1):S268–S275, 2002. Martin J. Wainwright and Michael I. Jordan. Graphical models, exponential families, and variational inference. Foundations and Trends in Machine Learning , 1(1–2):1–305, 2008. doi: 10.1561/2200000001. Jérôme Waldispühl, Srinivas Devadas, Bonnie Berger, and Peter Clote. Efficient algorithms for probing the RNA mutation landscape. PLoS Computational Biology , 4(8):e1000124, 2008. doi: 10.1371/journal.pcbi.1000124. Kazuteru Yamamura, Kiyoshi Asai, and Junichi Iwakiri. Consistent features observed in structural probing data of eukaryotic RNAs. NAR Genomics and Bioinformatics , 7(1): lqaf001, 2025a. doi: 10.1093/nargab/lqaf001. Kazuteru Yamamura, Goro Terai, and Kiyoshi Asai. A method for estimating energy parameters of RNAs by differentiating base-pairing probabilities. NAR Genomics and Bioinformatics , 7(4):lqaf171, 2025b. 32 Supplementary Material for A Differential Framework for Dynamic Programming in Biological Sequence Analysis 

## Kiyoshi Asai 

# Contents and relation to the main manuscript 

This Supplementary Material contains detailed derivations supporting the compact for-mulations in the main manuscript. Section S1 expands the HMM and SCFG results developed in the general framework of Section 2 of the main manuscript. Section S2 supplies the complete affine-gap alignment derivations for Section 3.1. Section S3 explains the reverse-mode relation discussed in the neural-predictor part of the Discussion. Section S4 supports the RNA ensemble and context-dependent recombination results in Sections 3.2 and 4, including outside recursions for a simplified McCaskill-type decomposition and Rchange finite-difference formulas. Section S5 records the exponential-family and information-geometric structure underlying the RNA feature derivatives. No biological datasets are analyzed; the only computational material is the short, self-contained script 

verify_numerics.py (Supplementary Code), which reproduces the machine-precision residuals of the numerical verification section of the main manuscript. This note supplies detailed forward–backward and inside–outside derivations, expected-count identities, and explicit one- and two-site replacement formulas that are summarized in the main text. Notation follows the main manuscript. 1S1 Detailed HMM and SCFG Derivations 

## S1.1 Forward–backward and inside–outside variables as adjoints 

We first consider hidden Markov models (HMMs). Let Q be the set of hidden states and 

Σ the set of output symbols. Let ai,j be the transition probability from state i ∈ Q to state j ∈ Q , and let ej (c) be the emission probability of symbol c ∈ Σ from state j. Let 

πs be the initial probability of state s; termination factors, when present, are absorbed into the boundary conditions. For an observed sequence 

x = x1 · · · xL,

let fj (t) be the forward probability that the prefix x1 · · · xt has been generated and that the hidden state at position t is j. We use the standard start convention, in which the initial probabilities enter the first step and ordinary transitions run from t = 2 :

fs(1) = πses(x1), fj (t) = ej (xt) ∑

> i∈Q

fi(t − 1) ai,j (t = 2 , . . . , L ).

The total probability of the sequence is 

P (x) = ∑

> π

P (x, π ),

where the summation is over all hidden state paths π. Although P (x) is a normalized probability, it is also a sum of nonnegative weights over latent paths, and therefore plays the role of a partition function. In this section we write 

Z ≡ P (x)

when emphasizing the partition-function viewpoint. For any position t, the total probability can be decomposed as 

P (x) = ∑

> i∈Q

fi(t)bi(t),

2where bi(t) is the total contribution of all suffix paths and emissions from positions 

t + 1 , . . . , L , conditional on being in state i at position t. Under the common convention 

P (x) = ∑ 

> i

fi(L), the terminal condition is bi(L) = 1 . Initial probabilities are encoded in the boundary values entering the first forward step; alternative explicit start or end states change only these boundary factors. In the computation graph defined by the forward recursion, this backward quantity is the adjoint of the forward variable: 

bi(t) = ∂P (x)

∂f i(t) .

Indeed, differentiating the next-step forward recursion gives 

bi(t) = ∂P (x)

∂f i(t)= ∑

> j∈Q

∂P (x)

∂f j (t + 1) 

∂f j (t + 1) 

∂f i(t)= ∑

> j∈Q

bj (t + 1) ai,j ej (xt+1 ).

This is precisely the usual HMM backward recursion, with the appropriate terminal conditions. Thus, the backward algorithm is reverse-mode differentiation of the forward dynamic program. The same differential viewpoint applies to stochastic context-free grammars (SCFGs). Consider a Chomsky-normal-form SCFG with nonterminal set N , terminal alphabet Σ,start symbol S, binary production probabilities tv→yz , and terminal emission probabilities 

ev(c). Here v, y, z ∈ N and c ∈ Σ.Let α(i, j, v ) be the inside probability that the substring xi · · · xj is generated from nonterminal v. The inside base case and recursion are 

α(i, i, v ) = ev(xi),

and, for i < j ,

α(i, j, v ) = 

> j−1∑
> k=i
> ∑
> y,z ∈N

tv→yz α(i, k, y )α(k + 1 , j, z ).

3The SCFG partition function is 

P (x) = Z = α(1 , L, S ).

Unlike the HMM case, an arbitrary interval [i, j ] does not necessarily occur as a constituent in every derivation tree. Therefore, rather than decomposing P (x) by cutting the sequence at a fixed position, we define the outside quantity directly as the adjoint of the inside variable: 

β(i, j, v ) = ∂P (x)

∂α (i, j, v ).

The outside recursion is obtained by differentiating all inside recursion terms in which 

α(i, j, v ) appears as a child inside variable. There are two cases: α(i, j, v ) may appear as the left child of a larger constituent, or as the right child. Thus, 

β(i, j, v ) = 

> L∑
> k=j+1
> ∑
> u,w ∈N

β(i, k, u )tu→vw α(j + 1 , k, w )+

> i−1∑
> k=1
> ∑
> u,w ∈N

β(k, j, u )tu→wv α(k, i − 1, w ).

The boundary condition is β(1 , L, S ) = 1 , with other outside quantities initialized to zero and accumulated according to the reverse dependencies of the inside computation. Thus, SCFG outside variables are adjoints of inside variables in the same sense that HMM backward variables are adjoints of forward variables. In both cases, the usual backward or outside dynamic program is the reverse-mode differentiation of the corresponding forward or inside computation graph. 

## S1.2 Expected counts as logarithmic derivatives 

Expected transition and emission counts in HMMs, and expected production-rule counts in SCFGs, are used in the parameter re-estimation steps of EM algorithms. They can also serve as feature vectors in marginalized kernels (Tsuda et al., 2002), including marginalized count kernels for RNA sequence analysis (Kin et al., 2002). The adjoint interpretation also explains why expected feature counts are obtained as derivatives of the log-partition 4function with respect to local factors. We first describe this for HMMs. For an HMM path π, let N (T ) 

> r,s

(π) be the number of transitions from state r to state 

s, and let N (E) 

> s,c

(π) be the number of emissions of symbol c from state s. The posterior probability of a hidden state path is 

P (π | x) = P (x, π )

P (x)= 1

P (x)

> ∏
> r,s ∈Q

aN (T ) 

> r,s (π)
> r,s
> ∏
> s∈Q
> ∏
> c∈Σ

es(c)N (E) 

> s,c (π)

.

Equivalently, 

P (π | x) = exp 

> ∑
> r,s ∈Q

N (T ) 

> r,s

(π) log ar,s 

+ ∑

> s∈Q
> ∑
> c∈Σ

N (E) 

> s,c

(π) log es(c) − log P (x)

> 

.

Thus, the posterior distribution over hidden paths has the form of an exponential-family distribution whose natural parameters are the logarithms of the local factors. Differen-tiating the log-partition function with respect to these logarithmic parameters gives the corresponding posterior expected counts: 

∂ log P (x)

∂ log ar,s 

= E[

N (T ) 

> r,s

| x]

,

and 

∂ log P (x)

∂ log es(c) = E[

N (E) 

> s,c

| x]

.

These identities can be written explicitly in terms of the forward and backward variables. For transitions, 

E[

N (T ) 

> r,s

| x]

= 1

P (x)

> L∑
> t=2

fr(t − 1) ar,s es(xt)bs(t).

Consistent with the start convention fs(1) = πses(x1), the sum runs over t = 2 , . . . , L ,so that only ordinary state-to-state transitions are counted. The initial state is handled 5separately through the initial-probability derivative 

∂ log P (x)

∂ log πs

= P (π1 = s | x).

For emissions, 

E[

N (E) 

> s,c

| x]

= 1

P (x) 

> ∑
> t:xt=c

fs(t)bs(t).

The summands are posterior probabilities of local events. In particular, the posterior probability of state s at position t is 

P (πt = s | x) = fs(t)bs(t)

P (x) , (S1.1) and the posterior probability that the transition r → s is used at position t is 

P (πt−1 = r, π t = s | x) = fr(t − 1) ar,s es(xt)bs(t)

P (x) .

Thus, posterior probabilities in HMMs are normalized forward–local–backward products. The same argument applies to SCFGs. All probabilities and expectations in this paragraph are over derivation trees. If the grammar is ambiguous, distinct derivations of the same biological structure remain distinct latent objects; identifying this distribution with one over structures requires an unambiguous representation. For a derivation tree 

π, let N (T ) 

> v→yz

(π) be the number of uses of the binary production v → yz , and let N (E)

> v→c

(π)

be the number of uses of the terminal production v → c. The posterior probability of a derivation tree is 

P (π | x) = 1

P (x)

> ∏
> v,y,z ∈N

tN (T ) 

> v→yz (π)
> v→yz
> ∏
> v∈N
> ∏
> c∈Σ

ev(c)N (E)

> v→c(π)

.

Therefore, 

∂ log P (x)

∂ log tv→yz 

= E[

N (T ) 

> v→yz

| x]

,

and 

∂ log P (x)

∂ log ev(c) = E[

N (E) 

> v→c

| x]

.

6For both HMMs and SCFGs, these derivatives treat the local probabilities as unconstrained positive factors. Derivatives with respect to a normalized simplex parameterization—in which each row of transition, emission, or production probabilities sums to one—are obtained by the chain rule. Using inside and outside quantities, these expectations become 

E[

N (T ) 

> v→yz

| x]

= 1

P (x)

> ∑
> 1≤i≤k<j ≤L

β(i, j, v )tv→yz 

× α(i, k, y )α(k + 1 , j, z ),

and 

E[

N (E) 

> v→c

| x]

= ev(c)

P (x) 

> ∑
> i:xi=c

β(i, i, v ).

The posterior probability that interval [i, j ] is derived from nonterminal v is 

P ([ i, j ], v | x) = α(i, j, v )β(i, j, v )

P (x) . (S1.2) This is the SCFG analogue of posterior state probabilities in HMMs. Together, these formulas show a common pattern: the posterior probability of a latent local component is obtained by multiplying its inside or forward contribution by its outside or backward contribution and normalizing by the total partition function. The same pattern will reappear in affine-gap alignment ensembles and RNA base-pairing probabilities. 

## S1.3 Exact finite sequence perturbations 

The derivative formulas above describe sensitivities with respect to model parameters or local factors. A sequence mutation, however, is not an infinitesimal parameter change. It is a finite replacement of the local factors associated with one or more sequence positions. The same forward–backward or inside–outside quantities can nevertheless be used to express such finite replacements exactly. The algebraic reason is multi-affinity with respect to position-specific factor groups. In the HMM and ordinary SCFG cases below, every path or tree uses exactly one factor from each position group, so the polynomial is also homogeneous of degree one in each 7position group. For a single affected position, the dependence on the entire group is linear. For multiple affected positions, exactness is maintained by including the corresponding mixed terms. We first illustrate this principle for HMMs and then for SCFGs. 

S1.3.1 Perturbations in HMMs 

Consider a single mutation at position p,

xp −→ c∗.

Only the emission factors at position p are changed. Introduce the position-specific local emission factor 

ρp,s = es(xp).

Then, with the start convention above, 

∂P (x)

∂ρ p,s 

=

> 

πs bs(1) , p = 1 ,

>  ∑
> r∈Q

fr(p − 1) ar,s  

> 

bs(p), p ≥ 2,

since at p = 1 the emitting factor enters only through fs(1) = πsρ1,s . Define the single-mutation finite difference by 

∆pP = P (xp→c∗

) − P (x).

Then the exact single-mutation difference is 

∆pP = ∑

> s∈Q

∂P (x)

∂ρ p,s 

{es(c∗) − es(xp)} .

Equivalently, for p ≥ 2 (with the coefficient replaced by πsbs(1) at p = 1 ), 

∆pP = ∑

> s∈Q
>  ∑
> r∈Q

fr(p − 1) ar,s  

> 

bs(p) {es(c∗) − es(xp)} .

8This is an exact finite difference, not a first-order approximation. Dividing by P (x), the same identity can be written in terms of posterior state probabilities: 

∆pPP (x) = ∑

> s∈Q

P (πp = s | x) 

> {

es(c∗)

es(xp) − 1

> }

.

This form makes explicit that the finite mutation effect is obtained by weighting finite local-factor ratios by posterior probabilities in the original sequence background. The ratio form assumes that the relevant original emission factors es(xp) are positive. The derivative form in the preceding equation remains valid when an original factor is zero. Now consider two mutations at positions p < q ,

xp → c∗, xq → d∗.

Let Hs,u (p, q ) denote the total weight of all paths from state s at position p to state u at position q, including the transitions and emissions between p and q, but excluding the emissions at positions p and q themselves. Thus, when q = p + 1 , Hs,u (p, q ) = as,u . Define 

Cp,q s,u =

>  ∑
> r∈Q

fr(p − 1) ar,s  

> 

Hs,u (p, q )bu(q).

Then the contribution of paths with state s at position p and state u at position q factorizes as 

Cp,q s,u es(xp)eu(xq)

in the original sequence, and as 

Cp,q s,u es(c∗)eu(d∗)

in the double mutant. Define 

∆ρp,s = es(c∗) − es(xp), ∆ρq,u = eu(d∗) − eu(xq).

9Also define the total double-mutation finite difference by 

∆p,q P = P (xp→c∗,q →d∗

) − P (x).

Then 

∆p,q P = ∑

> s,u ∈Q

Cp,q s,u 

[

∆ρp,s eu(xq)+ es(xp)∆ ρq,u + ∆ ρp,s ∆ρq,u 

]

.

The first two terms correspond to the finite single-mutation effects in the original sequence background. The last term is the finite interaction term between the two mutations. Equivalently, define the mixed finite difference by 

∆p∆qP = P (xp→c∗,q →d∗

) − P (xp→c∗

)

− P (xq→d∗

) + P (x).

Then 

∆p,q P = ∆ pP + ∆ qP + ∆ p∆qP, 

where 

∆p∆qP = ∑

> s,u ∈Q

Cp,q s,u ∆ρp,s ∆ρq,u .

Because P (x) is multi-affine across the position-specific emission groups, the same exact double-mutation effect can be written as the following finite polynomial identity: 

∆p,q P = ∑

> s∈Q

∂P ∂ρ p,s 

∆ρp,s 

+ ∑

> u∈Q

∂P ∂ρ q,u 

∆ρq,u 

+ ∑

> s,u ∈Q

∂2P∂ρ p,s ∂ρ q,u 

∆ρp,s ∆ρq,u ,

where 

∂2P∂ρ p,s ∂ρ q,u 

= Cp,q s,u .

Thus, derivatives provide the coefficients of the finite-difference expansion, while the 10 mutations themselves are represented by finite changes in local emission factors. For single mutations, the first-order term is already exact. For double mutations, the mixed term 

∆p∆qP is required to obtain the exact finite difference. 

S1.3.2 Perturbations in SCFGs 

The same distinction between derivatives and finite differences appears in SCFGs. Deriva-tives with respect to local factors describe sensitivities, whereas sequence mutations replace such factors by finite amounts. For ordinary SCFGs with position-independent terminal emission probabilities, these finite sequence perturbations can be computed exactly using inside and outside quantities. Consider a Chomsky-normal-form SCFG with terminal productions v → c. Introduce the position-specific terminal factor 

ρp,v = ev(xp),

so that the inside base case is 

α(p, p, v ) = ρp,v .

The outside quantity is 

β(i, j, v ) = ∂P (x)

∂α (i, j, v ).

Consider a single mutation 

xp −→ c∗.

Every derivation tree contains exactly one terminal production that emits position p.Cutting the derivation tree at this terminal production decomposes the tree into an outside context and the local terminal emission. Therefore, 

P (xp→c∗

) = ∑

> v∈N

β(p, p, v )ev(c∗),

11 whereas 

P (x) = ∑

> v∈N

β(p, p, v )ev(xp).

Define 

∆pP = P (xp→c∗

) − P (x).

Then the exact finite difference is 

∆pP = ∑

> v∈N

β(p, p, v ) {ev(c∗) − ev(xp)} .

Equivalently, since 

∂P (x)

∂ρ p,v 

= β(p, p, v ),

we have 

∆pP = ∑

> v∈N

∂P (x)

∂ρ p,v 

∆ρp,v ,

where 

∆ρp,v = ev(c∗) − ev(xp).

This is again an exact finite difference because P (x) is linear in the position-specific local factors associated with a fixed position p.Now consider two mutations at positions p < q ,

xp → c∗, xq → d∗.

Let 

ρp,v = ev(xp), ρq,w = ew(xq),

and 

∆ρp,v = ev(c∗) − ev(xp), ∆ρq,w = ew(d∗) − ew(xq).

Define the total double-mutation finite difference by 

∆p,q P = P (xp→c∗,q →d∗

) − P (x).

12 Because the SCFG sequence probability is multi-affine across the position-specific terminal-factor groups, the exact double-mutation effect is 

∆p,q P = ∑

> v∈N

∂P (x)

∂ρ p,v 

∆ρp,v 

+ ∑

> w∈N

∂P (x)

∂ρ q,w 

∆ρq,w 

+ ∑

> v,w ∈N

∂2P (x)

∂ρ p,v ∂ρ q,w 

∆ρp,v ∆ρq,w .

The mixed derivative 

Γp,v ;q,w = ∂2P (x)

∂ρ p,v ∂ρ q,w 

is the total contribution of all derivation-tree contexts in which position p is emitted from nonterminal v and position q is emitted from nonterminal w. Equivalently, 

Γp,v ;q,w = ∂β (p, p, v )

∂ρ q,w 

= ∂β (q, q, w )

∂ρ p,v 

.

Define the mixed finite difference by 

∆p∆qP = P (xp→c∗,q →d∗

) − P (xp→c∗

)

− P (xq→d∗

) + P (x).

Then 

∆p∆qP = ∑

> v,w ∈N

Γp,v ;q,w ∆ρp,v ∆ρq,w ,

and therefore 

∆p,q P = ∆ pP + ∆ qP + ∆ p∆qP. 

Thus, as in the HMM case, the first two terms are the finite single-mutation contributions in the original sequence background, and the final term is the finite interaction term. Including this mixed term gives the exact double-mutation effect. 13 S2 Detailed Affine-Gap Alignment Derivations 

This note gives the complete sum–product affine-gap recursions, their adjoint backward recursions, posterior event probabilities, expected feature counts, and the exact one-site substitution formula summarized in the main text. 

## S2.1 Affine-gap alignment as a partition function 

Pairwise sequence alignment is the problem of inferring a latent alignment path between two observed sequences (Needleman and Wunsch, 1970). Let 

x = x1 · · · xL, y = y1 · · · yK

be two biological sequences. An alignment path π is a monotone path through the two-dimensional lattice [0 , L ]×[0 , K ], where each step corresponds to aligning a pair of symbols, aligning a symbol of x to a gap, or aligning a symbol of y to a gap. We formulate alignment as a Boltzmann ensemble over alignment paths with affine gap costs, following the probability-alignment viewpoint of Miyazawa (1995); see also Bucher and Hofmann (1996) and the pair-HMM formulation of Durbin et al. (1998). Let s(a, b )

be the substitution score for aligning symbols a and b. A gap of length n ≥ 1 is assigned the affine penalty 

g(n) = d + ( n − 1) e, 

where d is the gap-opening penalty and e is the gap-extension penalty. For a temperature parameter T > 0, define the local Boltzmann factors 

q(a, b ) = exp {s(a, b )/T },pd = exp {− d/T }, pe = exp {− e/T }.

Thus a gap of length n has weight 

exp {− g(n)/T } = pdpn−1 

> e

.

14 The factors q(a, b ), pd, and pe are local Boltzmann factors, not necessarily normalized probabilities. They play the same algebraic role as local transition and emission factors in probabilistic alignment models, and as local thermodynamic factors in RNA folding models. For an alignment path π, let NM (a, b ) be the number of aligned pairs (a, b ), and let 

Nopen and Next be the numbers of gap openings and gap extensions. The Boltzmann weight of π is 

w(π; x, y ) = ∏

> a,b

q(a, b )NM (a,b )pNopen  

> d

pNext  

> e

.

Equivalently, 

w(π; x, y ) = exp 

> 

1

T

> ∑
> a,b

s(a, b )NM (a, b ) − dN open − eN ext  

> 

.

The alignment partition function is 

Z(x, y ) = ∑

> π

w(π; x, y ),

where the summation is over all alignment paths consistent with x and y. The posterior distribution over alignment paths is 

P (π | x, y ) = w(π; x, y )

Z(x, y ) .

Because the Boltzmann transformation is monotone, the maximum-score affine-gap align-ment is a maximum-posterior alignment for every T > 0; as T → 0, the ensemble concentrates on maximum-score alignments. The partition function at finite T describes the full ensemble of possible alignments. To compute Z(x, y ), we introduce three forward variables. Let FM (i, j ) be the total weight of alignments of x1 · · · xi and y1 · · · yj that end by aligning xi with yj . Let FX (i, j )

be the total weight of alignments that end by aligning xi to a gap, and let FY (i, j ) be the total weight of alignments that end by aligning yj to a gap. Thus X denotes a gap in y,15 and Y denotes a gap in x. Define 

Z(i, j ) = FM (i, j ) + FX (i, j ) + FY (i, j ).

For clarity, we use the standard affine-gap convention in which a new gap run is opened from the match state and then extended within the same gap state. Direct transitions between X and Y states are disallowed; including them would only change the local transition structure, not the differential interpretation. We assume global alignment, with the usual boundary initialization accounting for leading and trailing affine gaps; other alignment modes alter the boundary conditions but not the identities below. A concrete global-alignment initialization is 

FM (0 , 0) = 1 , FX (0 , 0) = FY (0 , 0) = 0 ,FX (i, 0) = pdpi−1 

> e

, FM (i, 0) = FY (i, 0) = 0 (i ≥ 1) ,

and 

FY (0 , j ) = pdpj−1 

> e

, FM (0 , j ) = FX (0 , j ) = 0 (j ≥ 1) .

For i, j ≥ 1, the sum-product affine-gap recursions are 

FM (i, j ) = q(xi, y j )Z(i − 1, j − 1) ,FX (i, j ) = pdFM (i − 1, j ) + peFX (i − 1, j ),

and 

FY (i, j ) = pdFM (i, j − 1) + peFY (i, j − 1) .

The full alignment partition function is 

Z(x, y ) = Z(L, K ).

These equations are the sum-product counterpart of the usual affine-gap dynamic program 16 (Gotoh, 1982): the maximum operation in the optimal-alignment algorithm is replaced by summation over all alignment paths. This affine-gap Boltzmann formulation is closely related to a three-state pair-HMM. With a suitable normalized parameterization of transitions and state-specific emissions, the same recursion topology yields the standard pair-HMM forward algorithm. In the present section, however, we use the unnormalized Boltzmann form because it makes the analogy with RNA thermodynamic partition functions especially transparent. 

## S2.2 Backward variables as adjoints on the alignment lattice 

As in the HMM and SCFG cases, backward variables are defined as derivatives of the global partition function with respect to forward variables. For U ∈ { M, X, Y }, define 

BU (i, j ) = ∂Z (x, y )

∂F U (i, j ).

This definition makes the backward variables adjoint variables of the affine-gap forward dynamic program. Differentiating the forward recursions gives the corresponding backward recursions. With terminal conditions 

BM (L, K ) = BX (L, K ) = BY (L, K ) = 1 ,

and omitting out-of-range terms, we obtain 

BM (i, j ) = q(xi+1 , y j+1 )BM (i + 1 , j + 1) + pdBX (i + 1 , j ) + pdBY (i, j + 1) ,BX (i, j ) = q(xi+1 , y j+1 )BM (i + 1 , j + 1) + peBX (i + 1 , j ),BY (i, j ) = q(xi+1 , y j+1 )BM (i + 1 , j + 1) + peBY (i, j + 1) .

Thus the usual backward computation for the affine-gap alignment ensemble is reverse-mode differentiation of the forward computation on the alignment lattice. 17 Equivalently, each backward variable represents the total contribution of all suffix alignments, or outside contexts, compatible with a given forward state. This is the direct alignment analogue of HMM backward variables, SCFG outside variables, and RNA outside partition functions. 

## S2.3 Posterior residue correspondences 

The forward and backward variables give posterior probabilities of alignment events. In particular, the posterior probability that xi and yj are aligned is 

P (xi ∼ yj | x, y ) = FM (i, j )BM (i, j )

Z(x, y ) .

This is the alignment analogue of posterior state probabilities in HMMs, posterior con-stituent probabilities in SCFGs, and base-pairing probabilities in RNA secondary-structure ensembles. More generally, posterior probabilities of local events are obtained by normalizing inside–local–outside products. For example, the posterior probability that an X-gap is opened by aligning xi to a gap after a match state is 

FM (i − 1, j )pdBX (i, j )

Z(x, y ) .

The posterior probability that an X-gap is extended by aligning xi to a gap after a previous 

X-state is 

FX (i − 1, j )peBX (i, j )

Z(x, y ) .

Analogously, the posterior probabilities of opening and extending a Y -gap are 

FM (i, j − 1) pdBY (i, j )

Z(x, y )

and 

FY (i, j − 1) peBY (i, j )

Z(x, y ) ,

18 respectively. These expressions show that posterior alignment probabilities are normalized derivatives of the alignment partition function with respect to intermediate forward quantities and local factors. They are precisely the residue-correspondence probabilities obtained by summing the posterior probabilities of all alignment paths containing the corresponding local event. 

## S2.4 Alignment features as logarithmic derivatives 

The posterior probabilities above describe individual alignment events. They can also be aggregated to obtain expected feature counts over the entire alignment ensemble. For substitution features, let NM (a, b ) denote the number of times that symbols a and 

b are aligned along an alignment path. Then 

E[NM (a, b ) | x, y ] = ∑  

> i,j :xi=a, y j=b

P (xi ∼ yj | x, y ).

Equivalently, this expected count is the logarithmic derivative of the alignment partition function with respect to the substitution factor: 

∂ log Z(x, y )

∂ log q(a, b ) = E[NM (a, b ) | x, y ] .

Similarly, the expected numbers of gap openings and gap extensions are obtained as logarithmic derivatives with respect to the gap factors: 

∂ log Z(x, y )

∂ log pd

= E[Nopen | x, y ] , (S2.1) 

∂ log Z(x, y )

∂ log pe

= E[Next | x, y ] . (S2.2) Thus the local factors in the alignment column of Table 1 of the main manuscript are q(a, b ), pd, and pe: their logarithmic derivatives give expected substitution counts, expected gap-opening counts, and expected gap-extension counts, respectively. 19 These identities follow directly from the fact that each alignment path has weight 

w(π; x, y ) = ∏

> a,b

q(a, b )NM (a,b )pNopen  

> d

pNext  

> e

.

In terms of the original scoring parameters, the same identities become 

∂ log Z(x, y )

∂s (a, b ) = 1

T E[NM (a, b ) | x, y ] , (S2.3) 

∂ log Z(x, y )

∂d = − 1

T E[Nopen | x, y ] , (S2.4) 

∂ log Z(x, y )

∂e = − 1

T E[Next | x, y ] . (S2.5) Thus substitution scores and affine-gap penalties play the same mathematical role as local factors in HMMs, SCFGs, and RNA folding models: logarithmic derivatives of the partition function reveal how often the corresponding local features are used in the posterior ensemble. 

## S2.5 Sequence perturbations in alignment ensembles 

Sequence substitutions can also be interpreted as finite replacements of local alignment factors. Consider a mutation in the first sequence, 

xi −→ x∗ 

> i

.

This mutation does not change the combinatorial set of possible alignment paths. In the affine-gap formulation above, it changes only the substitution factors associated with match events that align position i of x to positions of y:

q(xi, y j ) −→ q(x∗ 

> i

, y j ), 1 ≤ j ≤ K. 

The gap factors pd and pe do not depend on the identity of the emitted symbol, and therefore a substitution does not change the weight of alignment paths in which xi is aligned to a gap. 20 For any fixed alignment path, position i of the first sequence is consumed exactly once: either it is matched to one position j of the second sequence, or it is aligned to a gap. Since only the match factors depend on the identity of xi, the alignment partition function is affine in the position-specific match-factor group 

ρMi,j = q(xi, y j ).

A path that aligns xi to a gap uses no factor from this group and hence contributes the constant term. Thus the dependence is multi-affine but is not homogeneous of degree one in the match-factor group. Let 

∆iZ = Z(xi→x∗ 

> i

, y ) − Z(x, y ).

Then 

∆iZ =

> K∑
> j=1

∂Z (x, y )

∂ρ Mi,j 

{q(x∗ 

> i

, y j ) − q(xi, y j )}

=

> K∑
> j=1

FM (i, j )BM (i, j ) 

> {

q(x∗ 

> i

, y j )

q(xi, y j ) − 1

> }

.

This is an exact finite difference, not a first-order approximation. Here, 

∂Z (x, y )

∂ρ Mi,j 

= FM (i, j )BM (i, j )

q(xi, y j ) .

Dividing by Z(x, y ), the same identity can be expressed in terms of posterior residue-correspondence probabilities: 

∆iZZ(x, y ) =

> K∑
> j=1

P (xi ∼ yj | x, y ) 

> {

q(x∗ 

> i

, y j )

q(xi, y j ) − 1

> }

.

Thus, for a single substitution in one sequence, the finite change of the alignment partition function is exact and has the same inside–local–outside structure as the finite mutation formulas for HMMs and SCFGs. If one uses a more general pair-HMM in which gap emissions depend on the identity of the emitted symbol, then the substitution also replaces the corresponding gap-emission 21 factors. In that case, additional terms of the same form appear for the gap states. The essential point is unchanged: the mutation is represented by finite replacement of the local factors that consume the mutated position. The corresponding finite change in the log-partition function is 

∆ log Z = log 

> {

1 + ∆iZZ(x, y )

> }

.

Linearizing this expression gives the usual first-order sensitivity formula in terms of posterior alignment probabilities and logarithmic changes of substitution factors. For multiple substitutions, mixed terms can appear when affected local factors are used together in the same alignment path, and the exact finite expansion then requires the corresponding higher-order recombination terms. This exact single-site formula relies on the fact that each alignment path consumes the mutated position exactly once and uses at most one identity-dependent match factor from its position group. Paths aligning the position to a gap use none of these factors and supply the constant term. In RNA secondary-structure ensembles, the analogous situation is more complicated. A base mutation can change the sequence-dependent Boltzmann factors of many alternative loop, stacking, and multiloop decompositions, and in nearest-neighbor models it can also change the boundary contexts of dynamic-programming cells. Thus, exact finite mutation analysis in RNA folding requires recombination over all affected local decompositions, rather than a single local replacement. We turn to this case next. 

# S3 Relation to Neural-Network Backpropagation 

This appendix explains the relation between the forward–backward algorithm for HMMs and backpropagation in feed-forward neural networks. The purpose is not to regard HMMs as neural networks, but to clarify that the backward algorithm has the same algebraic form as reverse-mode differentiation of a feed-forward computation graph. 22 S3.1 Feed-forward computation and backpropagation 

Consider a feed-forward neural network with layer index t. Let h(t−1)  

> i

be the activation of unit i in layer t − 1, and let a(t) 

> j

and h(t) 

> j

be the pre-activation and activation of unit j in layer t. The forward propagation is written as 

a(t) 

> j

= ∑

> i

w(t) 

> ji

h(t−1)  

> i

, (S3.1) 

h(t) 

> j

= σ(

a(t)

> j
> )

, (S3.2) where w(t) 

> ji

is the weight from unit i to unit j, and σ is an activation function. Let L be a scalar output or loss to be differentiated. Define the backpropagated error with respect to the pre-activation by 

δ(t) 

> j

= ∂L ∂a (t)

> j

. (S3.3) Then the standard backpropagation recursion is 

δ(t−1)  

> i

= σ′(

a(t−1) 

> i
> ) ∑
> j

w(t) 

> ji

δ(t) 

> j

. (S3.4) Equivalently, if we define the adjoint variable with respect to the activation h(t−1)  

> i

, then 

∂L ∂h (t−1) 

> i

= ∑

> j

w(t)

> ji

∂L ∂a (t)

> j

. (S3.5) Thus, backpropagation is reverse-mode differentiation of the feed-forward computation graph. 

## S3.2 HMM forward–backward algorithm as a special case 

For an HMM, the forward variable is computed as 

fj (t) = ∑

> i∈Q

ai,j ej (xt)fi(t − 1) . (S3.6) 23 This recursion has the same form as a feed-forward computation if we set 

h(t) 

> j

= fj (t), w(t) 

> ji

= ai,j ej (xt), σ(a) = a. (S3.7) Therefore, the HMM forward algorithm is a linear feed-forward computation over the sequence positions. The activation function is the identity map, and hence σ′(a) = 1 .Let P (x) be the total probability of the observed sequence. In the forward computation graph, P (x) is the scalar output. Define 

δi(t) = ∂P (x)

∂f i(t) . (S3.8) Using the HMM forward recursion at the next position, 

fj (t + 1) = ∑

> i∈Q

ai,j ej (xt+1 )fi(t), (S3.9) reverse-mode differentiation gives 

δi(t) = ∑

> j∈Q

∂P (x)

∂f j (t + 1) 

∂f j (t + 1) 

∂f i(t) (S3.10) 

= ∑

> j∈Q

δj (t + 1) ai,j ej (xt+1 ). (S3.11) This is exactly the HMM backward recursion: 

bi(t) = ∑

> j∈Q

ai,j ej (xt+1 )bj (t + 1) . (S3.12) Therefore, 

bi(t) = δi(t) = ∂P (x)

∂f i(t) . (S3.13) The adjoint depends on the chosen scalar output: with the loss L = Z = P (x) one has 

δi = bi, whereas with L = log Z the chain rule gives δi = bi/Z .In this sense, the HMM backward algorithm is the special case of neural-network backpropagation in which the computation is linear and the activation function is the 24 identity map. 

## S3.3 Expected counts as gradient accumulation 

We finally compare expected counts in HMMs with gradient computation for shared parameters in neural networks. In a feed-forward neural network, suppose that a parameter w is shared and used at multiple places in the computation graph. For example, let w appear as the weight w(t) 

> ji

for several layers or positions t. By the chain rule, the gradient of the loss L with respect to the shared parameter w is obtained by accumulating the contributions from all of its uses: 

∂L ∂w = ∑  

> t:w(t)
> ji =w

∂L ∂w (t)

> ji

. (S3.14) For a single use in the feed-forward computation 

a(t) 

> j

= ∑

> i

w(t) 

> ji

h(t−1)  

> i

, (S3.15) the local contribution is 

∂L ∂w (t)

> ji

= ∂L ∂a (t)

> j

∂a (t)

> j

∂w (t)

> ji

= δ(t) 

> j

h(t−1)  

> i

. (S3.16) Therefore, for a shared weight w, backpropagation computes 

∂L ∂w = ∑  

> t:w(t)
> ji =w

δ(t) 

> j

h(t−1)  

> i

. (S3.17) Thus, the gradient with respect to a shared parameter is the sum of local backpropagated contributions over all places where the parameter is used. The expected-count formula in HMMs has exactly the same algebraic structure. Con-sider the transition probability ar,s . This parameter is shared across all sequence positions. In the HMM forward computation, the use of ar,s at position t contributes to 

fs(t) = ∑

> r′∈Q

fr′ (t − 1) ar′,s es(xt). (S3.18) 25 The local derivative contribution from this use is 

∂P (x)

∂a r,s  

> ∣∣∣∣∣∣position t

= ∂P (x)

∂f s(t)

∂f s(t)

∂a r,s 

= bs(t)fr(t − 1) es(xt). (S3.19) Accumulating the contributions over all positions gives 

∂P (x)

∂a r,s 

= ∑

> t

fr(t − 1) es(xt)bs(t). (S3.20) This is the direct analogue of (S3.17). Taking the logarithmic derivative gives 

∂ log P (x)

∂ log ar,s 

= ar,s 

P (x)

∂P (x)

∂a r,s 

(S3.21) 

= 1

P (x)

> ∑
> t

fr(t − 1) ar,s es(xt)bs(t). (S3.22) The summand 

fr(t − 1) ar,s es(xt)bs(t)

P (x) (S3.23) is the posterior probability that the transition r → s is used at position t. Therefore, 

∂ log P (x)

∂ log ar,s 

= ∑

> t

P (πt−1 = r, π t = s | x) = E[

N (T ) 

> r,s

| x]

. (S3.24) Similarly, for the emission probability es(c), which is also shared over all positions at which symbol c may be emitted from state s, we obtain 

∂ log P (x)

∂ log es(c) = E[

N (E) 

> s,c

| x]

. (S3.25) Thus, the expected transition and emission counts in HMMs are precisely the logarithmic gradients of shared parameters. This is the same principle as gradient accumulation for shared weights in neural-network backpropagation. 26 S4 RNA Partition Functions, Outside Quantities, and Finite Mutation Recombination 

This appendix gives explicit recombination formulas for finite sequence mutations in RNA-like dynamic programs. The purpose is to make precise the statement in the RNA recombination section of the main manuscript that exact mutation effects are obtained by inside–local–outside recombination, rather than by a first-order Taylor approximation. 

## S4.1 Simplified McCaskill recursions and boundary conditions 

We first recall a simplified McCaskill-type set of recursions (McCaskill, 1990). These recursions are not intended to reproduce all details of a full Turner nearest-neighbor implementation, but to make explicit the algebraic structure of inside quantities, outside quantities, and local Boltzmann factors. We use the following interval partition functions. Zi,j is the partition function over all structures on interval [i, j ]. Z1 

> i,j

denotes the contribution of structures on [i, j ] whose leftmost paired component starts at position i. In the simplified recursion below, 

Z1 

> i,j

= ∑

> i<k ≤j

Zbi,k .

Thus the component starts at i, closes at k, and the remaining positions k + 1 , . . . , j , if any, are unpaired in the external region. Zbi,j is the partition function over structures in which i and j form a base pair. Zmi,j is the partition function of a multiloop segment on 

[i, j ] containing at least one base-paired component. Zm1 

> i,j

is the contribution of a multiloop segment containing one base-paired component starting at i, followed by unpaired bases. For notational compactness, write the local Boltzmann factors as (with R the gas constant and T the absolute temperature) 

ϕ1(i, j ) = exp {− f1(i, j )/RT },ϕ2(i, j, k, ℓ ) = exp {− f2(i, j, k, ℓ )/RT },

27 ϕ3(i, j ) = exp {− f3(i, j )/RT },

and 

ϕu 

> 4

(a, b ) = exp {− f4(a, b )/RT }, ϕ5 = exp {− f5/RT }.

Here ϕ1 represents a hairpin-like contribution, ϕ2 an interior-loop or stacking contribution, 

ϕ3 a multiloop-closing contribution, ϕu 

> 4

the Boltzmann weight of an unpaired segment inside a multiloop, and ϕ5 a branch contribution inside a multiloop. We use the conventions 

ϕu 

> 4

(a, a − 1) = 1 , Zma,a −1 = 0 , Zm1 

> a,a −1

= 0 ,

for empty intervals. An empty unpaired segment contributes a unit Boltzmann factor, whereas an empty interval contains no base-paired component and therefore contributes nothing to Zm or Zm1.A simplified McCaskill-type recursion can then be written as 

Zi,i −1 = 1 ,Zi,j = 1 + ∑

> i≤k<j

Zi,k −1Z1 

> k,j

,Z1 

> i,j

= ∑

> i<k ≤j

Zbi,k ,Zbi,j = ϕ1(i, j ) + ∑

> i<k<ℓ<j

Zbk,ℓ ϕ2(i, j, k, ℓ )+ ∑

> i+1 <k<j

Zmi+1 ,k −1Zm1

> k,j −1

ϕ3(i, j ),Zmi,j = ∑

> i≤k<j
> {

ϕu 

> 4

(i, k − 1) + Zmi,k −1

> }

Zm1 

> k,j

ϕ5,Zm1 

> i,j

= ∑

> i<k ≤j

Zbi,k ϕu 

> 4

(k + 1 , j ).

The term ϕu 

> 4

(i, k − 1) in the recursion for Zmi,j represents the case in which the prefix 

[i, k − 1] is entirely unpaired inside the multiloop. The term Zmi,k −1 represents the case in which that prefix already contains one or more base-paired components. Thus the bracketed term is a partition function over possible multiloop prefixes ending before k.28 The total partition function is Z1,L .

## S4.2 Outside adjoints and local decomposition contributions 

The corresponding outside quantities are defined as adjoint variables: 

Wi,j = ∂Z 1,L 

∂Z i,j 

, W 1 

> i,j

= ∂Z 1,L 

∂Z 1

> i,j

,W bi,j = ∂Z 1,L 

∂Z bi,j 

, W mi,j = ∂Z 1,L 

∂Z mi,j 

, W m1 

> i,j

= ∂Z 1,L 

∂Z m1

> i,j

.

These outside quantities are the McCaskill-type counterparts of SCFG outside variables. In particular, because a pseudoknot-free structure uses a coordinate-specific base pair at most once, its posterior probability is 

P (i, j | x) = Zbi,j W bi,j 

Z1,L 

.

This is the RNA analogue of the HMM state posterior and the SCFG constituent posterior. The outside quantities are seeded at the root by 

W1,L = ∂Z 1,L 

∂Z 1,L 

= 1 ,

with all other outside quantities initialized to 0. They are then accumulated by the rules below, evaluated in an order compatible with reverse-mode differentiation, for example from larger intervals to smaller intervals. Each “ +=” rule is understood to be summed over all parent intervals and cut points that produce the indicated child. Since W denotes a total adjoint, no additional upstream contribution is added when differentiating a local factor. The outside recursions are obtained by reverse-mode differentiation of the above inside recursions. Equivalently, each term in an inside recursion propagates an adjoint from a 29 parent variable to its child variables. For example, from 

Zi,j = 1 + ∑

> i≤k<j

Zi,k −1Z1 

> k,j

,

we obtain the adjoint contributions 

Wi,k −1 += Wi,j Z1 

> k,j

, W 1 

> k,j

+= Wi,j Zi,k −1.

From 

Z1 

> i,j

= ∑

> i<k ≤j

Zbi,k ,

we obtain 

W bi,k += W 1 

> i,j

(i < k ≤ j).

From the interior-loop term in Zbi,j ,

Zbi,j ⊃ Zbk,ℓ ϕ2(i, j, k, ℓ ),

we obtain 

W bk,ℓ += W bi,j ϕ2(i, j, k, ℓ ).

From the multiloop-closing term in Zbi,j ,

Zbi,j ⊃ Zmi+1 ,k −1Zm1

> k,j −1

ϕ3(i, j ),

we obtain 

W mi+1 ,k −1 += W bi,j Zm1

> k,j −1

ϕ3(i, j ),

and 

W m1 

> k,j −1

+= W bi,j Zmi+1 ,k −1ϕ3(i, j ).

Similarly, from 

Zmi,j = ∑

> i≤k<j

{

ϕu 

> 4

(i, k − 1) + Zmi,k −1

}

Zm1 

> k,j

ϕ5,

30 we obtain 

W mi,k −1 += W mi,j Zm1 

> k,j

ϕ5,

and 

W m1 

> k,j

+= W mi,j 

> {

ϕu 

> 4

(i, k − 1) + Zmi,k −1

> }

ϕ5.

Finally, from 

Zm1 

> i,j

= ∑

> i<k ≤j

Zbi,k ϕu 

> 4

(k + 1 , j ),

we obtain 

W bi,k += W m1 

> i,j

ϕu 

> 4

(k + 1 , j ) (i < k ≤ j).

The same adjoint quantities also give derivatives with respect to local Boltzmann factors. These derivatives should be understood as sums over direct occurrences of the corresponding local factor, with the W -quantities already representing total adjoints. For example, 

∂Z 1,L 

∂ϕ 1(i, j ) = W bi,j ,∂Z 1,L 

∂ϕ 2(i, j, k, ℓ ) = W bi,j Zbk,ℓ ,

and 

∂Z 1,L 

∂ϕ 3(i, j ) = W bi,j 

> ∑
> i+1 <k<j

Zmi+1 ,k −1Zm1

> k,j −1

.

For a nonempty unpaired-segment factor ϕu 

> 4

(a, b ), with a ≤ b, the total derivative is the sum of its direct occurrences, each weighted by the total adjoint of the parent variable in which it occurs. The empty segment has the fixed value ϕu 

> 4

(a, a − 1) = 1 and is a constant, so no parameter derivative is taken with respect to it. In the recursion for Zm,the factor ϕu 

> 4

(a, b ) occurs as the unpaired prefix term with i = a and k − 1 = b, that is, 

k = b + 1 . The constraint i ≤ k < j therefore gives b + 1 < j ≤ L, and the corresponding contribution is 

∂Z 1,L 

∂ϕ u 

> 4

(a, b )

> ∣∣∣∣∣Zm

= ∑

> b+1 <j ≤L

W ma,j Zm1 

> b+1 ,j

ϕ5.

In the recursion for Zm1, the factor ϕu 

> 4

(a, b ) occurs as the unpaired suffix term with 31 k + 1 = a and j = b, that is, k = a − 1. The constraint i < k ≤ j therefore gives 

1 ≤ i ≤ a − 2, and the corresponding contribution is 

∂Z 1,L 

∂ϕ u 

> 4

(a, b )

> ∣∣∣∣∣Zm1

= ∑

> 1≤i≤a−2

W m1 

> i,b

Zbi,a −1.

Thus, 

∂Z 1,L 

∂ϕ u 

> 4

(a, b ) = ∂Z 1,L 

∂ϕ u 

> 4

(a, b )

> ∣∣∣∣∣Zm

+ ∂Z 1,L 

∂ϕ u 

> 4

(a, b )

> ∣∣∣∣∣Zm1

.

No additional recursive term is required: the dependence of Z1,L on ϕu 

> 4

(a, b ) through nested 

Zm cells is already contained in the total adjoints W m.These derivatives give local sensitivities. Exact finite mutations, however, are obtained by replacing the affected local factors by their mutant values and recombining them with the appropriate inside and outside quantities, as described below. 

## S4.3 Context-indexed finite recombination 

The displayed formulas use the notation of the RNA-like grammar specialized in the following subsection. The use of context-indexed inside and outside copies, however, is the part of the construction that extends to nearest-neighbor energy models. In nearest-neighbor RNA energy models, local factors may depend on bases neighboring the current dynamic-programming cell. In such cases, the ordinary inside and outside variables are not sufficient, because a mutation at position j can change the boundary conditions of nearby cells. Exact recombination is still possible by using context-dependent inside and outside variables. Following the Rchange construction, introduce context-dependent inside variables 

α(i, j, (c1, c 2)) .

For an inside variable, the context pair (c1, c 2) specifies the bases immediately outside the interval, namely the bases at positions i − 1 and j + 1 . Similarly, introduce context-32 dependent outside variables 

β(i, j, (c1, c 2)) .

For an outside variable, the context pair specifies the boundary bases at positions i and 

j. Thus the two context slots have different meanings for inside and outside variables: outside-neighbor bases for α, and boundary bases for β. This convention is essential for interpreting the formula below. For a single mutation xj → c∗, the exact recombination formula becomes 

Z(xj→c∗

) = ∑

> j+1 <k ≤L

α(j + 1 , k − 1, (c∗, x k)) eP (c∗, x k | j, k )t(P )β(j, k, (c∗, x k)) + ∑

> 1≤k<j −1

α(k + 1 , j − 1, (xk, c ∗)) eP (xk, c ∗ | k, j )t(P )β(k, j, (xk, c ∗)) + ∑

> j<k ≤L

α(j + 1 , k, (c∗, x k+1 )) eL(c∗ | j)t(L)β(j, k, (c∗, x k)) + ∑

> 1≤k<j

α(k, j − 1, (xk−1, c ∗)) eR(c∗ | j)t(R)β(k, j, (xk, c ∗)) .

Here eP (a, b | u, v ), eL(a | u), and eR(a | u) denote local factors whose values may depend on neighboring bases around the indicated positions. At sequence boundaries, x0 and xL+1 

denote fixed boundary symbols, or equivalently the corresponding boundary conditions of the dynamic program. For example, in the third term, the inside interval is [j + 1 , k ], so its right outside neighbor is xk+1 . In contrast, the outside variable β(j, k, ·) carries the boundary base xk on the right. Therefore the appearances of xk+1 and xk in that term are intentional. In Rchange, for a single mutation, the context pair is restricted to the nucleotide pairs that differ from the original boundary pair by at most one base. This finite set of context-dependent copies makes it possible to include the boundary effects of the mutation exactly without recomputing the full dynamic program for every mutant. Replacing c∗ by the original base xj gives the corresponding decomposition of Z(x).Therefore, subtracting the two expressions gives the exact single-mutation difference in the neighbor-dependent model. 33 S4.4 RNA-like SCFG specialization and explicit formulas 

We first recall the exact recombination formula for the simple RNA-like SCFG used in Rchange. This simple grammar has a single nonterminal S and the five transition rules 

P : S → a<Sa >, L : S → aS, R : S → Sa, B : S → SS, E : S → ϵ. 

Here a< and a> denote the two terminals of a paired emission, whereas a denotes an unpaired terminal. The nonterminal index of the inside and outside variables is therefore suppressed. The grammar has four types of local decompositions that can emit a base at position j: a paired emission in which j is the left base of a pair, a paired emission in which j is the right base of a pair, a left unpaired emission, and a right unpaired emission. The four-term expression below reproduces Eq. (2) of Kiryu and Asai (2012); its strict and non-strict index bounds follow from these grammar rules and should not be altered independently. Consider a single mutation 

xj −→ c∗.

Let α and β be the inside and outside variables computed for the original sequence x; here 

β(i, j ) is the SCFG outside variable. Define the recombination operator Rj (c; α, β, x ) by 

Rj (c; α, β, x ) = ∑

> j+1 <k ≤L

α(j + 1 , k − 1) eP (c, x k)t(P )β(j, k )+ ∑

> 1≤k<j −1

α(k + 1 , j − 1) eP (xk, c )t(P )β(k, j )+ ∑

> j<k ≤L

α(j + 1 , k )eL(c)t(L)β(j, k )+ ∑

> 1≤k<j

α(k, j − 1) eR(c)t(R)β(k, j ).

Then the mutant partition function is given exactly by 

Z(xj→c∗

) = Rj (c∗; α, β, x ).

34 Similarly, the original partition function can be decomposed as 

Z(x) = Rj (xj ; α, β, x ).

Therefore the exact finite mutation effect is 

Z(xj→c∗

) − Z(x) = Rj (c∗; α, β, x ) − R j (xj ; α, β, x ).

Equivalently, 

Z(xj→c∗

) − Z(x)= ∑

> j+1 <k ≤L

α(j + 1 , k − 1) {eP (c∗, x k) − eP (xj , x k)} t(P )β(j, k )+ ∑

> 1≤k<j −1

α(k + 1 , j − 1) {eP (xk, c ∗) − eP (xk, x j )} t(P )β(k, j )+ ∑

> j<k ≤L

α(j + 1 , k ) {eL(c∗) − eL(xj )} t(L)β(j, k )+ ∑

> 1≤k<j

α(k, j − 1) {eR(c∗) − eR(xj )} t(R)β(k, j ).

This is an exact finite difference. No first-order approximation is involved. The recombination formulas above were written using the SCFG notation α and β. In McCaskill-type RNA partition-function algorithms, the same roles are played by interval partition functions and their adjoint outside quantities. Thus, schematically, 

α ←→ Z, Z b, Z m, Z m1,

and 

β ←→ W, W b, W m, W m1.

This correspondence is schematic rather than one-to-one: a single SCFG inside variable may correspond to different McCaskill interval variables depending on the type of structural decomposition being considered. The correspondence can be seen directly from local decomposition terms. Consider the 35 recursion 

Zbi,j = ϕ1(i, j ) + ∑

> i<k<ℓ<j

Zbk,ℓ ϕ2(i, j, k, ℓ ) + ∑

> i+1 <k<j

Zmi+1 ,k −1Zm1

> k,j −1

ϕ3(i, j ).

Each term represents one possible local decomposition of a structure closed by the base pair (i, j ). Since W bi,j is the outside contribution of Zbi,j , the corresponding contributions to the global partition function Z1,L are 

W bi,j ϕ1(i, j ),W bi,j Zbk,ℓ ϕ2(i, j, k, ℓ ),

and 

W bi,j Zmi+1 ,k −1Zm1

> k,j −1

ϕ3(i, j ).

These are McCaskill-type analogues of the SCFG inside–local–outside product 

β × local factor × α. 

If a mutation changes only the local Boltzmann factor in one of these decomposition terms, the corresponding single-term finite replacement is obtained by replacing that factor by its mutant value while keeping the appropriate inside and outside contexts. For example, a finite replacement of ϕ2(i, j, k, ℓ ) by ϕ∗

> 2

(i, j, k, ℓ ) changes this single decomposition term by 

W bi,j Zbk,ℓ {ϕ∗

> 2

(i, j, k, ℓ ) − ϕ2(i, j, k, ℓ )} .

Similarly, a finite replacement of ϕ3(i, j ) in the multiloop decomposition gives the single-term contribution 

W bi,j 

> ∑
> i+1 <k<j

Zmi+1 ,k −1Zm1 

> k,j −1

{ϕ∗

> 3

(i, j ) − ϕ3(i, j )} .

36 The same correspondence also holds for multiloop segment recursions. From 

Zmi,j = ∑

> i≤k<j
> {

ϕu 

> 4

(i, k − 1) + Zmi,k −1

> }

Zm1 

> k,j

ϕ5,

a finite replacement of the direct unpaired-prefix factor ϕu 

> 4

(i, k − 1) by ϕu, ∗ 

> 4

(i, k − 1) gives the single-term contribution 

W mi,j Zm1 

> k,j

ϕ5 {ϕu, ∗ 

> 4

(i, k − 1) − ϕu 

> 4

(i, k − 1) } .

Likewise, from 

Zm1 

> i,j

= ∑

> i<k ≤j

Zbi,k ϕu 

> 4

(k + 1 , j ),

a finite replacement of ϕu 

> 4

(k + 1 , j ) gives 

W m1 

> i,j

Zbi,k {ϕu, ∗ 

> 4

(k + 1 , j ) − ϕu 

> 4

(k + 1 , j )} .

These expressions describe changes in individual local decomposition terms. They are not, by themselves, complete mutation formulas. A base mutation at position p changes every sequence-dependent local factor whose energy depends on p. The exact mutation effect is obtained by summing over nonoverlapping recombination blocks, with all primitive sequence-dependent factors affected within each block replaced jointly; equivalently, the block-local weight may be regarded as the product of those primitive factors. Independent single-factor differences evaluated entirely in the original background are not generally additive. Moreover, in nearest-neighbor models, when an affected factor depends on boundary bases of a dynamic-programming cell, the appropriate context-dependent inside and outside quantities must be used, as described in the previous subsection. For realistic nearest-neighbor RNA energy models, a mutation can change the sequence-dependent energy of a local loop, stack, dangle, terminal mismatch, or multiloop component. In such cases, ordinary inside and outside variables computed for the original sequence are not always sufficient. One must use context-dependent inside and outside variables, as in Rchange, so that the appropriate boundary bases are fixed when the mutant local Boltz-37 mann factor is inserted. Nevertheless, the algebraic structure remains the same: products of outside context , mutant local Boltzmann factor , and inside child partition functions .Thus, the Rchange algorithm can be regarded as a finite-recombination version of the same adjoint decomposition that gives outside partition functions by differentiation. 

## S4.5 Multiple mutations and numerical specialization 

Double mutations can be computed by applying the same exact single-site recombination formula in a single-mutant background. Suppose that 

xi → c∗∗ , xj → c∗, i < j. 

Let 

x′ = xj→c∗

be the sequence in which the j-th base has already been mutated. Let α′ and β′ denote the inside and outside variables computed for the single-mutant background x′. Then the double-mutant partition function is 

Z(xi→c∗∗ , j →c∗

) = Ri(c∗∗ ; α′, β ′, x ′).

Thus, 

Z(xi→c∗∗ , j →c∗

) − Z(x)

is obtained exactly by subtracting Z(x) from this recombined partition function. This expression is not the sum of two single-mutation effects in the original background. The effect of the mutation at i is evaluated in the background x′, and therefore all interactions between the two mutations are included through the inside and outside variables α′ and β′. The same final partition function is obtained if the mutations are applied in the opposite order, although different intermediate inside and outside variables may be used. In implementations such as Rchange, many entries of α′ and β′ can be reused from the original sequence x, because only dynamic-programming cells whose 38 decompositions depend on the first mutation need to be updated. The double-mutant construction above extends to the neighbor-dependent model by replacing the simple-grammar operator Ri with the context-dependent recombination operator introduced earlier in this appendix. The numerical verification in the main text exercises the RNA-like SCFG specialization of this construction, through the Numerical Verification section and the RNA rows of Table 2; it does not exercise a full Turner nearest-neighbor implementation. 

# S5 Exponential-Family and Information-Geometric Structure of RNA Folding 

## S5.1 RNA folding as an exponential-family model 

As shown in the parameter-derivative discussion of the main manuscript, with structure energy E(s) = ∑ 

> k

θkfk(s) and natural parameters ηk = −θk/RT (R the gas constant, T

the absolute temperature), the Boltzmann distribution over structures is an exponential family, 

P (s | x) = exp 

> (∑
> k

ηkfk(s) − Ψ( η)

> )

, Ψ( η) = log Z(x),

with sufficient statistics given by the loop-level structural features fk(s). Here we record the resulting second-order structure. 

## S5.2 Gradient structure and expected statistics 

A fundamental property of exponential-family models is that derivatives of the log-partition function yield expectations of sufficient statistics (Wainwright and Jordan, 2008). In the present setting, this gives 

∂Ψ

∂η k

= E[fk(s)] . (S5.1) Equivalently, in terms of the original thermodynamic parameters, 39 ∂ log Z∂θ k

= − 1

RT E[fk(s)] .

This result recovers the parameter-derivative identity discussed in the main manuscript and provides a statistical interpretation of thermodynamic quantities: the gradient of the log-partition function corresponds to expected structural features in the ensemble. 

## S5.3 Second-order structure and covariance 

Taking second derivatives yields 

∂2Ψ

∂η k∂η k′

= Cov( fk, f k′ ). (S5.2) Equivalently, in the original energy parameters, 

∂2 log Z∂θ k ∂θ k′

= 1(RT )2 Cov( fk, f k′ ).

Thus, the Hessian of the log-partition function encodes correlations between structural features. In natural coordinates this covariance matrix is the Fisher information matrix of the model (possibly singular for a nonminimal feature representation) and characterizes the local sensitivity of the distribution to parameter perturbations. Because this Hessian is a covariance matrix, it is positive semidefinite; equivalently, the log-partition function Ψ( η) = log Z is convex in the natural parameters η, and log Z

is likewise convex in the energy parameters θ, which enter linearly through ηk = −θk/RT .This convexity governs parameter estimation and sensitivity within the exponential family. It does not, however, extend to optimization over sequences: no corresponding convexity guarantee is available for optimization over the discrete sequence space, and the finite-mutation recombination of Section S4 carries no global-optimality guarantee inherited from convexity in parameter space. From a practical perspective, this implies that structural features in RNA folding are generally coupled: for example, the formation of one loop type may influence the likelihood 40 of others. The covariance structure captures these dependencies explicitly. 

# Supplementary References 

Philipp Bucher and Kay Hofmann. A sequence similarity search algorithm based on a probabilistic interpretation of an alignment scoring system. In Proceedings of the Fourth International Conference on Intelligent Systems for Molecular Biology (ISMB) , pages 44–51, 1996. Richard Durbin, Sean R. Eddy, Anders Krogh, and Graeme Mitchison. Biological Sequence Analysis: Probabilistic Models of Proteins and Nucleic Acids . Cambridge University Press, Cambridge, 1998. Osamu Gotoh. An improved algorithm for matching biological sequences. Journal of Molecular Biology , 162(3):705 – 708, 1982. Taishin Kin, Koji Tsuda, and Kiyoshi Asai. Marginalized kernels for RNA sequence data analysis. Genome Informatics , 13:112–122, 2002. doi: 10.11234/gi1990.13.112. Hisanori Kiryu and Kiyoshi Asai. Rchange: algorithms for computing energy changes of RNA secondary structures in response to base mutations. Bioinformatics , 28(8):1093 – 1101, 2012. John S McCaskill. The equilibrium partition function and base pair binding probabilities for RNA secondary structure. Biopolymers , 29(6-7):1105–1119, 1990. Sanzo Miyazawa. A reliable sequence alignment method based on probabilities of residue correspondences. Protein Engineering , 8(10):999 – 1009, 1995. Saul B. Needleman and Christian D. Wunsch. A general method applicable to the search for similarities in the amino acid sequence of two proteins. Journal of Molecular Biology ,48(3):443 – 453, 1970. Koji Tsuda, Taishin Kin, and Kiyoshi Asai. Marginalized kernels for biological sequences. 

Bioinformatics , 18(Suppl 1):S268–S275, 2002. 41 Martin J. Wainwright and Michael I. Jordan. Graphical models, exponential families, and variational inference. Foundations and Trends in Machine Learning , 1(1–2):1–305, 2008. doi: 10.1561/2200000001. 42